# 忆述光华 · 重构批次 H 提示词（2026-08-27）

> 来源：docs/重构执行计划_20260827.md 批次 H（杂项收口，R1/R4/R7/R8 低优先，最后一批）
> 前置：批次 A-G 已全部合入，**基准 develop @ 433444b**（F-Events + G1/G2 均已集成；以最新代码为准，不得回退 F-Content/F-Events/F-Auth/G1/G2 成果）。
> 原则：行为等价迁移先行；每 Agent 文件域零重叠；不 push（集成 Agent 统一 merge）；快速门禁过 commit；踩坑 `python scripts/lessons.py add` 登记；完成后显式呼叫集成 Agent。
> **功能防丢门禁（本批强制）**：每 Agent 提交前后 diff 三张契约快照（`docs/openapi.json`、`backend/app/core/errors.py` 的 ERROR_REGISTRY、`feature_list.json`）——只允许新增，不允许无声消失；真删必须显式登记 + 集成 Agent 确认。完成后 `git grep` 双验证：旧实现调用点=0（删干净）、新实现有真实消费方（没丢功能）。

---

## H 批次并行性（5 Agent 全并行，文件域零重叠）

| Agent | 任务 | 文件域 | 可并行 |
|---|---|---|---|
| H1 | 后端杂项收口（R1 系列） | `backend/app/db/models.py`、`backend/app/services/`（upload.py、wechat/、notify.py、pipeline.py、event_aggregation/）、`scripts/`（仅 event_aggregation 开发脚本迁移） | ✅ 与 H2-H5 互斥 |
| H2 | CI 增强（R7 系列） | `.github/workflows/ci.yml`、`scripts/test_agent.py`、`scripts/warm_hf_models.py`、requirements 相关 | ✅ |
| H3 | 测试杂项（R8 剩余） | `backend/tests/`、pytest 配置、`backend/tests/conftest.py` | ✅ |
| H4 | API 契约收口（R4 剩余） | `backend/app/api/`、`backend/app/schemas/`、`backend/app/core/errors.py` | ✅ |
| H5 | 客户端低优先（R3 O14-O20 + R1#17） | `client/utils/`、`client/pages/`、`client/uni_modules/`（如涉及） | ✅ |

边界约定：
- H1 **不碰** `backend/app/api/`、`backend/app/schemas/`、`backend/app/core/errors.py`（归 H4）；**不碰** `backend/tests/`（归 H3）。包级 import 显式化仅限 services/db 域内。
- H4 **不碰** `backend/app/services/upload.py`（拆包归 H1）；`api/upload.py` 的 form 约束归 H4。
- H2 只碰 CI 工作流与测试驱动脚本，**不碰** `backend/app/`、`backend/tests/`。
- R1#17「客户端 HTTP 上传统一」从原 H1 划入 H5（客户端域）。

---

## H1 · 后端杂项收口（R1 系列）

### 任务
1. **R1#15 包级 import 显式化**：消除 services/db 域内模块级隐式/裸相对导入（改为显式 `from app.xxx import yyy`），不破坏循环依赖现状（拆包后 import 无环冒烟）。
2. **R1#16 db/models.py 拆包**：按域拆 `app/db/models/` 子包（或模块分组），保持 ORM 唯一权威（Alembic check 零漂移、`schema.sql` 同步）。
3. **R1#14 event_aggregation 开发脚本迁移**：`services/event_aggregation/` 下的 `generate_test_photos.py`、`load_real_photos.py` 等**开发辅助脚本**移入 `scripts/`；`pipeline.py`/`st_dbscan.py`/`run_validation.py` 留原位（运行时代码）。
4. **R1#7 pipeline 内容类型注册表**：内容类型处理器 if/elif 收敛为注册表（text/voice/photo 各处理器注册），行为等价。
5. **R1#9 wechat 依赖反转**：`services/wechat/` 依赖端口（签名/网关抽象）而非具体实现，行为等价。
6. **R1#12 推送通道 PushChannel 端口**：`services/notify.py` 抽象推送通道端口（mock/真实通道实现），行为等价。
7. **R1#13 端云聚合参数契约同步**：端侧（client agg_config）与云侧（pipeline AGG_CONFIG）聚合参数契约对齐文档 + 代码同步。
8. **R1#18 upload.py 拆 protocol/register**：`services/upload.py` 拆协议适配与注册逻辑（protocol/ 与 register 分离），行为等价。

### 文件域（只许改这些）
`backend/app/db/models.py`（拆包后 `models/` 子包）、`backend/app/services/`（upload.py、wechat/、notify.py、pipeline.py、event_aggregation/）、`scripts/`（仅 event_aggregation 开发脚本迁移）、`client/utils/agg_config*`（仅 R1#13 参数契约同步，若有）

### 验收
- 受影响域精准测试全绿（test_upload/test_pipeline/test_notify/test_wechat*/test_agg_reference/test_event_aggregation 等）
- `git grep`：旧 import 形态调用点=0；上传注册/推送/聚合各新实现有真实消费方
- 快速门禁 EXIT=0；契约快照零消失；`python -c` 逐模块 import 无环
- 注意：upload.py 现含 F-Content（photo_content 收口）+ P0（魔数嗅探/幂等）成果；wechat 现含 G2（防重放）成果；notify 现含 B2（幂等键）成果，均不得回退

---

## H2 · CI 增强（R7 系列）

### 任务
1. **client tsc 试点**：CI 增加 client 目录 `tsc --noEmit` 类型检查（复用本地 .cowork-temp/c2_tscheck 方案），失败 annotation。
2. **模型路径 annotation**：CI 失败时把 HF/模型路径信息写进 annotation（匿名 API 可读）。
3. **覆盖率统一 60 + rag 计覆盖**：覆盖率阈值统一 60%，RAG 用例纳入覆盖率统计（先本地摸底再改 CI）。
4. **缓存提速**：pip 缓存、HF 缓存拆 key（按模型/commit 拆，避免缓存失效全量重下）。
5. **docker exec psql**：PG 就绪/迁移校验改 docker exec psql 形态（对齐 #13 教训：多用户密码内联）。
6. **deselect 显式警告**：测试被 deselect 时输出显式警告（防静默漏跑）。
7. **pip-audit 定时**：依赖漏洞扫描定时 job。
8. **coverage.xml 产物**：CI 产出 coverage.xml 并上传 artifact。
9. **fast 模式 rename/dirty 一致性 + secrets 盲区**：fast 模式与全量在 rename/dirty 语义一致；secrets 扫描盲区补齐（排除 client 工具链外的遗漏点）。

### 文件域（只许改这些）
`.github/workflows/ci.yml`、`scripts/test_agent.py`、`scripts/warm_hf_models.py`、requirements 相关（pip 缓存注解）

### 验收
- CI 静态层（--skip-tests 全量）EXIT=0；本地跑 test_agent.py 快速门禁 EXIT=0
- 改动不碰 backend/app/ 与 backend/tests/（CI 编排层仅引用不改被测代码）
- 快速门禁 EXIT=0；契约快照零消失
- 注意：ci.yml 现含 P1C/P1B 已落地的 rag 分组/隔离步骤，不得回退

---

## H3 · 测试杂项（R8 剩余）

### 任务
1. **参数化**：重复断言/多分支用例改 pytest.mark.parametrize（sensitive_words/ner/retry/asr 等）。
2. **分层 marker**：单元/集成/慢测试分层 marker 补齐（pytest.ini 或 pyproject 配置）。
3. **0% 脚本覆盖**：覆盖率 0% 的脚本（CLI/辅助脚本）补最小单测或显式登记豁免（含理由）。
4. **固定 id 清理**：测试硬编码固定 id（跨运行/并发撞车隐患）改随机/参数化。
5. **autouse 显式化**：conftest autouse fixture 注释/命名显式化（用途 + 依赖说明）。
6. **批次文件按域拆分**：大测试文件按域拆分（对齐 D1/D2 已落地的 db_user 迁移/conftest 公共版范式）。

### 文件域（只许改这些）
`backend/tests/`、pytest 配置（pytest.ini/pyproject 中测试配置段）、`backend/tests/conftest.py`

### 验收
- 全量 pytest 与集成前基线持平或更高（不得因拆分丢用例）；快速门禁 EXIT=0
- 新增用例数 > 删除；契约快照零消失
- 注意：conftest 现含 D1（db_user 公共版）+ G1（限流默认关）成果，不得回退

---

## H4 · API 契约收口（R4 剩余）

### 任务
1. **R4#6 分页三套风格统一**：分页响应三套风格统一为一套（limit/offset 或 cursor 定标）。
2. **R4#7 GET 参数校验统一**：GET 查询参数校验统一（共享 Query 校验器/依赖）。
3. **R4#8 响应契约类型化**：响应模型类型化补齐（response_model 全覆盖）。
4. **R4#9 arbitrate 路由归属**：arbitrate 裁决路由归属明确（/classify 或 /corrections 域定标）。
5. **R4#10 访谈鉴权文档对齐**：interview 鉴权实现与 OpenAPI 文档对齐。
6. **R4#11 AUTH_099 拆分**：AUTH_099 通用错误拆分为具体错误码（登记 errors.py）。
7. **R4#12 upload form 约束**：`api/upload.py` 表单字段约束补齐。
8. **R4#14 成功响应 request_id 回填**：成功响应头回填 request_id（对齐 API-008）。
9. **R4#15 echo dismiss 幂等**：echo dismiss 幂等键（重复 dismiss 不重复计数）。

### 文件域（只许改这些）
`backend/app/api/`、`backend/app/schemas/`、`backend/app/core/errors.py`

### 验收
- 受影响域精准测试全绿（test_events/test_interview/test_echo/test_upload/test_correction/test_api_smoke 等）
- 契约快照：openapi 路径**新增/改签名允许**、旧路径与字段零消失；errors.py 新错误码登记、旧码不消失
- `git grep`：arbitrate/echo dismiss 新实现有真实消费方；分页统一后无旧风格残留
- 快速门禁 EXIT=0
- 注意：api/ 现含 F-Auth（auth 协议层）+ E1（ERR_* 常量）+ G1（logout）成果，不得回退

---

## H5 · 客户端低优先（R3 O14-O20 + R1#17）

### 任务
1. **O14 uploader id 持久化**：上传任务 id 持久化（应用重启后可续）。
2. **O15 contract.ts**：客户端契约类型文件（对齐 OpenAPI，`client/utils/contract.ts` 新建）。
3. **O16 upload_pipeline**：上传管线收敛（uploader.ts 复用统一封装）。
4. **O17 分页字段**：列表分页字段规范化（与后端统一）。
5. **O18 retry 死参清理**：retry 死参数清理（无调用方的参数删除）。
6. **O19 日志脱敏**：客户端日志脱敏（token/手机号/敏感字段不落日志）。
7. **O20 dev 页常驻**：dev 调试页常驻开关（config 控制，非 dev 构建关闭）。
8. **R1#17 客户端 HTTP 上传统一**：uploadFile 类路径统一封装（复用 api.ts 底层 rawRequest 模式）。

### 文件域（只许改这些）
`client/utils/`、`client/pages/`、`client/uni_modules/`（如涉及）

### 验收
- tsc 类型自检 EXIT=0（基线 + 每 commit 后）；快速门禁 EXIT=0（client 目录跳过属正常）
- `git grep`：旧上传实现/死参调用点=0；contract.ts/upload_pipeline 有真实消费方；日志脱敏覆盖敏感字段
- 行为等价：上传/分页/重试语义不变（纯客户端，无 pytest，靠 tsc + grep 契约 + 真机/人工冒烟）
- 注意：client 现含 C1/C2（rawRequest/parseIsoMs/DEVICE_ID 单源）+ F-ClientA/B（timeline/queue_store/pause_controller）+ G1（auth.ts single-flight）成果，不得回退

---

## 提交纪律（所有 H Agent）

1. `git commit -- <paths>` 路径限定，禁止 `git add -A`；提交前 `git diff --cached --name-only` 核对。
2. 每 commit 过快速门禁（pre-commit hook 自动）。
3. 不 push；分支建议 `techdebt/h1`…`techdebt/h5`；报告文件留集成 Agent 统一提交。
4. 完成后显式呼叫集成 Agent（merge + 精准门禁 + CI 验证 + 契约快照 diff）。
