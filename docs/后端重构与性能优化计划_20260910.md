# 后端重构与性能优化计划（2026-09-10 凌晨审计）

> 方法：主线程 grep/静态定点审计（三路子 agent 因 429 配额阵亡，安全维深扫待补）。
> 纪律：本文件只立项不执行；每波动手前单独拍板，破坏性删除列清单二次确认。

## 一、审计发现分级台账

### 🔴 P0（磁盘挤占 + 并发短板）

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P0-1 | **checkpoints/ 6.4GB** SetFit 训练中间产物，运行时零引用（train_setfit.py 输出=model/，重训可再生） | `du` 实测；代码 grep 无消费 | 确认后删目录（gitignore L81 已覆盖，不碍仓库） |
| P0-2 | **models/bge-reranker-v2-m3 疑似死重**（settings.reranker_model 默认=base，v2-m3 无运行时引用点） | rerank.py L4 只引 base；需 grep 全库+问峰宝设计意图 | 确认无切换计划→删（7.4G→省 ~2.3G）；若留待 rerank 升级→登记保留理由 |
| P0-3 | **孤儿上传分片永不清理**：data/storage/uploads/ 36 个 *.part 全部 >7 天；cleanup_job.py L15 自己登记「TODO(P0-6) 孤儿对象扫描未做」 | find 实测 + 代码 TODO 自供 | **写代码**：uploads 分片 reaper（upload_tasks 表无活跃记录且 mtime>N 天→删目录+清行），挂进 cleanup_job；带测试 |
| P0-4 | **DB 池 5+10 撑不住并发**：压测报告已定位 DB 池瓶颈；pool_size=5/max_overflow=10（session.py L12-13）且无 pool_pre_ping | 100 并发×多 worker 进程 = 池秒穿 | 调参（pool 10+20、pool_pre_ping、pool_recycle=1800）+ F1 复测窗口验证；worker 与 api 分池 |

### 🟡 P1（内存/性能次优，负载到量才爆）

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| P1-1 | 无 limit 全量拉：fetchall/.all() 全库 48 处待逐条审（timeline/export/聚合/清理 job 高危） | grep 计数 | 专项波：逐条判定「有界（成员≤N/分区截断）」vs「随数据量线性膨胀」；后者加 cap 或游标化 |
| P1-2 | lru_cache 核查结果=**全有界或单例模式**（config get_settings 无参=单例、sensitive_words 带参有界） | 逐处看过 | 无需动，结案 |
| P1-3 | `_token_cache`（wechat/service.py L77）固定键+TTL 刷新 | 读实现 | 健康，结案 |
| P1-4 | 同步 httpx 调用在 RQ worker 管线里=正常（worker 非 event loop）；**api 请求路径内**的同步外呼待点名审（amap.py L82 / content_safety L207 若被端点直调=卡 loop） | grep 六处调用点 | 专项：确认每个调用点归属（端点线程池 vs worker）；端点直调改 async 或下推 |

### 🟢 P2（可维护性，攒着顺手做）

| # | 发现 | 处置 |
|---|---|---|
| P2-1 | 巨型文件 top3：pipeline.py 708 / event_aggregation/pipeline.py 615 / aggregate.py 516 | 有回归测试护航再拆（照 h1 upload/ 子包先例）；**不与功能波混做** |
| P2-2 | 归属校验/软删过滤模式跨 8+ 文件重复（`user_id == user.id` + `deleted_at.is_(None)` 手抄） | 收敛 `get_owned_content(db, cid, uid)` 类 helper；新增端点强制走 helper |
| P2-3 | TODO 仅 4 处且均为正当登记（短信 T1/厂商推送 T1/孤儿对象=本计划 P0-3 吸收） | 无死代码潮，结案 |
| P2-4 | 硬编码密钥/路径穿越/生产 mock 后门：定点核查**未发现** | 安全子 agent 深扫补报后终判 |

## 二、执行波次（依赖排序）

- **波 A 磁盘清理（半小时，删前报清单）**：P0-1 删 checkpoints/；P0-2 v2-m3 定性后处置。
- **波 B 并发硬伤（半天）**：P0-4 池参数 + P0-3 reaper job 代码+测试。
- **波 C 无界查询专项（1 天）**：P1-1 逐条审 48 处 + P1-4 端点阻塞点名，出补丁。
- **波 D 结构重构（穿插，1-2 天）**：P2-1/2（测试护航，独立波）。
- 每波完成标准：受影响测试绿 + 全量 pytest 不红 + 编译/冒烟（涉 api 出参时）。

## 三、分支清理方案（git 账已核）

**主仓状态**：develop=adec639 已推远程；工作区脏文件均为他窗在途（AGENTS/register.py/manifest 等 6 件+untracked 若干）**不碰**。worktree 仅剩 missing-pages 活跃。

| 组 | 分支 | 定性（git cherry + merge-base 实证） | 建议 |
|---|---|---|---|
| 主力 | feature/missing-pages-impl @a3da8cb | 今晚全部成果（B10/凭证/后端五缺口/A8/C5），未并 develop | **验收波后 merge develop**（下面波 E） |
| 祖先 | main @3869111 | merge-base(main,develop)=main 本体=**已全含于 develop** | 保留（发布快照惯例）或按需 fast-forward |
| 僵尸 | techdebt/{f-events,g1,g2,h1,h2,h3,h4,h5} ×8 | cherry +0=内容全合入 | `git branch -d` 安全删 |
| 已吸收 | fix/4b、fix4b-v2、zzz-test-4b | D-18/19 声明+manifest 两刀今晚已等效入库（3769013/a3da8cb 谱系） | 复核 diff 后删；v2 纯重复先删 |
| 历史波 | wrap1-agent{A1,A2,A3,B1,B2,B3,C1,C2,C3,D1}、feature/m1-* ×4、feat/wtest4 | 经 cea5025 跨天 merge/快照换代，patch-id 失真显 +；内容归宿=develop 现状+main 快照 | **打 tag 归档后删**（`archive/<名>`，远程同留） |

## 波 E · missing-pages → develop 合并验收（新增，P4 前置）

missing-pages 分支已含 30+ 枚未并 develop 的提交（A/B 批、B10、五缺口、凭证），develop 侧有他窗在途未提交文件——合并需他窗先收口。建议波 A 前完成，避免双史拉长。
