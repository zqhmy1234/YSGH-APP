# 微信入口攻坚（F6）· 执行任务卡——feat/wechat-entry

> 状态：🔴 建卡待执行（2026-09-01）
> 分支：`feat/wechat-entry`（worktree `.wt/wechat-entry`，基于 develop @ 2059f40）
> 关联故事：US-31/32/33（微信收+找，🟡半通 3 条）
> 关联文档：`docs/parallel-dev/12_wave4_agentL_M3微信.md`（历史底稿，本卡是其后凭证就位的执行版）

## 1. 凭证现状（2026-09-01 实测 Infisical dev env）

| 密钥 | 取值状态 | 解锁能力 | 阻塞判断 |
|---|---|---|---|
| WECHAT_APPID | ✅ 已取到（wxde074602b0bb7bcc） | 微信 code2session 登录 | **登录侧解锁** |
| WECHAT_SECRET | ✅ 已取到 | 同上 | 同上 |
| WECHAT_CORP_ID | ✅ 已取到（wwcbd00ba9f80932fd） | 企微回调身份 | 回调侧部分解锁 |
| WECOM/WECHAT_TOKEN | ❌ Infisical 无此 key | 企微 URL 验证+收包验签 | **回调侧仍阻塞** |
| WECOM_ENCODING_AES_KEY | ❌ Infisical 无此 key | 企微消息加解密 | **回调侧仍阻塞** |

> config.py 已定义 `wechat_token` / `wechat_encoding_aes_key` 两字段（读取 env `WECHAT_TOKEN` / `WECHAT_ENCODING_AES_KEY`，默认 `""`）。缺值时 `api/wechat.py:35` 判定未配置 → 503 拒绝回调（安全默认，正确行为）。
> **结论**：登录侧 3 件已够实网验证；回调侧 2 件待企微后台获取（决策台账 §6 口径一致：前 3 就绪，TOKEN/AES 待获取）。

## 2. 代码现状核实（develop @ 2059f40 实查）

| 模块 | 状态 | 说明 |
|---|---|---|
| `auth.py:39-42` + `providers.py:287-360` | ✅ 已实现 | code2session 真实调用（httpx GET）、三态判定（真实/生产 501 拒绝/mock）、错误语义 AUTH_001/012/013 |
| `api/wechat.py`（回调网关） | ✅ 已实现 | URL 验证+收包处理，未配置 503 拒绝（ERR_WECHAT_099） |
| `services/wechat/gateway.py` | ✅ 已实现 | 验签→解密→解析 XML→消息 dict（text/image/voice 入库） |
| `services/wechat/service.py` | ✅ 已实现 | msg_id 幂等（UNIQUE）+ 媒体下载 COS + content_safety 敏感排除 |
| `services/external/content_safety.py` | ✅ 已实现 | 三 provider 可切换（tencent_ci 当前默认 / aliyun 上架前 / off） |
| 客户端页面 | ➖ 无页面需求 | 微信域交付面在企微回调/微信侧，APP 端无页面是正常设计（见覆盖矩阵 §2.1 说明） |

## 3. 攻坚任务分解（凭证就位后的可执行项）

### 批次 A · 登录侧实网验证（3 件已够，优先）
- [ ] A1：`infisical run --env=dev -- pytest backend/tests/test_auth.py -k wechat`（code2session 单测，mock 微信响应）
- [ ] A2：真实 code 冒烟——需微信小程序/公众号开发工具产出一个真实 js_code，调 `POST /api/v1/auth/wechat` 验证 unionid 三端一致（AUTH-001）
- [ ] A3：确认 dev 环境 `_wechat_configured()` 判真（appid+secret 注入后），mock 路径退出
- [ ] A4：回归 test_wechat.py 11 项 + 全量 pytest，确保不破坏

### 批次 B · 企微回调侧（2 件待 TOKEN/AES，代码先行）
- [ ] B1：TOKEN/AES 到位后，`WECHAT_TOKEN` + `WECHAT_ENCODING_AES_KEY` 存 Infisical（dev+prod）
- [ ] B2：回调 URL 验证冒烟（GET verify_url：验签+解密 echostr 原样返回）
- [ ] B3：收包冒烟（POST text/image/voice → 幂等入库 + 敏感排除 → 云端镜像）
- [ ] B4：微信"找"（find_memories）实网链路验证（US-32）
- [ ] B5：可信 IP 追加：当前 61.171.241.17（开发机出口，CGNAT 会漂移）——换网后 `curl ip.sb` 向负责人追加新 IP

### 批次 C · 合规（US-44，与 #15 隐私政策联动）
- [ ] C1：content_safety provider 切换 aliyun 的开关 + AccessKey 就位（上架前）
- [ ] C2：隐私政策定稿审阅签字（#15 产出后）

## 4. DoD（完成定义）

1. 登录侧：code2session 真实链路真机验证通过（A 级证据：真实 js_code → unionid 入库 → token 签发）
2. 回调侧：TOKEN/AES 就位后，回调 URL 验证 + 收包入库 E2E 通过（B 级起步，真网验收升 A）
3. 全量测试通过（后端 pytest 全绿，回归 test_auth + test_wechat 不破坏）
4. 台账/凭证登记：Infisical 5 件齐 + 决策台账 §6 状态列同步 + `_resource_endpoint_matrix.md` 回刷
5. 数字口径：US-31/32/33 🟡→✅ 需用户授权同步五处（AGENTS + 08 + progress + session-handoff + feature_list）

## 5. 依赖与阻塞

- 登录侧实网验证依赖：一个真实 js_code（微信开发工具可产）+ 后端可达微信 API（出网 OK）
- 回调侧依赖：TOKEN/AES 两件（企微后台获取）+ 公网可回调 URL（M1 服务器，决策台账 §6）
- 合规侧依赖：阿里云 AccessKey（上架前）
