# 待办处理排期（2026-09-23）· 按难易排序 × 服务器不可用约束

> 触发：用户令「对新发现并登记剩余的待办项，按难易排序处理」，并明确**服务器暂时不买**。
> 来源：`docs/决策台账.md` §5/§7 + `docs/审计_未关闭缺陷_20260923.md` + `docs/远期待办总账.md` + `parallel-dev-收尾/19_wave3_真机补验跟踪表.md` §4 + `parallel-dev/_B5a_B5d修正后待办.md`。
> 排序四档：**S 极易**（≤1h，文本/开关/注释）· **A 易**（≤半天，单点代码+本地验证）· **B 中**（1–2 天，单模块接线／需本地后端或真机）· **C 难**（≥3 天，新端点/新能力/需重校）· **D 阻塞**（等外部：服务器/凭证/拍板）。

## 0. 关键判断（先看这条）

**服务器不来，最有效的一块是「A 档后端契约缺口（A1–A11）」**：

- 它是**纯后端开发 + 本地 Docker 可验**，零公网依赖；
- 它是**客户端一大批假交互的根因**——`B8 演示数据开关清账`、`B12 十七处诚实 toast 三分类`、`A9 用量字节`、`A11 回响真图`都以它为前置；
- 现在不动它，等服务器来了也得回头做，**不推进＝纯等待**。

次优是 **S 档清账**（今天可做完，且多为"误导性宣称"类隐患，收益是**降低后续会话被错前提带偏的概率**）。

**唯一被服务器硬卡的**只有 §5 那 7 项（云打包/上机/分发/上架回收），**其余全部可以推进**。

---

## 1. S 档 · 极易（今天可清，零外部依赖）

| # | 项 | 出处 | 动作 | 验证 |
|---|---|---|---|---|
| S1 | **声明漂移三则** | §7.7 | ① `client/pages/shell/shell.uvue:7` 陈旧注释（称老四页仍在 pages.json，实际已删）② `client/utils/search_api.uts:183` 陈旧 mock 叙述 ③ `feature_list.json:64` 同条 evidence 自相矛盾 | 改文本；`grep` 复核 |
| S2 | **Agent 页"不可关"最小修复** | §7.1 | 补真实存在的 `USE_MOCK_CHAT` 常量（默认 true）+ 页面顶部加"内测演示数据"一行标注 → 把"无条件硬编码"变成"可关且不误导"（**完整三选一仍需拍板**） | HBuilderX 编译过 + 真机目视 |
| S3 | **mock 开关清账检查脚本** | B8 | 把「发版前 `grep USE_MOCK` 全部=false」写成 `scripts/check_mock_switches.py`（可执行门） | 脚本自跑出当前清单（预期多处 true） |
| S4 | **openapi 双向对拍** | 审计 §6.2 未覆盖项 | 本地起后端 → `curl /openapi.json` → 与 `docs/openapi.json` 双向 diff（**后端有路由但契约缺 / 契约有但后端无**） | 产出差异清单；顺带闭 C1 |
| S5 | **24 张表使用率盘点** | 审计 §5.5 | 静态统计：每个 ORM 模型 → 是否有业务读写引用 | 产出"建了未用"表清单 |
| S6 | **部署就绪包确认** | §5.1 | 文件已产出（`docs/部署就绪包_20260921.md`），**只需你过目点头**即销项 | 你确认 → 台账 §5.1 打 ✅ |

## 2. A 档 · 易（≤半天，单点代码 + 本地验证）

| # | 项 | 出处 | 动作 | 前置 |
|---|---|---|---|---|
| A1 | **蜂窝/省流上传接线压缩步骤**（原写"解除 hold"——**2026-09-23 复核后改写**：后端契约与客户端 `uni.compressImage`（app-android 受支持，HBuilderX dts 已取证）均具备，缺的是接线） | §7.5 | ① `uploader.uts` 的 `resolveMode`：`thumbnail` 不再直接 hold，改为返回 `'thumbnail'`；② 上传主流程在 init 前调 `uni.compressImage({src, quality})` 取压缩件；③ 以 `upload_mode='thumbnail_meta'` init+上传（后端语义＝上传物即缩略图，只落 thumbnail_key 占位 + `extra.original_pending=true`）；④ 记录 `content_id`，WiFi 后凭 `meta.content_id` 补传原件（复用 `continuePendingUploads`）；⑤ 压缩失败则退回 hold（**不得用原图冒充缩略图**） | 编译门 + 真机（本轮环境 307MB 内存 + Docker 停 → 无法验证，故未动管线） |
| A6 | **O-1 stage2 条件复现** | O-1 | 按 `docs/P2诊断_O1O2_20260829.md` 的复现单跑本地后端，定位 uvicorn exit 1 | 本地后端（**A 档唯一仍需实跑项**） |

## 3. B 档 · 中（1–2 天，单模块接线 / 需本地后端或真机）

| # | 项 | 出处 | 动作 | 备注 |
|---|---|---|---|---|
| B1 | **图搜接线（客户端）** | §7.3 | `TabSearch.uvue` 加相机/相册入口 → 调 `search_api.uts:252 searchByImage()`（后端端点已存在） | 接线易；**质量无基线**，须配 B2 |
| B2 | **图搜评测补建** | §7.3 | 新建"图片查询"评测脚本（现有 `eval_image_search.py` 实为**文字**搜图）；用现有截图语料做 query 图 | 需评测集 |
| B3 | **Content.duration 前端显示**（后端半 ✅ 已落库出参 + `test_ba1_fields.py` 覆盖；**残项＝前端语音卡空闲态槽位未接**） | A6 | 前端接线（timeline 需下发 duration） | 后端半已落 |
| B4 | **A8 Content.remark 字段** | A8 | 同上；前端 remark UI 已留好 | 含迁移 |
| B5 | **A10 MessageOut.content_id** | A10 | 后端补字段；前端 `openMessage` 路由骨架已留好，加一行 navigateTo | 小 |
| B6 | **A2 收藏 favorite 端点** | A2 | 后端新端点（全后端 grep favorite 零命中） | 中 |
| B7 | **A3 内容删除端点（软删 30 天）** | A3 | 后端端点 + `trash.uvue` 三处接线（列表/恢复/清空 UI 已落地） | 中 |
| B8 | **P-1…P-4 性能四项** | P 档 | timeline 游标分页 / 图片 LRU 磁盘缓存 / 虚拟滚动 / 启动并行化 | 本地可测 |
| B9 | **B9 波形呼吸动画** | B9 | 解决多实例元素定位（函数 ref / 宿主注入唯一 id） | 需真机 |
| B10 | **字节用量聚合（A9 剩余半项）** | A9 | Content 加 `size` 字段 + 用量卡改真实字节 | 导出半项已 ✅ 待真机复验 |
| B11 | **纠错裁决链路接线**（`submitArbitrate` / `pollArbitrate` 零调用） | §7.8 | `text_recorder.uts:206/228` 导出零调用 → 接到裁决交互（`record.uvue`）；**与观察项 O-2 / 缺陷 D-22 同域，动手前先读 `docs/P2诊断_O1O2_20260829.md`**（既有诊断聚焦"回声覆盖+层①锁死"，本项补上"链路压根没接"这一角度） | 客户端 + 真机（需编译门） |
| B12 | **删除 / 字段编辑入队接线**（`enqueueDeleteOp` / `enqueueFieldOp` 零调用） | §7.8 | `sync_client.uts:111/130` 导出零调用 → 依赖 A3 软删端点与 A8 remark 端点，端点就绪后一并接线 | 后端 A3/A8 + 客户端 |
| B13 | **Sentry 埋点接线**（`addBreadcrumb` / `captureMessage` 零调用） | §7.8 | `sentry.uts:101/166` 导出零调用 ⇒ 线上问题**无迹可循**；至少覆盖登录失败 / 上传失败 / 搜索失败三条关键路径 | 客户端（需编译门） |
| B14 | 上传横幅计数接线（`pendingPhotoCount` 零调用）· 语音直传阈值生效（`MAX_DIRECT_UPLOAD_BYTES` 零引用） | §7.8 | 小项，随「上传管线」批次（含 A1 压缩接线）一起做，避免多次触碰同一文件 | 客户端（需编译门） |

## 4. C 档 · 难（≥3 天；新子系统 / 需重校 / 依赖真值数据）

| # | 项 | 出处 | 为什么难 |
|---|---|---|---|
| C1 | **RAG 门禁 FAIL 重校**（负样本误召回 57% vs 阈值 5%） | §7.2 | 需重做负样本集 + 阈值/混合策略调优 + 五层 placeholder 评测补齐；**依赖真值数据（团队）** |
| C2 | **产品 Agent 后端实现**（chat/agent 域） | §7.1 | 后端零实现，需从契约到端点全做；**B1「问 AI 带上下文」也挂在此** |
| C3 | **推送真实通道** | §7.4 | 需厂商通道凭证（团队）+ 服务端通道 + 客户端接线 |
| C4 | **A4 评论/感想体系** | A4 | 需产品定义（挂载对象/独立表），且 messages 回响卡文案已承诺该能力 |
| C5 | **A5 时间胶囊端点** | A5 | 封存/查询/到期开启 + 到期推送；前端 F7b 面板可先行 |
| C6 | **A7 照片 AI 描述 + 照片分类链路** | A7 | 后端描述端点 + 分类入库；前端网格已按画布落地 |
| C7 | **情绪校准（S2 开心漏报 0.496）** | §7.6 | 需真值数据（C 批）+ 文案库（产品部） |
| C8 | **B6 定位服务** | B6 | 需产品定义 + 隐私合规 + 逆地理编码方案 |

## 5. D 档 · 阻塞（等外部，解冻条件已写明）

| # | 项 | 卡在什么 | 解冻条件 |
|---|---|---|---|
| D1 | 云打包 + aapt/dex 尸检 + 真机冒烟 | 出包前置门第 2 条（`PROD_BASE_URL` 占位） | **服务器地址** |
| D2 | 部署上机（RUNBOOK §1） | 同上 | **服务器 + 访问信息**（`deploy/SERVER_INTAKE.md`） |
| D3 | E1 下载页部署 + APK 投放 | 依赖 D1 | 出包产物 |
| D4 | D-18/D-19 真机复验 | 需**自定义基座**（`cli launch --playground custom`）而基座来自云打包 | 同 D1 |
| D5 | C5 测试音回退拆除 | 缺 `GET /api/v1/media/audio/{content_id}` 真实音频 | 后端音频端点（可先做） |
| D6 | B2 分享真实能力 / B5 绑定管理 | 开放平台凭证 + 企微/微信实网 | 团队 |
| D7 | C3 上架前接内容安全 | 阿里云内容安全开通 | 上架批次 |
| D8 | 上架前回收清单 6 项 | 上架批次 | 上架前 |
| D9 | ICP 备案 | 运营 | 20 工作日 |

---

## 6. 建议执行顺序（三批）

| 批 | 内容 | 预计 | 交付判据 |
|---|---|---|---|
| **批 1（今天）** | S1–S6（含一次 `USE_MOCK` 清点与 openapi 双向对拍） | 半天 | 脚本/清单产出 + 漂移修正提交 |
| **批 2（本周）** | A1 + A6 + 小字段前端接线（后端半已落：BA1 迁移覆盖 duration/remark/content_id） | 1–2 天 | 本地后端跑通 + 真机可验项留证 |
| **批 3（服务器到位前）** | B6–B7（收藏/删除端点）+ B1/B2（图搜接线+评测）+ B8（性能） | 数天 | 后端端点 green + 12 处假交互随之可销 |

## Agent 接线批（2026-09-23 起 · 交付包已入库 `agent/`）

出处与改造依据：`agent/UPSTREAM.md`（出处指纹 + 读码结论 + **去 Coze 改造映射表 11 项** + 7 条已知缺陷）。
用户拍板（09-23）：形态＝**独立进程 + 我们后端反代（JWT→可信 user_id 注入）**且**全量去 Coze 运行时**；数据落**我们 Postgres**；**先接 App 客户端 AI 页**；模型改**阿里云百炼**。

| # | 步 | 动作 | 验收 |
|---|---|---|---|
| AG1 | 入库 | 交付包**全量**入 `agent/`（50 文件 / 778.8 KB）+ `UPSTREAM.md` | ✅ 已入库 |
| AG2 | 去 Coze 核心三件 | ① env 加载改 `.env`（去 workload identity）② DB 层改自有 `Base` + alembic 建三表（落我们 Postgres）③ LLM 改百炼 OpenAI 兼容端点 | 服务能起 + 模型可应答 |
| AG3 | 数据层重写 | 约 40 处 Supabase PostgREST → SQLAlchemy repository（`memories`/`knowledge_collections`/`memory_categories`） | 记忆读写测试绿 |
| AG4 | 能力适配层 | ASR / 网页抓取 / S3 预签名 / 日志去 Coze SDK（对接我们既有百炼 ASR、COS 预签名；抓取需新增） | 全仓 `grep coze` 零 import 残留 |
| AG5 | 后端契约与反代 | 我们后端实现 `POST /api/v1/chat/messages` + `GET /api/v1/chat/replies/:id`（JWT 鉴权 → 可信 user_id）+ 反代到 agent 进程 + 消息落库 | 后端契约测试绿 |
| AG6 | 客户端接线 | `TabAi` 从 mock 换真（`USE_MOCK_CHAT=false` 路径）+ 顺带图搜接线（`searchByImage`） | 编译门 + 真机一条真对话 |
| AG7 | **撤销 lint 豁免（硬待办）** | 删除 `scripts/review_agent.py:_skip_path` 里的 `"agent"` 豁免并全量 lint 收口（354 违规：239 可自动修 + 115 手动，含 `S310`/`S110`/`DTZ` 等真问题） | `ruff check agent` 全绿且豁免已删 |
| AG8 | **`alembic check` 本地常年红（既有漂移，非 AG 引入）** | AG3 施工时实测发现 | 本地库有 **12+ 张 ORM 已不声明的遗留表**（`profile_l2_evidence`/`voice_segments`/`guardrail_logs`/`question_templates`/`event_tags`/`tags`/`api_cost_stats`/`finetune_jobs`/`audit_log`/`question_history`/`ai_request_logs`/`content_tags`…）+ `users.*`/`wechat_messages.*` 类型漂移（TEXT↔String、INTEGER↔BigInteger）⇒ 每次 `alembic check` 必红，**真漂移会被噪音淹没**（本项目最怕的失败形态）。**注意**：AG3 的三表经实测**零漂移**（只余两条 SERIAL 序列 INFO） | 二选一：① 补一条迁移显式 drop 遗留表（**须先确认确无引用**）② 在 ORM 补声明后 `--autogenerate` 对齐。前置：确认这些表是否 `schema.sql` 时代遗产 |

**需要你拍板才能启动的 3 件**（不拍板则按保守方案执行）：
1. **§5.8 / §7.1 产品 Agent 页**：接线（C2，重）／**隐藏入口**（一行）／保留但显式标注（S2 已先做）
2. **§5.2 自定义分类管理页**：入 MVP 还是归二期
3. **S2 的"演示数据"标注文案**：是否接受"内测演示数据"这一措辞
