# 决策台账（口径·说法·决定 统一登记簿）

> **用途**：消灭"说法散各文档、决策散各会话"的债务。基线 32 项决策在交付文档 `忆述光华_开发决策清单.md`（本簿不重复展开，只标 📖）；本簿登记 **2026-08-19 开工以来的补充决策、口径裁定、待拍板项**。
> **规则**：新决策先登记再执行；已拍板勿重开（AGENTS.md）；「待拍板」销项须用户明确拍板并回填结果。
> **状态**：✅ 已拍板 · ⏸ 搁置 · 🔴 待拍板 · 📖 基线引用（交付文档既有）
> **更新时机**：每次会话 End of Session 有决策/口径变化即登记。

## 0. 术语消歧（统一说法——新会话先读这张表）

| 术语 | 唯一定义 | 易混点 |
|---|---|---|
| **开发 Wave 1–4** | 并行开发期（2026-08-25~26），Agent A–L 特性开发波次；任务卡原在 `docs/parallel-dev/`（**2026-09-23 清理仅留 `_B5a_B5d修正后待办.md`**，见 §4.10） | 与"收尾 Wave"不是一回事 |
| **收尾 Wave 1–4** | 收口期（2026-08-27~28）：Wave1=A1/A2/A3/B1-B3/C1-C3/D1 分支 · Wave2=INT 集成门禁 · Wave3=真机 7 清单补验 · Wave4=归档（4a/4b）；任务卡原在 `docs/parallel-dev-收尾/`（**2026-09-23 清理仅留 `19_wave3_真机补验跟踪表.md`**） | AGENTS/08 文档里的"Wave 1–4 收口"指这套 |
| **4b 修复批次（批次1/2/3）** | 下一步缺陷修复排期：批次1=D-18/D-19 · 批次2=D-16/D-07/D-08 · 批次3=S2 校准/D-06 | 与技术债期「重构批次 C–H」（08-26~27，已完成）不同族 |
| **F1–F9** | MVP 功能编号（AGENTS/feature_list.json） | **勿与「远期待办 F1–F7」（00 总纲 §7）混用**，后者引用时须带前缀"远期待办" |
| **A/B/C 级** | 证据级：A=nova 11 实测三要素 / B=模拟器·日志级 / C=代码·mock 级 | 与故事状态 ✅/🟡/❌ 是两维，**不可合并统计**（§4.5） |
| **用户故事终值** | ✅46 / 🟡7 / ❌0，A 级 32 条（收尾 Wave 全收口后） | 唯一现行口径 = AGENTS.md「当前状态」；02 line119"✅42"是笔误；08 §0/附录与 progress/handoff 4b 段的 ✅41/A27 是 4b 当时值（已加勘误标注） |
| **缺陷单 D-nn** | 唯一来源 `docs/parallel-dev-收尾/19_wave3_真机补验跟踪表.md` §4，共 19 单（D-01~D-16、D-18、D-19、D-21） | **D-17 未启用**；**D-20**（导出落盘）是 4a/4b 补验修复单、不在 §4 表内，见 commit f726942 |
| **观察项 O-n** | 未定性为缺陷：O-1 uvicorn 嵌入时刻瞬时原生崩溃 / O-2 纠错裁决恒"混合" | 非修复单；O-1 修复批次优先排查、O-2 并入批次3 排查 |
| **门禁（三族）** | ①提交审核门禁：快速（hook/review_agent）+ 全量（--full）②性能门禁 18 项（07 §7，现 10/2/6）③收尾 Wave2 集成门禁 G1–G5 | 引用"门禁"必须带族名 |
| **真值 A–G 批** | 产品部七批真值数据（08 §1.4：文字/录音/照片/负样本/纠错/画像/护栏） | 与远期待办 F1–F7 同字母族，无关，勿混 |
| **Agent（两种含义·务必指明）** | ① **开发流程 Agent**：并行开发期的 A–L 特性开发窗（任务卡目录已于 2026-09-23 清理，见 §4.10）② **产品 Agent 系统**：App 内的智能体/对话能力，入口 `client/components/TabAi/`（**当前全 mock**，见 §7.1） | 说"Agent 未完成/未接入"时必须指明哪一个；二者已被混谈多次（2026-09-23 审计 §1） |
| **"未关闭缺陷" 与 "待拍板"** | 缺陷＝**已实证做不到/不达标**（有代码或实测证据），登记 §7；待拍板＝**尚未定方案**，登记 §5 | 同一件事可从 §5 升级为 §7（先例：AI 对话 mock，§5.8 → §7.1） |
| **三座大山** | 外部凭证 / 真实数据 / 合规三申请（团队阻塞项，非代码） | — |
| **铁律** | 无 nova 11 实测（型号+时间 / 截图日志 / 判定三要素）不得宣称真机打通；缺前置只能写"待补" | 所有 A 级宣称的准入闸 |

## 1. 范围与节奏

| # | 日期 | 决策 | 状态 | 出处 |
|---|---|---|---|---|
| 1.1 | 08-26 | 桌面端（F9/B5e/Zvec/Windows 相关）移出 MVP 归二期；并行开发只覆盖 Android+云端+微信 | ✅ | AGENTS 仓库结构 / parallel-dev 总纲 |
| 1.2 | 08-26 | O6 双离线队列合并随 F9 归二期（共享 queue_store.ts 属 F9 域，缺地基不宜半合并） | ✅ | progress·CDE 集成条目 |
| 1.3 | 08-27 | 收尾 17 项范围与四波计划（16 号拆分为 21/22 = 4a 代码侧 / 4b 真机填充） | ✅ 已全收口 | 00 总纲收尾 §1 / 16 / 21 / 22 |
| 1.4 | 08-28 | **4b 修复批次排期**：批次1=D-18/D-19 原生能力对（重打包复验含会话 B FGS 通知项）；批次2=D-16/D-07/D-08 语音链；批次3=S2 情绪校准 + D-06 机型适配 | ✅（当前下一步） | 08 §1.4 / tracker19 §4 / AGENTS |
| 1.5 | 08-28 | 待补项不堵修复批次：02 来电场景（P6 第二设备）/ 06①②中文 IME 人工复验 / 04 L3 数据 | ✅ | tracker19 §2 |
| 1.6 | 08-29 | **散单未并批**：D-05（横幅 emits/drain 双登记）、D-10（auth.ts 守卫）、D-14（上传游标静默丢）、D-21（两页滚动失效）均客户端向，建议随批次2 排入；D-12 观测性改进、D-09 遗留（单腿健康检查，已并 D-16 处置） | ✅ 用户 08-29 拍板：**并批次2**（执行排期见 docs/4b修复批次执行计划_20260829.md R1） | 本次整饬发现（批次1/2/3 原文案未覆盖这 4 单） |
| 1.7 | 08-28 | 远期待办 F1–F7（性能复测/encode_query/依赖升版/Retry-After/COS 实网/目标页/不再提示）：条件触发不排硬期，**F1 优先** | ✅ | 00 总纲收尾 §7 / 08 §1.4 |
| 1.8 | 08-29 | **D-06 来电验收改等价复现路径（调研定案，不再卡第二设备）**：①相机抢麦脚本（08-28 `evidence/ck02_grab_trigger` 彩排已通过：am start VIDEO_CAPTURE→点录像→force-stop 释放，数字零窗+恢复可精确触发）②去电模拟 `am start -a android.intent.action.CALL -d tel:<短号>`（nova 11 有卡，Telecom 抢占与来电同源）③看门狗状态机单测；真来电降为可选加测。定性依据：Android 官方《共享音频输入》抢麦写静音=平台政策（evidence/ck02_external_corroboration） | ✅ 本次调研 | ck02 证据族 + 官方文档 |
| 1.9 | 08-29 | **4b 拍板三连**：①客户端 UTS 5.24 迁移**归第三窗**（wrap1-agentA2-ui-restore），fix/4b 让位客户端域——队列域 D-14/05/08 + D-07/06/21/10/U3/D-22 客户端半全部冻结，待其迁移提交后 rebase 统一编译复验；②**D-22 确认并批批次2**（后端半当日落地 81424fb）；③D-09 遗留单腿暴露=**登录态 GET /api/v1/asr/channels**（/healthz 保 G2 最小信息纪律不动，9922e4a 落地）|
| 1.10 | 08-29 | **wrap1 协调与卫生三连拍**：①第三窗**未停手**→fix/4b 与主仓均不碰其分支/不抢编译窗，编译门合流等其收口；②SarasaGothic 字体×3（69MB）**剔出仓库**，wrap1 合流时用 fonttools 子集化（常用汉字+Latin 数字，目标 ≤3MB/字重）——合流点执行；③主区他窗旧脏 132 文件**三重备份后清空**（快照分支 2bad295+patch+文件拷贝，✅已执行，连带暗物质审计见 tracker O-3） |
| 1.11 | 09-01 | **wrap1 合流 + A/B 放行**：①wrap1 波 17 枚经 `cea5025` 跨日 merge 入 develop（老基线 677ea68 真 merge，lessons/progress 双保留）——5.24 迁移正式落地（utils 21 文件 .ts→.uts/Vapor 启用/10 页零错零警+峰宝九条验机通过），字体子集 65.7→26.4MB（§1.10②半执行：剔出改子集化✓、仍在包内，云包实重 R2 实测再拍），33 处 gap 修复零残留实测；②**媒体票据（Valet Key）第四窗在途**：主区 8 文件未提交+HBuilderX 的 hens-svg 插件同步回潮（主区/fix4b worktree 双现，IDE 噪音不碰）；③08-31 全页面审查报告抢救入库 `docs/audit_20260831_*`（detail overlay 两丢失层待 R2 目验）；④AI 对话页=contract-first mock（后端 `POST /chat/messages` 无实现）→ 登记 §5.8 待拍板；⑤用户放行 A（本账）+B（fix/4b rebase onto cea5025+统一冷编译） |

| 1.12 | 09-21 | **内测下发四项拍板（峰宝）**：① 内测登录＝**设备码免输入自动建档**（零 UI 改动）② 内测期存储＝**切 COS**（原图 STS 直传不经服务器）③ 服务器规格＝**8核16G / 系统盘 60G / 带宽 5Mbps** ④ 云打包＝**本轮改造后一次打包**（4b 的 D-18/D-19 关单排其后）。**规格口径冲突（须登记不改交付文档）**：交付文档《外部API清单与成本》§3.1「2核4G 起步、4核8G 更稳」的依据是旧「BGE-M3 568M int8」，与现行实现（fp16 双模型 + SenseVoice ONNX 第三个模型 + **API 与 worker 两进程各持一份**）不符 → **4核8G 会 OOM**；现行结论以 `docs/服务器规格核算_20260921.md` 为准 | ✅ | 用户拍板 · 核算书 |
| 1.13 | 09-21 | **内测期临时口径与上架前回收清单**（三项，上架前必须逐条回收）：① 明文访问 `http://<公网IP>:8010` + `usesCleartextTraffic`（内测省备案等待；端口保持 8010 以对齐 `config.uts` 现状）② 设备登录通道开关 `ALLOW_DEVICE_LOGIN`（默认 False 的 fail-closed 门控）③ `Settings.Secure.ANDROID_ID` 作为设备标识（属个人信息，须在隐私政策「收集的设备信息」条款声明——本轮只登记不改隐私政策正文） | ✅ | 核算书 §0 · 任务卡 §一/卡 C2 |
| 1.14 | 09-21 | **并行开发计划与两处偏差登记**：阶段2（后端上云）∥ 阶段3（客户端改造）以「契约先行 + 文件域硬隔离」推进，任务卡母版 `docs/内测下发并行任务卡_20260921.md`（7 卡 S1/S3/S2/C1/C2/C3/E1）。**偏差-1**：下载页落 `deploy/download/` 而非计划书写的 `client/static/dist/`（`client/static/**` 会打进 APK 且该页须公网独立访问）；**偏差-2**：S1 与 S3 合并为同一会话串行（同占 `deploy/` 目录）。另：`docs/部署就绪包_20260828.md`／`docs/压测报告_20260828.md`／`docs/parallel-dev-收尾/20_内测包构建配置清单.md` 三份被多文档引用的资产**只存在于历史提交、不在 HEAD**（分别 `175de68`/`277b7ac`），按「恢复+更新」处置 | ✅ | 本次规划 |

## 2. 技术与架构补充（开工基线之后新增）

| # | 日期 | 决策 | 状态 | 出处 |
|---|---|---|---|---|
| 2.1 | 08-24/25 | ASR 双通道定档：Fun-ASR Flash 主通道（Data URI 多格式）+ 本地 SenseVoiceSmall-ONNX 情绪增强（置信阈值 0.7，`<0.7` 只记录）；生产入口拒 mock | ✅ | feature_list F3 |
| 2.2 | 08-25 | 转写与情绪拆两个 RQ 阶段：情绪失败不抹真实转写；auto 模式主通道已有情绪则跳过本地推理 | ✅ | PR 审查 5 项 |
| 2.3 | 08-25 | RAG 降级默认档：双层 Rerank 第一层 bge-reranker 已接但**默认关**（CPU ≈850ms/对，50 候选 40s ≫ P95 3s 门禁；GPU 部署时开启）；LLM 改写成「门控+加性」（短关键词原样过，路由固定规则版） | ✅ | progress·RAG 审查 |
| 2.4 | 08-25 | 检索门禁只取产品口径 hit_rate@3≥0.70（recall 分母=类全集是评测集结构缺陷，不作硬门禁） | ✅ | progress·RAG 指标 |
| 2.5 | 08-26 | CI 建库源 = schema.sql（**CI 不能跑 alembic**，issue #2 方向修正）；alembic 仅本地/生产增量；表名漂移 diff 脚本做守卫 | ✅ | CI #21 修复批次 |
| 2.6 | 08-26 | 部署侧约定：22:00 复盘 = 系统 cron 跑 `daily_review.py`（幂等，不进代码调度）；dev worker Windows 用无 scheduler 的 `work()`（RQ Retry 延迟不回投，生产 Linux 全模式） | ✅ | progress·Wave4 / tracker19 §5 |
| 2.7 | 08-25 | 上传分片粒度=单块（UTS 无可靠 ArrayBuffer 切片；MVP 照片 ≤20MB；>8MB 真分片归二期） | ✅ | progress S-ST-1 |
| 2.8 | 08-25 | 客户端 Sentry = 轻量 Envelope `uni.request` 直传（uni-app x 无 DOM，@sentry/vue 不可用）；DSN 按官方惯例内嵌 | ✅ | progress·Sentry 接线 |
| 2.9 | 08-27 | 认证安全定档：refresh single-flight（客户端）+ logout 吊销端点 + refresh_token HMAC-SHA256 独立密钥（hmac$ 前缀兼容存量）+ 短信验证码加盐 + Redis 固定窗限流（降级不 500，429 带 X-Request-ID） | ✅ | progress·G1 |
| 2.10 | 08-28 | EXIF 权威单点 = `backend/app/services/exif.py`（子 IFD 0x8769 优先，华为布局实证），multipart 与分片两路径统一回填；端侧静默回退链保留观测 | ✅ | commit 00a9b08（D-03）/ tracker19 §4 |
| 2.11 | 08-28 | uni-app x 文件落盘口径：`writeFile` data 仅接受 base64(string)+`encoding:'base64'` 或 ArrayBuffer；文本→TextEncoder→arrayBufferToBase64 | ✅ | commit f726942（D-20） |
| 2.12 | 08-19 | STS root ARN → 子账号角色改造：⏸ 用户搁置（待团队子账号 ARN；UPLOAD_005/008 登记已知问题） | ⏸ | OPS-SECRETS / progress·CDE |
| 2.13 | 08-28 | A3 必要偏差裁决（INT 采信先例）：只读文件域（timeline.ts）允许「类字段默认 false 不改构造函数 + 字段名走 contract.ts 常量」的向后兼容零回归式改动 | ✅ | progress·A3/D1 补集成 |

| 2.14 | 09-21 | **契约快照漂移事故（本次发现并纠正）**：`docs/openapi.json` 自 2026-08-27 起停在 **46 路径**，而代码真实导出 **63 路径** → 本次重导后为 **64**（diff：新增 18 / **消失 0**——「只增不减」纪律本身未被破坏，但快照与代码静默漂移一个月）。其中仅 `/api/v1/auth/device` 是本次新增，另 **17 条是历史积压未重导**（胶囊 4 / 内容详情·PATCH·DELETE 3 / 收藏 4 / 回收站 3 / 媒体票据与音频 2 / 统计 2 / 导出 1 / users/me 1）。根因：重导是**人工步骤**，无脚本、无 CI 校验、无「路径数只增不减」自动化断言（已核实 `scripts/review_agent.py` 不含 openapi 检查）。明细已补录进 `docs/OpenAPI契约.md`（含由 openapi.json 生成的 23 行操作表）。**建议**：把「重导 + 路径数 before/after 比对 + 消失即红」纳入提交前门禁或 CI（属 harness 改动，需主窗拍板） | ✅ 本次纠正 | `docs/OpenAPI契约.md` · 卡 S2 |

| 2.15 | 09-21 | **P1 安全缺口（S3 卡发现，未修，待主窗安派）**：`_apply_production_safety` 只比对「默认字符串」，**空串能绕过**——`JWT_SECRET=`（空）会让生产服务带**空签名密钥**启动（同理 `REFRESH_TOKEN_HMAC_KEY`）。规避手段已写进 `deploy/.env.production.template`（必填项一律保持注释状态 → 走默认值 → 触发 RuntimeError 拒绝启动）与 `deploy/RUNBOOK.md` §2（启动后用脚本确认三项 `SET` 且 `len≥32`）。**建议加固**：兜底改为「为空 或 长度<32 或 等于默认值」即拒绝启动 | 🔴 待修 | 卡 S3 · 部署就绪包 §4.1 |
| 2.16 | 09-21 | **备份与 PITR 口径**：① RPO ≤24h 由每日 03:30 `deploy/scripts/backup_pg.sh`（dump + 非空/可读性校验 + 按天轮转）承担；② WAL 归档（RPO ≤5min）**已接线但默认 `PG_ARCHIVE_MODE=off`**——archive_command 失败会让 PG 持续保留 WAL 直至 `pg_wal` 打满磁盘，属「未验证就默认开启反而更危险」，故改为默认关 + RUNBOOK §6 四步开启法（含目录属主 uid 999 与验证 SQL）；③ 本机 Docker/WSL 不可用（README §8.1）→ **S3 全部命令未经真机执行**，上机时按 RUNBOOK §1 逐步跑并回填 | ✅ 口径已定 | 卡 S3 · `deploy/RUNBOOK.md` |

| 2.17 | 09-21 | **D-25 修复落地（客户端运行时录音权限）**：① `yishu-recorder` 新增 `isRecordPermissionGranted / requestRecordPermission / getRecordPermissionName`（复用 D-24 五路径终态守卫范式；**不新增请求码常量**——相册插件的 `REQ_READ_MEDIA=2001` 是零使用死常量，回调由框架代管）；② `client/utils/voice.uts` 的 `startRecord()` 加权限前置：未授权先弹系统框、授权后才开录，**拒绝走既有 `onError` 通道**（宿主零改动即完成 UI 回滚 + 文案提示），并加 15s 看门狗防框架回调丢失导致永久挂起；③ `client/manifest.json` **两处权限块**补 `RECORD_AUDIO`。**重大更正**：`git log -S RECORD_AUDIO -- client/manifest.json` 为空 = 该权限**从未被声明过**，但 2026-08-28 云包尸检（`scripts/realdevice/evidence/ck07_pack_20260828.md:32`）显示**包内早已存在**（云打包按所用能力自动补）→ 故 D-25 的实效不在"让权限进包"而在**"让它被真正授予"**；显式声明仍补，以去除对未文档化自动行为的依赖。**新发现两单**：① `setRecordingActive`（microphone/dataSync 前台服务互斥）**宿主侧零调用点** = 互斥链未接线（B5d §3 设计未落地）；② 权限弹框期间宿主秒表已开始走字（仅首次录音会遇到；真实时长以插件 `recordedMs()` 为准，**非数据问题**）。**未闭环**：本地编译门（`cli.exe launch app-android --compile true`，需用户批准 + HBuilderX GUI 存活）本次**未执行**；D-25 关单须等云包 aapt 尸检（含 `RECORD_AUDIO`）+ 真机授权后录音转写实测 | 🟡 代码就绪待编译门与真机 | 卡 C1 |

| 2.18 | 09-21 | **设备标识插件落点决策（C2 卡，峰宝授权自定）**：**新建独立插件 `yishu-device-id`**，不并入 recorder / photo-watch。理由：① **职责单一**——项目既有能力域隔离（photo-watch=相册 / recorder=录音 / background-tasks=WorkManager）；设备标识是**隐私敏感**能力，独立成域后"谁在用它、用在哪"可查，将来换 OAID 或**上架后直接下线**只需删一个目录；② **降低回归面**——recorder 刚经云包 dex 实证修复（37793e5）稳定下来，不宜再塞无关能力；photo-watch 属相册域（D-26 已有"通知权限不塞进来"的同类裁决：不塞）；③ **零既有文件改动**（纯新增目录）。成本=多几 KB dex 类，可忽略。**取值链**：内存 → **ANDROID_ID**（权威：清缓存/同签名重装不变）→ storage 上次已知 → 框架 `uni.getDeviceInfo().deviceId`（官方文档明载清空 Storage 即变，仅兜底）→ **仅 dev 构建**的本地持久化兜底 id（保住标准基座下的开发循环；正式包宁可显式失败也不静默造新账号）。**配套事实更正两处**：① `logout()` **并非零调用**——早已在 `pages/account/security.uvue`（W10 波次，晚于 08-31 旧矩阵快照）接线，故 C2 卡"settings 补登出入口"指令失效并已撤销（避免造出两个同义入口）；② 该页 `:105` 注释仍称"徽章=微信登录方式"，在设备码登录下**文案失真**（未改，登记待办）。**新增风险**：`config.uts` 的 `ENV='prod'` 是"真机联调临时切 'dev'"的**翻转开关**，漏改即发出砖头 → 已在卡 C3 增出包前置静态门（grep 三条）。**白名单外扩**：`sync_client.uts` / `event_sync.uts`（原 import `auth` 的 `DEVICE_ID` 常量，不一起改会复现"device_id 双源分裂"事故） | ✅ 落点已定 | 卡 C2 |

| 2.19 | 09-21 | **内测下载页（卡 E1）落成 + 4 处实现取舍**：`deploy/download/` 五文件（`index.html` / `style.css` / `assets/app-icon.svg` 手写品牌标识 / `qr/*-placeholder.svg` 两个占位 / `apk/` 投放目录），**严格零 JavaScript**（全页无 `<script>`、无 `on*=` 属性，已 grep 实证）。与原卡的差异（均为**刻意**，逐条给理由）：① **不采用 `<img onerror>` 兜底**——`onerror` 本身就是 JS 属性，与同一条要求里的「零 JavaScript」直接冲突；改为放"永远能加载的占位 SVG"，两者同时满足且不会出现浏览器裂图图标；② 滚动渐显用 CSS `animation-timeline: view()`（无 JS 只有这条路），并**以「默认可见」为初始态**——绝不用 `opacity:0` 当初始态，否则在不支持该特性的浏览器上会**整页看不见**；已用 `@supports` + `prefers-reduced-motion` 双门控；③ 「一键复制邮箱」退化为 `mailto:` 链接 + 可长按选中的邮箱文本（写剪贴板必须用 JS），功能不减；④ Tailwind CDN 的 `@import` 保留，但**版式与配色全部由手写 CSS 承载**（内测用户网络环境不可控，CDN 挂了页面不能垮）。**待替换点 5 处**已用 HTML 注释 `[替换点]` 标记：APK 固定名 `apk/yishu-beta-latest.apk`（放进该目录即自动生效，无需改 HTML）、文件大小、更新日期、下载二维码、反馈群二维码；页脚备案号与隐私政策/用户协议链接为 `#` 占位（待备案完成）。本地静态服务已实测 200（`python -m http.server 8899 --directory deploy/download`） | ✅ 已落成待替换与部署 | 卡 E1 · `deploy/download/README.md` |

| 2.20 | 09-21 | **C3 可完成部分落成：应用图标 / 启动图 / 自有签名证书（云打包挂起）**。① **键名取证**：两份官方文档均明文拒绝给出 `icons`/`splashScreens` 的 JSON 写法，改从 **HBuilderX 自带官方 schema**（`plugins\hbuilderx-language-services\...\schemas\manifestUniAppx.json`）解析取证——`app-android.distribute.icons` 的密度键（`hdpi/xhdpi/xxhdpi/xxxhdpi`）是**直接子键、无 `android` 包装层**；`splashScreens` 子键为 `default/background/icon/brand`。**据此推翻了 `manifest.html` 把层级写成 `splashScreens.android.default` 的说法**（该写法属旧 `app` 节点，`hookManifestJson` 里的 `icons.android` 是其迁移源，二者不矛盾）。② **路径基准**：官方未规定，且语言包证实 `unpackage/res/icons` 仅是「自动生成图标」功能的落点（`unpackage/` 为构建产物目录、已被 gitignore）→ 采用**项目根相对路径** `static/app-icon/*.png`，保证资源真源可入库（避免重演"文件写了、git 里却不存在"的暗物质族）。**残余风险已登记**：若云打包按其它基准解析，现象为"图标仍是默认图标且不报错"；修法 = 10 行字符串改写 + 重打包，**唯一验收手段 = 出包后 `aapt dump badging \| findstr application-icon`**。③ **新增 10 个 PNG（441.4 KB）**：图标 4 档（72/96/144/192）+ 启动图 3 档（720x1280/1080x1920/2160x3840）+ Android 12 中部 Logo 3 档（480/720/960 透明底），几何与落地页 `deploy/download/assets/app-icon.svg` 同源（年轮/光晕，品牌金→锈红）；刻意不配 `default` 的 ldpi/mdpi/hdpi 与 `brand`（官方称"此类设备不常见"，省体积）。④ **签名证书（仓库外）**：`%USERPROFILE%\.yishu-signing\yishu-guanghua-beta.jks`，alias `yishu`，SHA256withRSA/RSA2048/PKCS12，有效期 10950 天；**证书 MD5 = `48:D7:6D:BA:80:AC:66:CB:5E:CD:2B:8C:BE:99:25:FC`**（备案用；同目录另有 SHA1/SHA256 与导出证书）。密码全程仅以随机变量传递、未在命令文本与对话中回显，只写入同目录待转移文件。⑤ **静态门 2/3**：`ENV='prod'` ✅、`RECORD_AUDIO`=2 ✅、**`PROD_BASE_URL` 仍为 `<公网IP>:8010` 占位 ❌** → 按卡面纪律「任一条不符不得打包」**云打包与 aapt 尸检明确挂起**，非冒充完成。解冻条件 = 服务器就绪后填真实地址并重跑三门（无新开发）。⑥ 未启用 Android 自适应图标（需手写 `nativeResources/android/res/mipmap-anydpi-v26/icon.xml` + 前景/背景 vector，HBuilderX 5.25+）；证书主体字段是 ASCII 占位（规避云打包非 ASCII 兼容风险），若日后上架要求主体一致需重生成证书（换签名=换身份，已发布包须卸载重装） | ⚠️ 部分落成，打包挂起 | 卡 C3 · `docs/图标与签名证书_20260921.md` |

| 2.21 | 09-21 | **收口到「只差换址」：明文放行 P0 补齐 + 出包前置门可执行化 + 换址清单**。① **P0（暗物质族，性质最坏的一类）**：`usesCleartextTraffic` 在**五处文档**（`config.uts` 注释、`部署就绪包_20260921` §2.7、`RUNBOOK` §5、`服务器规格核算` §0、本台账 §1.13）被写成"已生效"，而 `client/AndroidManifest.xml` **里根本没有**——开发期之所以无感，是**标准调试基座自带放行明文**，故该缺陷**只在云打包出包后暴露**，且现象是"所有页面全空且不报错"（本项目最贵失败形态）。已补 `android:usesCleartextTraffic="true"` + `tools:replace`（防基座 manifest 合并冲突）。② **出包前置门可执行化**：C3 的三条手敲 grep → `deploy/scripts/preflight_pack.ps1`（7 段：环境开关/权限/图标启动图/明文自洽/真源可入库/服务端提醒/出包后待回填；退出码即判据）。实测 **1 FAIL，且唯一 FAIL 就是地址占位**——即"其余全就绪"的机器可验证据。③ **换址清单 `deploy/ONE_SWAP.md`**：换址**只有 2 处**（客户端 `config.uts:PROD_BASE_URL`、服务器 `deploy/.env:BASE_URL`），端口固定 8010 不改、`CORS` 不改、后端刻意不持有 host（媒体 URL 发相对路径）故无 host 漂移；附一键替换命令（**无 BOM 写回**——BOM 会污染工程源）。④ 顺带修复：`security.uvue` 微信行硬编码「已绑定」徽章 → 「未绑定」（C2 设备码登录落地后微信并未绑定，属失实表述；原 `wechatLogin(code=dev-client)` 时代残留）；`auth.uts` 的 `deviceLogin` 畸形 200 响应（缺 token 字段）原为 `as string` 强转**运行时崩**，改为 `?? ''` 判空后诚实失败；`config.uts` 占位守卫由只认「公网IP」泛化为特征词（含 `example.com`/`localhost`/`127.0.0.1`/`10.0.2.2`/缺 scheme）。**更正上一轮误判**：`logout()` 并非"零调用"——`settings.uvue → goAccount() → security.uvue onLogout` 链路早已存在且可达 | ✅ 已落成 | 卡 C3/E1 + 本批收口 · `deploy/ONE_SWAP.md` |

| 2.22 | 09-23 | **服务器到货回报单 + 两个"开工即卡"（上机前发现）**。产出 `deploy/SERVER_INTAKE.md`（§A 访问网络/§B 规格地域/§C 系统环境/§D 域名备案/§E 云资源**只报就绪状态不贴真值**/§F 两个待拍板决策/§H 服务器一键采集命令/§I 收到后立刻做什么）。发现两处**不填就拿不到地址也开不了工**的隐藏项：① **`origin` 里没有本批任何东西**——核实 `git remote` = `git@github.com:zqhmy1234/YSGH-APP.git`，但本批 30+ 项改动（**含整个 `deploy/` 目录**）全部未提交，HEAD 仍是 `65f40a3`；服务器上 `git clone origin` 拿到的代码里**连 `deploy/RUNBOOK.md` 都不存在**，更无 `config.uts` 的地址改动 → 代码上机方式必须拍板（推荐「工作树打包直传」：零决策依赖、不碰"push 归用户"纪律；备选「提交后 bundle」「你 push 后 clone」）。② **发行版 Python 版本**：RUNBOOK §0 要求 Python 3.11，而 Ubuntu 22.04 自带 3.10、24.04 自带 3.12 → 回报单 §C1 必填 OS 版本，否则"系统准备"步骤照跑会失败。③ 顺带确认 `deploy/scripts/*` 五个脚本（bootstrap/healthcheck/pull_models/deploy_one/backup_pg）**均存在**，非暗物质。④ 修正 RUNBOOK §0 编号重复（两个"4."，上批引入）。⑤ **用户拍板（09-23）**：上机方式＝**工作树打包直传**（不用 `git clone`）；执行方＝**Agent 从本机 SSH 驱动**。据此改 RUNBOOK §1.1 的上机第一步为「本机打包 → 校验 sha256 → scp → 服务器解包」（原写法 `git clone <repo>` 会拿到旧代码，已废止）。⑥ **回报单按用户要求二次收敛**：删除 §E 云资源与密钥现状（已核实过，无需再问）、§F 待拍板决策（已拍板）、§D 域名与备案、§I/§J——**只保留继续开发所需的信息**（§A 访问网络 / §B 机器与系统 / §C 一键采集） | ✅ 已拍板并落地 | `deploy/SERVER_INTAKE.md` · RUNBOOK §1.1 |

## 3. 数据与产品口径

| # | 日期 | 决策 | 状态 | 出处 |
|---|---|---|---|---|
| 3.1 | 08-25 | 真值数据四决定：来源=beta 期真实用户（M1/M2 门禁仍以合成基准为准）/ expected_ids+expected_label 双轨 / 人脸打码+录音只收自述片段 / JSON 交付 | ✅ | 峰宝 grill 拍板 / docs/真值数据规格标准_v1.md |
| 3.2 | 08-25 | 真值定位=**给产品部的人工采集手册**（产品部是收集者）；不做自动采集管道（明确不做 search_log 埋点）；技术侧仅可选辅助脚本 | ✅ | 峰宝复核修正 |
| 3.3 | 08-28 | 关怀文案：40 条默认库兜底可上线（换文件即生效），正式库与基调待产品部 | ✅ **08-29 基调拍板，默认库转正式 v1（§5.5 销项）** | docs/copy_library/ · 08 §1.4 |
| 3.4 | 08-2x | 产品部既有确认：回响敏感双查（画像 L1 + 检测，SAF-006/007）；情绪关怀分层（SAD 分原因追问/ANGRY 陪伴/深夜轻量/频次递减）；软删 30 天；用户操作优先算法不覆盖 | 📖 | P2-ECHO/F3 证据 / 转达稿 |
| 3.5 | 08-28 | 情绪"未测出"显示口径定档：emotion_source=none 时 API 返回 null/「未识别」，UI 不渲染情绪 chip——**禁止三层默认值伪造"平静"**（D-16 修复即按此验收） | ✅（待落码） | tracker19 §4 D-16 |

## 4. 流程与门禁（harness 自身）

| # | 日期 | 决策 | 状态 | 出处 |
|---|---|---|---|---|
| 4.1 | 08-20 | 教训登记程序化强制：排查修复后必 `scripts/lessons.py add`，失败后未登记阻断 commit（不可绕） | ✅ | lessons.py + check_lessons |
| 4.2 | 08-26 | 审核门禁拆分：快速门禁=commit 时（hook 自动）；全量门禁=完成 DoD/集成/CI 前必跑 `--full` | ✅ 用户拍板 | AGENTS Pre-Commit Gate |
| 4.3 | 08-26~27 | 并行/集成纪律：文件域所有权表（17 号）· lessons 冲突保留双方 · 契约快照只增不减（openapi/errors）· models.py+migrations 冻结 · 密钥只走 Infisical | ✅ | 13 号 · 14 号 |
| 4.4 | 08-27 | 真机证据铁律与"待补"规则：缺任一前置标"待补"，不得虚构 A 级 | ✅ | 15/19/21/22 号 |
| 4.5 | 08-28 | 数字口径规范：状态×证据级两维不合并；跨文档数字必须回源核实；汇报"打通数变化"须带基线时点+逐条列出移动 | ✅ | AGENTS · lessons 08-28 20:53 |
| 4.6 | 08-29 | **命名约定**：行文一律写「开发 Wave n」「收尾 Wave n」「4b 修复批次 批次 n」；「F 前缀」三族（功能 F1–F9 / 远期待办 F1–F7 / 真值 A–G 批）引用时必须带族名 | ✅ 本次整饬 | 本簿 §0 |
| 4.7 | 08-20 | 交付文档只读纪律；修改须用户明确同意（先例：08 快照数字随补验同步 cbc1751 属用户授权例外） | ✅ | AGENTS Working Rules |
| 4.9 | 08-29 | **文档/工作区清理拍板（用户授权「过时的文档，空的旧的文件夹一律删」）**：删除 4 份孤立执行类文档（PR1_审查报告 + RAG管线审查/指标提升/生产兜底审计，结论已被权威文档吸收，git 保留）；拔除 13 个旧 worktree（.wt 全部+backend/.wt）；清 464 空目录（uploads 残留/APK 尸检树/truth-data 占位）；**保留判据=被代码/测试/交付文档/台账 live 引用**（refactor-plan/技术债三件套/批次F-H 提示词/Sprint2/COS/B2对照/RAG测评 等 46+ 处核查后全保留）；`backups/20260827_残留归档/` 按 D8 拍板保留（交付文档 08 明载，删除会断证据链） | ✅ 本次执行 | 本条 · lessons 族7 |
| 4.8 | 08-29 | **harness 工具不销毁语义 + EOL 守卫**：`lessons.py add` 表头失配只告警+分隔线后安全插入（08-29 硬编码 HEADER 失配曾清空 1199 行台账）；`.gitattributes` 立 `*.sh/*.py eol=lf`（init.sh 曾被写成 CRLF 致 bash 不可执行）；大文件改写=临时 .py+唯一命中断言 | ✅ 本次整饬 | lessons 08-29 01:24/01:30 |

| 4.10 | 09-23 | **harness 二轮清理（用户授权「CEF全删」，部分**取代** §4.9 的保留判据）**：删除 **91 份**（`docs/` 512→428 文件、2.3→1.87MB）——① **A 档 51**＝`missing-pages-restore/`(32)+`parallel-dev/`(14)+`parallel-dev-收尾/`(5)；用户判断"引用他们的都是已关闭的过去项"**经实测基本成立**（其"未关闭标记"多为**假阳性**：`卡12` 命中的是界面文案「2 条记忆待补传」、`15/22` 命中的是通用规则文本、`00_总纲` 只是配套清单）② **C 档 9**＝`_l0_refine_*.json`（聚合结果在 `画像维度枚举集_l0.json`）③ **E 档 31**＝一次性过程报告与提示词——**含 §4.9 曾「核查 46+ 处后决定保留」的 refactor-plan／技术债三件套／批次F-H提示词／Sprint2／B2对照：本次按用户新指示删除，该保留判据就此作废** ④ **F 档**＝根目录杂散（`+` 无名文件、`_verify_0905/`）。**硬保留 3 件（承载未关闭事项，删＝制造暗物质）**：`parallel-dev-收尾/19_wave3_真机补验跟踪表.md`（**D-18/D-19 仍「已修待复验」**）、`parallel-dev/_B5a_B5d修正后待办.md`（**J-1/J-2/J-3 未闭环**）、`P2诊断_O1O2_20260829.md`（**O-1 未定性**的取证包，AGENTS.md 已知环境项引用）。**引用清扫**：活文档已同步（`AGENTS.md` 仓库结构段+执行窗注、`docs/README.md` 索引、本表 §0）；**历史日志不改**（`progress.md`/`_diff_ledger.md`/`lessons.md`/`session-handoff.md` 的引用按历史事实保留）。**前置铁律**：删除前必确认已 tracked（未跟踪文件不可恢复）——唯一未跟踪的 `画像维度枚举集_*.bak.json` **刻意未删**。回看：`git log --diff-filter=D -- <路径>` | ✅ 本次执行 | 本条 · `docs/审计_未关闭缺陷_20260923.md` §7 |

| 4.11 | 09-23 | **harness 三轴审计工具落成（把审计发现门禁化）**：`scripts/audit_harness.py {openapi\|client\|models\|all}`（退出码 1=有 CRITICAL）。为什么需要：审计发现三类静默漂移**此前无任何门禁覆盖**——① 契约漂移（openapi 再生是人工步骤、无 CI 断言）② 客户端漂移（注释里的死路径、`USE_MOCK_*` 无清点手段、`.ts` 残留）③ 模型使用率（"建了表没人用"）。**首次实测结论**：契约 **64/64 完全对齐**（无漂移；`OpenAPI契约.md` 所述"积压 17 条"已由 09-21 重导消除）｜客户端 **无死路由、无 .ts 残留**，但查出 **B8 清单已过时**（8 个 mock 开关实际只剩 1 个，见 `远期待办总账.md` 头部注）｜模型 25 类 **0 引用 0 个**（16 个低引用留人工判）。**工具自身两个 bug 已修并留痕**：路径比对未归一化前导斜杠曾误报 **44 条"死路由"**；裸词匹配曾把注释里的开关名算成真开关（9→1）。**教训**：工具的假阳性比缺陷本身更危险——会直接污染台账 | ✅ 本次落成 | 本条 · `docs/审计_未关闭缺陷_20260923.md` §8 |
| 4.12 | 09-23 | **Agent 交付包入库 + 临时 lint 豁免（带撤销待办）**：开发部同事的 Coze Agent 项目（LangGraph + 13 工具 + Supabase 三表 + 企微长连接）**全量**入 `agent/`（50 文件 / 778.8 KB，SHA256 `82314B8D…E5D3`），出处/读码结论/去 Coze 改造映射表/7 条已知缺陷详载 `agent/UPSTREAM.md`。**用户拍板**：形态＝独立进程 + 我们后端反代（JWT→**可信 user_id 注入**）且**全量去 Coze 运行时**；数据落**我们 Postgres**（alembic 三表 + PostgREST→SQLAlchemy repository）；**先接 App 客户端 AI 页**；模型改**阿里云百炼**（复用 `core/config.py:54-59` 既有凭证口径）。**临时豁免**：`scripts/review_agent.py:_skip_path` 加 `"agent"`——上游 354 处 lint 违规（239 可自动修 + 115 手动，含 `S310` URL 打开 4 / `S110` 静默吞异常 3 / `DTZ005·003` naive datetime 7 / `PLW0603 global` 12 等真问题），不与"保住交付物（临时目录会被清理）"抢优先级；**豁免必须在改写批次 AG7 完成时删除**（已同时登记排期 AG7 与代码内 `TODO(AG7)` 注释，双处可见） | ✅ 入库；⚠️ 豁免待撤 | `agent/UPSTREAM.md` · 排期「Agent 接线批」 |

| 4.13 | 09-24 | **易扩展/易维护重构波启动 + 结构棘轮落地（门禁新增）**：① 先做**逐功能域只读审计**出重构台账（14 域，P0×3 / P1×9 / P2×5，唯一排期事实源＝`.trae/specs/refactor-extensibility-maintainability-wave/refactor-ledger.md`）；② **结构棘轮**并入提交门禁——`scripts/audit_harness.py` 新增**轴 5 文件体积**（backend/app `*.py` ≤600 / client `*.uts|*.uvue` ≤800：存量 7 个基线冻结、**新增超阈即 CRITICAL**，存量增长 WARN）、**轴 6 重复模式**（`deleted_at.is_(None)` 手抄总数不得上升，基线 44 处/21 文件）、**轴 3 修镜像表假阳性**（`models/memory_agent.py` 镜像模型 0 引用属**预期**——仅为 metadata/alembic 一致与漂移守卫，非"建了未用"）；③ `scripts/review_agent.py` 新增 `structure` 检查（快/全量均跑，秒级）；基线 `scripts/audit_harness_baseline.json` **只允许收缩**（每条带 reason/target）；④ 声明漂移清零（§3.1 三则 09-23 已修；残余两处注释含「对齐原 …」，轴 2「原」标注抑制生效）；⑤ 脚本域凭据/可移植性清理（`check_schema_drift.py` 移除内联默认凭据 `postgres:admin` → `--admin-url`/`DRIFT_ADMIN_URL`，缺则 exit 2；`backup_pg.ps1` PGBin 探测；个人绝对路径清理）。**门禁纪律（新铁律的落地）**：新增门禁**必须先做负向探针自校验**（清空基线应全数命中，证明非空转）——先例是 `audit_harness` 曾误报 44 条假死路由、本次又实测 1 条镜像假 CRITICAL，**假阳性比缺陷更危险**（会污染台账）。**本次顺延（非偷懒，均已给理由）**：B2 后端巨文件拆分（`contents.py` 902 行实为 **4 router 混居**，须转 `api/contents/` 子包 + 专用验证窗口；部分抽取不解除棘轮故收益为零）、B5 客户端拆分（编译门不可用）。 | ✅ 本次执行 | 本波 spec/ledger · commits `6acd826`/`09708d7` |

## 5. 🔴 待拍板（3 项，销项须回填本表）

| # | 事项 | 背景/出处 |
|---|---|---|
| 5.1 | 部署就绪包确认 | `docs/部署就绪包_20260828.md`（C3 产出待用户确认） |
| 5.2 | 自定义分类管理页是否入 MVP | A3 纠错弹层「确认」后无目标页，当前 toast"即将上线"（18 号契约只定义弹层） |
| 5.8 | AI 对话页后端端点归宿 | 09-01 wrap1 波 contract-first：`POST /api/v1/chat/messages`（kind=bubble…）客户端 mock 已上线，后端未实现——入内测批次补真联调，还是砍 mock 归二期？（08-31 审查报告同页另见 detail overlay 缺层，随 R2 目验） |
| 5.9 | **`schema.sql`（CI 建库源）与 ORM/alembic 谁是权威** | 重构波 B4 实跑 `alembic check` 取证：漂移量大且含**破坏性项**（多表 `remove_fk`/`remove_index`、大量 TEXT↔String、索引改名）。二选一：① 补一条显式迁移 drop 遗留表（**须先证零引用**）② ORM 补声明后 `--autogenerate` 对齐。**未定权威前禁止直接以 autogenerate 落迁移**（会真删外键/索引，属可删约束的静默事故） |
| ~~5.3~~ | ✅ **已拍板（08-29）：取消=永久不再提示**（老人防打扰），接 dismissCorrectionPrompt hook，R1 随散单实施 | 原：hook 已就绪未接线（U3） |
| ~~5.4~~ | ✅ **已拍板（08-29）：改标 'device'**（与 generated_by='device' 对齐，避开 B3-5 背书保护误触）；tracker19 D-15 行已同步，R1 随批落地 | 原：D-15 端云语义待拍板 |
| ~~5.5~~ | ✅ **已销项（08-29 用户拍板）**：基调维持「温和克制」定稿 + 禁区 10 项照单通过 + 现有默认库**升格内测正式基线 v1**（docs/copy_library/，产品部正式稿到位仍仅换 JSON 触发逻辑不动）——US-21 卡点从「文案库+D-16」缩为「仅 D-16」 | 原：产品部（08 §1.4；现 40 条默认兜底） |
| ~~5.6~~ | ✅ **已销项（08-29 用户授权）** | 交付文档旧数字同步：08 line12/21/173 与 02 全表（含 6a7f0f9 漏回填的 US-12/25/40/41/42/48 六行+统计行+半通清单）已同步为 ✅46/🟡7/❌0 · A 级 32；02 顶部加注记指回权威口径 |
| ~~5.7~~ | ✅ **已拍板（08-29）：并批批次2 随 R1 修**（后端半当日落地 fix/4b 81424fb；客户端半冻结随 5.24 迁移 rebase）。原提案：建议随批次2（客户端向：record.uvue 纠错流重构+classifier 降级标记+/corrections 回写+测试盲区修复；污染 correction_log 清洗走人工确认另列 R3） | docs/P2诊断_O1O2_20260829.md §2.3；缺陷单 tracker19 §4 D-22。另：US-25 现为 ✅A 级，与 D-22 现象矛盾，复验波须重断 |

## 6. 等待团队速览（三座大山，详见 08 §1.4）

- **凭证（负责人）**：企微三件套 / 微信 AppID·Secret / 阿里云短信 AK / uni-push 厂商通道 / 内测包 M1 公网服务器·域名 · M4 HTTPS 证书 · M6 DCloud 签名账号——代码侧 501 门控+Infisical 就绪，到位即插。。**08-29 进展（负责人通报）**：企微 5 项中前 3 就绪（CORP_ID/TOKEN/AES_KEY），APPID/SECRET 待云打包后去微信平台申请；可信 IP 暂配开发机出口 61.171.241.17（手机流量 CGNAT，随网络漂移——换网后 curl ip.sb 向负责人追加新 IP，可多配）。判定：入站回调仍需公网 URL（M1 卡），get_access_token 仍需 SECRET——US-31/32/33 卡点性质由「凭证未到位」缩为「等打包+M1 服务器」。**08-29 企微后台口径补充**：『可信域名』两栏（OAuth 回调 / JS-SDK·跳转小程序）不收 IP、须 ICP 备案域名且备案主体=企业主体或关联——我方 MVP 企微链为纯服务端 API（微信客服收发消息），无企微内嵌 H5/小程序 → **两栏暂留空**；61.171.241.17 只配进『企业可信IP』栏；M1 取域名时以公司主体备案，JS-SDK 栏将来需域根放置 WW_verify 校验文件
- **数据（产品部）**：B/C/D 批真值 + A 批补真实负样本（≥10 条且 ≥70%，误召回 0.5714 重校）+ E/F/G 批排期——harness/validate 就绪，到位即跑。
- **合规（运营）**：企微认证 / ICP 备案 / 软著三申请**未提交**（5–6 周串行，M0 硬依赖）+ 隐私政策定稿审阅签字。

---

## 7. 🟠 未关闭重大缺陷（2026-09-23 系统性审计登记）

> 来源：`docs/审计_未关闭缺陷_20260923.md`（方法：**以代码为唯一事实来源**，`docs/**` 的"已完成"宣称一律不采信）。
> **登记规则**：① 销项须回填本表 ② 登记前**必须先核"是否已有记录/决策"**，防假缺陷——本次已据此**撤销 3 条**（登录 501 内测口径 / 定时任务已在 RUNBOOK §5 / reranker 默认关），详见审计报告 §4。
> **与 §5 的区别**：§7 = 已实证做不到或不达标；§5 = 尚未定方案。同一事可从 §5 升级（先例：§5.8 → §7.1）。

| # | 缺陷 | 级 | 关键证据（代码为凭） | 用户可见后果 | 状态 |
|---|---|---|---|---|---|
| 7.1 | **产品 Agent 系统＝全 mock，且无法关闭** | 🔴 | 后端无 chat 路由（`main.py:117-140`）；`TabAi.uvue:5-9` 自认 mock；`:9` 声称的 `USE_MOCK_CHAT` 常量**全仓不存在**（只在注释里）→ 无条件硬编码；入口 `TabBar.uvue:86-89` 真实可达 | 点开 AI Tab 能"聊"、有卡片与确认按钮，**但 0 后端能力**，用户以为智能体已上线 | ⏸→✅ **已落成最小修复层（commit `9632710`）**：`USE_MOCK_CHAT` 补真身（原"常量"只存在于注释）+ 三处接线点按其分支（`false` 时诚实提示「尚未接入」、**不伪造回复**）+ 页面顶部「内测演示数据 · 智能体尚未接入后端」标注。✅ **客户端编译门已跑通并销账（2026-09-23 19:39）**：HBuilderX 5.x 在 `D:\GuangH-App\client` 上**增量编译 + 全量冷编译（`--cleanCache true`）双通过**（后者 63470ms，SKILL §6 硬规矩：全量才暴露存量错）。原始欠账记录（09-23 上午）：HBuilderX 未运行 + 空闲内存 1869MB < SKILL 闸值 2000MB → 当时**不点火以免制造假通过**。**拍板（用户 09-23「三件同意并通过」）**：采纳现状方案＝**保留标注 + 可关化**（即最小修复层）；「接线」仍待后端 chat 能力（重，不在内测范围）。⚠️ 若此处对"三选一"的理解与原意不符，请纠正——我按"批准推荐方案"登记 |
| 7.2 | **RAG 整体门禁 FAIL：负样本误召回 57%**（阈值 5%，超 11 倍）；五层评测**全 placeholder** | 🟠 | `research/rag_benchmark/evaluation_report.json:163` `overall_pass:false`；`:145` `0.5714` vs `:160` `0.05`；`:66/:75/:84/:93/:102` `placeholder:true`；`:146` `placeholder_n:16` | 搜索"不相关也召回"；约 1/3 查询类型**无质量基线** | 🔴 未关闭（§6 数据行仅提"重校"，**整体 FAIL 与占位层无记录**） |
| 7.3 | **多模态图搜：客户端零接线 + 无图片查询评测**（P95 旧值超门禁一说**已于 2026-09-23 更正**） | 🟠 | `client/utils/search_api.uts:252` 导出 `searchByImage` **全仓无调用方**；`TabSearch.uvue:69` import 不含它、模板无相机/相册入口；`scripts/eval_image_search.py:42` 实测的是**文字**检索；`feature_list.json:64` 记 P95=7629ms，**但同字段后文写明 08-26 caption 缓存已降至 2013ms（门禁内）**——初次登记误用旧值 | 宣称的"多模态搜索"**用户完全触达不到** | 🔴 未关闭（**P95 部分已更正**：2013ms 在门禁内，当前值未复测） |
| 7.4 | **推送通道全 mock（`MOCK_PUSH`）** | 🟠 | `backend/app/services/notify.py:52-56` 只写日志；`:4-5` TODO(T1)；`:212-215` 恒走 mock | 每日复盘／语音完成／胶囊到期等 push **用户端不可见**（仅 in-app 消息中心） | 🔴 未关闭（**此前无任何记录**） |
| 7.5 | **蜂窝网络下照片永不自动上传**（省流缩略图上传从未启用；注释理由已过期） | 🟠 | 客户端 `uploader.uts:196-200` `mode=='thumbnail' → 'hold'`（注释"Agent G 未完成前"）；`:209-211` `cellular → 'hold'`；而后端**早已就绪**：`services/upload/protocol.py:28` 支持 `thumbnail_meta`、`api/upload.py:71,79,86-91`、`api/thumbnails.py` 已接线 | 蜂窝用户拍照上传被**无限期挂起**等 WiFi | 🔴 未关闭。**2026-09-23 复核更正**：性质是"**未接线**"而非"能力缺失"——客户端压缩能力具备（`uni.compressImage` 在 app-android 受支持，HBuilderX 自带 dts `uni-media/utssdk/interface.d.ts` 的 `@uniPlatform` 含 `app.android`，已取证），后端语义＝上传物即缩略图、WiFi 后凭 `meta.content_id` 补传原件（`register.py:43-49`）。本轮已更正 `uploader.uts` 注释与 `UploadStatusBanner` 失实文案；**接线本身需编译门＋真机，已登记 `docs/待办处理排期_20260923.md` A1** |
| 7.6 | 情绪能力未校准 + 桌面端 F9 零实现 | 🟠 | `feature_list.json:42`（文案库待产品部／S2 开心漏报 0.496 待 C 批校准）；`:109-111` F9 `not-started`；`research/asr_bench/wer_report.json:4` avg_cer 0.0969（n=20） | 情绪关怀内容供给与分类校准未闭环；桌面端 0 实现 | ⏸ 部分已记录（§6 数据行） |
| 7.7 | 声明漂移三则（误导审计与维护） | 🟡 | `client/pages/shell/shell.uvue:7` 称"老四页仍在 pages.json"（**实际已删**）；`client/utils/search_api.uts:183` 陈旧 mock 叙述；`feature_list.json:64` 同条 evidence 自相矛盾 | 后续会话据错前提工作 | 🔴 未关闭 |
| 7.8 | **零调用的能力导出 9 处 → 0 处（✅ 09-23 全部闭环，轴 4 现为常驻绿灯）**（能力写了、客户端根本没接线）——由**新增轴 4** 机器查出（新增门禁首查即得，说明这类"能力写了没接线"此前完全无防线）。**逐项处置（每项均：轴 4 复跑降数 + 编译门通过 + 独立提交）**：① `searchByImage` **接线**（`TabSearch` 加"以图搜"入口 → `POST /search/image`；`c1825d0`）② ③ `addBreadcrumb`/`captureMessage` **接线**（搜索前/图搜前/对话发送记面包屑 + 搜索降级发 warning；**隐私纪律：只记长度/标签，绝不记用户原文**——面包屑随事件发往 Sentry 外部服务；`90299d4`）④ `pendingPhotoCount` **删除而非接线**（横幅刻意分开显示 held/failed 两种文案与 emit，合并会丢掉"待上传 vs 失败待重试"这一用户操作分界；其注释自称"供 UploadStatusBanner 展示"而横幅从没用过=失实注释；`25e00ec`）⑤ `MAX_DIRECT_UPLOAD_BYTES` **接线**（`transcribeWav` 拆「闸+真身」：超 8MB 不发直传、告知改走分片，修掉"30min 录音必被后端 8MB 上限拒绝"；`f45a813`）⑥ ⑦ `enqueueDeleteOp`/`enqueueFieldOp` **接线**（play.uts 离线入队 + detail.uvue 两处文案改口与本地墓碑；**三件一起动**，否则离线入队却报"失败"= 状态与事实不符；`6188be8`）⑧ ⑨ `submitArbitrate`/`pollArbitrate` **删除而非接线**（P2 诊断 §2.1 RC1 指认其旧用法"用裁决回声覆盖用户所选"正是 O-2 恒「混合」的元凶；§2.3 P0 修复已落地=`RecordSheet.uvue:978/1328` 直接 `submitCorrection(用户所选)` 全程不调仲裁 ⇒ 这两个导出是被取代的旧破设计残留，接回去＝重新引入 O-2；`f022938`）。**余 0 项**。以下为 09-23 当天逐项过程中的探查记录，**保留作历史**（其结论均已被上述最终处置覆盖，且两项"曾是待办"的 B12/B11 已分别以 `6188be8`（接线）与 `f022938`（删除残留）闭环）。（`MAX_DIRECT_UPLOAD_BYTES` 已于 09-23 接线生效：`transcribeWav` 拆「闸+真身」，超 8MB 不发直传、明确告知改走分片，`f45a813`）：

**B12（`enqueueDeleteOp`/`enqueueFieldOp`）为何不能"加一行就接完"**——探查结论，下一轮直接照此做：
  · 现状：`play.uts:429 deleteContent`（`DELETE /contents/{id}`）与 `play.uts:620 updateContentRemark`（`PATCH`）**只走在线**，离线即白丢用户操作；而 `sync_client.uts:130/111` 的入队 API 早已写好。
  · **拦阻是语义而非代码量**：离线入队后**仍有第三态**——"已排队"既不是成功也不是失败。而调用方 `detail.uvue:460`（`删除失败 · 请稍后重试`）与 `:541`（`云端保存失败`）把 false 一律当失败 ⇒ 照抄会得到「用户看到失败、操作其实已排队」的**状态与事实不符**。
  · 正确接线需三件一起动：① 离线判定（`uploader.uts getNetKind()` 返回 `none`）② 入队 ③ **调用方文案与本地态同步**（删除需本地墓碑隐藏该条 + 文案改"已离线，联网后自动完成"；备注改"已离线，联网后同步"）。三者缺一即制造失实状态。
  · 判据：`pendingSyncCount()` 的上屏位置（同步卡）需一并确认，否则用户无处看到"待同步"。

`submitArbitrate`/`pollArbitrate`（B11，**动前必读 `docs/P2诊断_O1O2_20260829.md`**——把"裁决链路压根没接"并入既有"回声覆盖+层①锁死"诊断） | 🟠 | `python scripts/audit_harness.py exports` 实测（296 个导出符号中）：`searchByImage`（图搜，**= §7.3 的机器佐证**；✅ **已接线 09-23**：`TabSearch` 增"以图搜"入口 → `searchByImage` → `POST /api/v1/search/image`，轴 4 复跑由 9 降 8）· `enqueueDeleteOp` / `enqueueFieldOp`（sync_client —— **删除与字段编辑连本地入队都没接**，佐证 A3/A8）· `submitArbitrate` / `pollArbitrate`（text_recorder —— **纠错裁决链路客户端零接线**，与观察项 O-2 / 缺陷 D-22 同域）· `addBreadcrumb` / `captureMessage`（sentry —— **监控埋点未接线 ⇒ 线上问题无迹可循**）· `pendingPhotoCount`（上传横幅计数未使用）· `MAX_DIRECT_UPLOAD_BYTES`（voice 直传阈值已定义未生效） | 图搜 / 删除 / 纠错裁决 / Sentry 埋点**四类能力对用户与运维均不可用**，而文档多处写"已实现"；O-2 与 D-22 因此多一条独立线索（裁决链路压根没接） | 🔴 未关闭（**新门禁首查即得**；另 18 个零调用契约常量单列 INFO 供人工判） |
| 7.9 | **`<scroll-view>` 属性缺陷族 3 处**（`scroll-y` / `scroll-x` 在 uni-app x 上**不受支持**、被静默忽略） | 🟢 | 由**编译门**（09-23 首次点火）抓出：uni-app x 的 scroll-view 只定义 **`direction`**（none/vertical/horizontal/all）+ `refresherEnabled`——HBuilderX 自带 dts `dom2/UniNativeScrollView.d.ts` 实证，且全仓无 `scroll-y` 声明。命中 `TabAi:23` `scroll-y`、`TabAi:59` `scroll-x`、`theme-detail:18` `scroll-y`。**其中 `TabAi:59` 是真缺陷**：记忆卡横滑组本应横向可滑（画布 4:323「第二张露边示意滑动」），`scroll-x` 被忽略后退化为默认方向（视觉/交互双错） | 另将 **5 处依赖默认方向**的 scroll-view（TabIndex / TabSearch / favorites / manage / agg-check）**显式化** `direction="vertical"`——显式化对"默认即 vertical"等价、对"默认非 vertical"则是修好，**严格占优**；`aria-label` 在 `<view>` 上同样不受支持（`TabIndex:51` 骨架屏**死属性**）已清除 | ✅ **已修复并验证**：全量冷编译通过、`scroll-y`/`not supported` 警告归零。**警告基线**（刻意保留、非缺陷）：`backdrop-filter` ×2（theme-detail/TabBar，注释写明真机实测生效）+ `uts插件[yishu-background-tasks] 在标准基座不生效` 提示（D-19 域） |
| 7.11 | **审计/排期自身的假阳性：多个"待做"项其实早已完成**（A2 J-2、A3 J-3、A4 P-5、A5 B7、A7 `/users/me`，以及 **B 档全部后端端点**） | 🟡 已更正 | 09-23 用户要求"一路继续"时按纪律**先验码再动手**，逐项取证：A4 `theme-detail.uvue:127-130` 已是 `fetchTimeline(2)`（注释即"P-5 收口 2026-09-04"）· A5 `sync_client.uts:539-546/559` `getLastSyncAt()` + `LAST_SYNC_KEY` 落盘 · A7 `api/users.py:27 GET /me` 真实存在 · A2/A3 见 §7.10 · **B 档后端端点全部已存在**：`contents.py:620 DELETE /{content_id}`（软删）、`:641/:660 POST｜DELETE /{content_id}/favorite`（收藏）、`search.py:36 POST /search/image`（图搜） | **危害**：若不验码，就会去"实现"已存在的东西（重演图搜式浪费）；根因＝审计抽样 A/B 档时**采信了 08-26 的陈旧文档**（`_B5a_B5d修正后待办.md`、`远期待办总账.md` 等）而非**当日代码** | 🟡 已更正：排期 A2~A5/A7 销项（逐项附证据）+ §7.10/§7.11 登记 + README 状态。**仍成立的**：B 档**客户端接线**缺口（`searchByImage`/`enqueueDeleteOp`/`enqueueFieldOp` 零调用＝轴 4 机器实证）、A6 O-1 条件复现（需实跑）、a-fields 小字段三项、RAG 门禁实测 FAIL |
| 7.10 | **待办文档陈旧：所列"待办"实为已完成（J-1~J-6 六项）——且我据此把排期写错** | 🟡 已更正 | 触发：用户要求"一路继续"，我按纪律**先验码再动手**，结果 `docs/parallel-dev/_B5a_B5d修正后待办.md`（写于 08-26）的 J-1~J-6 **全部已在代码中实现**：J-1 音频事件消费（`asr/emotion.py` + `transcriber.py:249-254`）· J-2 SNR 降权（`audio.py:estimate_snr` + `emotion.py:_noise_weight`，测试 `test_asr.py:676-713`）· J-3 段级合并 dominant/peak（`asr/emotion.py:merge_segment_emotion`，测试 `:716-756`）· J-4 录音上限实为 **30 分钟**（`yishu-recorder/.../index.uts:44`，非原述 3.5min）· J-5 `event.emotion` 落库（`pipeline_ext/emotion.py:74/93`）· J-6 `notify.py` **有真实调用方**（`emotion.py:112/119`、`echo.py:306`） | **危害有实证**：我在 09-23 排期里把 J-2/J-3 写成"待做"（A2/A3）——若不是先验码就会去"实现"已存在的东西（重演图搜式浪费）；`docs/README.md` 亦把该文档标为"🔴 活" | 🟡 已更正四处（待办文档逐项加证据 + 头部复核注 + README 状态 + 排期 A2/A3 销项）。**未作销项的部分**：J-7（录音中断状态机 POC）与 J-6 的"分层触发条件/文案库"**未验，仍按待办看待** |

**疑似待确认（未定性，不作缺陷登记）**：微信端实网未验收（`api/wechat.py:34-40` 未配 503）／「忽略待确认事件」无后端（`event_ops.uts:179-180` 纯本地）／reranker 模型文件是否随仓库分发（`services/rerank.py:57-61`）／24 张表实际使用率未量化。

**harness 清理**：分类、不可删项与执行前置见审计报告 §7 附录（**含两条与"全删"冲突的硬证据**：`docs/_l1_*` 是在途 pipeline 的工作目录；`docs/parallel-dev*` 被 10+ 处 harness 文档引用）。

---

## §4.14 易扩展/易维护重构波 · Phase A 深审 + Phase B 第二轮（2026-09-24）

| # | 事项 | 级别 | 内容 | 结论/证据 | 状态 |
|---|---|---|---|---|---|
| 14.1 | **Phase A 逐功能域深度审计（14 域）以"抗断连"策略重做** | 🟠 | 上一轮 7 个并行 subagent 中途全部挂掉、且只做了行数/grep 计数（**浅层**）。本轮改为**串行小批量**（每批 2 域、单域一 agent、独立报告文件、失败即续跑） | 产出 `audit/domain-01..14-*.md`（14 份）+ 汇总 `audit/_PHASEA_ROLLUP.md`：**249 条 = 结构类 185 + 功能与安全缺陷 64**（P0 17 / P1 111 / P2 121；候审 30）；结论并入 `refactor-ledger.md` **§9**（覆盖旧浅层条目） | ✅ 完成 |
| 14.2 | **旧台账 4 处结论被深审推翻（须以 §9 为准）** | 🔴 | ① 软删手抄 `44/21` 与 `49` 属**口径差**（44=backend/app 生产口径，49=含 tests/scripts 5 处），非矛盾；② **L-11 的"守卫 `test_agent_schema_alignment.py`"是幻影**——全仓 + git 全历史 0 命中；③ **B3「验证即达标、无缺口」证伪**——`test_authz_gate.py:47` 登记名 `load_owned_capsule` ≠ 实际符号 `_load_owned_capsule` ⇒ **门禁哑条目**；④ B2′ **B2 子包化后 `glob("*.py")` 漏扫子包** ⇒ contents 全部端点静默退出扫描面 | 教训登记 `docs/lessons.md`（幻影守卫 + 门禁哑条目两条）：**"已有 X 守卫"必须验证文件真实存在；门禁全绿 ≠ 真覆盖** | ✅ 已更正 |
| 14.3 | **B2 `api/contents.py` → `api/contents/` 子包拆分** | 🟠 | `git mv` 保历史；移出 serializers(48)/profile_sensitive(89)/favorites(111)/trash(115)/waveform(115) 五子模块 | `__init__.py` **902 → 524 行**（<600，轴 5 棘轮销项 7→6）；AST 逐语句比对 **31/31 顶层语句一致**；`MAX_PHOTO_BYTES`/`enqueue_unique` 与 6 核心端点留在 `__init__.py`（monkeypatch 面）；全量 pytest **850 passed / 4 skipped**；`openapi` 67/67 对齐 | ✅ 完成 |
| 14.4 | **B3′ 归属 helper 收敛 + 门禁哑条目修正** | 🟠 | `deps.load_owned_entity` 泛化；`_load_owned_capsule`/`load_owned_message`/`load_alive_content` 三处委托（**错误码/文案/HTTP 逐字保留**）；登记名改真名、删 3 幻影预留项、新增 `test_ownership_loaders_all_exist` | `pytest test_authz_gate+contents+content_upload+echo` → **56 passed** | ✅ 完成 |
| 14.5 | **B10 门禁加固：消除"恒绿/静默"门禁面** | 🔴 | ① `review_agent` 新增 **`audit_axes`**（轴 1–4 此前**零自动门禁**，D14-1/P0）；② CI full-gate 新增**阻断式** `audit_harness all`；③ 重复模式改**总数 + per_file 双 gate**（D14-3）；④ 镜像豁免收窄为「docstring 含"镜像" ∧ 类自身有 `__tablename__`」（D14-5）；⑤ 体积阈值改由基线驱动（D14-2 死配置） | 负向探针：临时零调用导出 → `exports` **CRITICAL** + `review_agent` **退出码 1**（已还原） | ✅ 完成 |
| 14.6 | **D13-1：CI「client tsc 试点」是恒绿假门禁** | 🔴 | 核实：该步骤 `continue-on-error: true` **且脚本末尾无条件 `exit 0`**，include 为 `client/utils/*.ts` 而 5.24 迁移后全为 `.uts`（tsc 不认 `.uts`），且 `client/tsconfig*.json` **根本不存在** ⇒ 匹配 0 文件、永远绿 | 退役该步骤，替换为**阻断式** `audit_harness client`（真覆盖：`USE_MOCK_*`/死路由/`.ts` 残留）；部署 README 补齐 3 脚本；硬编码路径清 4 处（env + 显式默认 + 缺失报错） | ✅ 完成 |
| 14.7 | **D13-3：`check_env_template.py` 目前就跑不过** | 🟡 | 实跑取证：`AGENT_SERVICE_BASE_URL/TIMEOUT_S/TOKEN` **config 有、模板缺** | 按"先补模板再挂门禁"纪律：**本批只登记不接线**（不接一个当下即红的门禁，避免造假绿/假红） | 🟠 待处置 |
| 14.8 | **B9 端口/边界收口** | 🟡→✅ | L-03c（api/rag 越级直连 external/算法包）、D05-16（`correction.py` 自建第二套 Qdrant）、D02-3（存储后端注册点含 api 层硬编码 COS） | **已完成（三子批）**：B9a `44a837c`（`l3_lifecycle` 经 events 域门面再导出 ⇒ API 不再直连算法包，23 passed）；B9b `32b4a89`（rag 的 LLM 调用全走 `llm_ops`，新增 `image_caption` 转发，79 passed）；B9c `8286327`（`BackendSpec` 注册项 ⇒ 新增后端**只改 1 行**；`build_cos_raw_client()` 唯一构造点；`cos_sts_configured()` 归位存储层 ⇒ API 不再直读 COS 私有字段；探针 8 项 + 83 passed）。**D05-16 改判为已收敛**：客户端早已共享 `get_qdrant_client()`（P2-04），残余"裸调用未走门面"会**变更 collection schema** ⇒ 登记不改 | ✅ 完成 |
| 14.11 | **备份超期（RPO 违规）处置** | 🔴→✅ | `audit_security` 首次跑通后报"最近 dump 距今 493h（≈20.5 天）"——此前该检查因工具崩溃**从未被评估** | 按用户指示处置：跑 `backup_pg.ps1` 生成新 dump（2.4 MB）→ **`pg_restore --list` 校验可解析（388 TOC 条目，archive created 2026-09-24 10:17）** → 删除全部过期 dump（>24h）→ `audit_security` **阻断项 0**（"最近备份新鲜度 0.0h"）。教训登记（"按文件名排序取最新会取错对象"） | ✅ 完成 |
| 14.12 | **客户端编译门恢复可用** | 🟢 | 此前多轮判定"编译门不可用"（HBuilderX GUI 需人工启动；UTS 编译器崩溃日志实为**编译期**产物） | 用户手动启动 HBuilderX 后：**基线冷编译成功**（`项目 client 编译成功。ready in 107925ms`）⇒ B5 解锁 | ✅ 已恢复 |
| 14.9 | **B11 残余声明漂移 & B10-b 门禁缺口补强未执行** | 🟡 | B11 残余：D02-7/9、D04-3、D05-9/15、D07-13/14、D09-10、D11-5、D12-7（多数需**决策**或属**死配置删除**）；B10-b：D04-15（双跑夹具未覆盖 `approx`/`corrected` → **端云同源门禁无鉴别力**）、D11-2（错误码 AST 门禁跨模块盲区）、D12-9（轴 2 三向缺一向） | 已入 `refactor-ledger.md` §9.7 待办表 | ⏸ 待执行 |
| 14.10 | **在途文件名单已过期** | 🟢 | AGENTS「第四窗媒体票据 8 文件在途＝勿碰」——深审 14 域一致记录**在途 = 0**，该批**均已入库** | 勿碰名单**解除**（已回填 ledger §1） | ✅ 已更正 |

## §4.15 易扩展/易维护重构波 · L-02 销项 + GAP-2 轴 7（2026-09-25）

| # | 事项 | 级别 | 内容 | 结论/证据 | 状态 |
|---|---|---|---|---|---|
| 15.1 | **L-02 销项：`TabIndex` 抽两块 composable** | 🟠 | 承「方案 A：整块 view-model 进 composable」——**L-02a** 照片路径/挂载/详情（含 `photoUrlById`/`localPhotoPathMap` 两张非响应式索引表 + 7 方法）→ `usePhotoAttach`；**L-02b** 卡片操作/拆分（四态 + 13 方法）→ `useEventOps` | `TabIndex.uvue` **1046 → 738（−308）**；两子步各冷编译 ✅（48.7s / 36.8s）；轴 5/6/7 均无 CRITICAL；**axis-5 条目删除 ⇒ L-02 销项**（存量超阈 **4 → 3**） | ✅ 完成 |
| 15.2 | 🚨 **GAP-2：类成员是第二道编译假绿面** | 🔴 | 复核 f333430 发现 `useVoiceRecord.uts` **方法体写 `this.saving.value` 却漏声明 `saving` 字段**——HBuilderX 编译报绿（GAP-1：编译不覆盖符号解析）**且轴 6 抓不到**（它只查跨模块 import）；真机 `submitVoice` 必 `undefined.value` 崩溃 | 修复＝补 `saving = ref(false)`（恢复迁移前并发保存守门） | ✅ 修复 |
| 15.3 | **新增门禁轴 7（`class_members`）** | 🔴 | 口径变化：客户端静态门禁从「跨模块符号（轴 6）」扩到「**类成员**」两个面——① `this.<名>` 不在本类；② `const x = new Cls(…)` 后 `x.<名>` 不在 `Cls`。**误报防线**：`extends` 类整体跳过；同名变量非「**全部**赋值皆 `new Cls(`」则跳过（实测挡下 `uploader_batch.uts` 的 `held` 遮蔽） | `scripts/audit_class_members.py` 接入 `audit_harness` 轴 `class_members`（含 `all`）+ `review_agent.check_audit_axes`（快/全量均跑）；全仓 **76 类 0 误报**、3 个 extends 跳过；**反向探针两例**（删字段 → 报 `this.saving`；`pa.attachAll`→`pa.attachAllZZZ` → 报该项）**均 EXIT=1 且字节级还原**；两条教训已登记 | ✅ 完成 |
| 15.4 | **TabIndex 悬空面板（拆分选片面板 + 照片反向入口浮层）** | 🟡 | git 取证：08-24 `24b205f` **实现且在模板渲染**（split 全链路，模板内 20 处）；08-26 `a42d51d` 增照片反向入口浮层（8 处）；**08-29 `cd41f18`「像素级UI还原」重写该页（817 行）时删除模板节点**（未迁移、未新建子组件）；此后 `7b831fc`（组件化）/`e2e3c26`（删旧页）**照搬**该状态；script 逻辑一路悬空，直到 2026-09-24 被我在 L-02 中搬进 composable（**只动 `<script>`、未碰模板**） | **2026-09-25 用户裁决：不恢复** —— 该删除与 `uvue_gen/index_canvas.json`（**亦无此两帧**）口径一致，属**有意为之**；悬空逻辑**无害**，不再列为待办（孤儿样式 `.split-panel` 随 `tab-index.css` 保留） | ✅ 已裁决（不修） |
| 15.5 | **L-18/L-19/L-20 样式外置 ⇒ 轴 5 棘轮清零** | 🟠 | 三个残留超阈客户端页（`TabAi` 974 / `favorites` 957 / `manage` 891）按 B5d/B5.2e 已证机制做整块 `<style>` 纯移动（**零 script/运行时改动**） | 974→**469** / 957→**500** / 891→**466**；等价性＝归一化后与 `HEAD` 字节全等；冷编译 ✅ + 产物 `style.bytes` 类名取证；**baseline allowlist 清空 ⇒ 存量超阈 0** | ✅ 完成 |

## §4.16 波浪调度与令牌波开波（2026-09-25 用户拍板）

| # | 事项 | 级别 | 内容 | 结论/证据 | 状态 |
|---|---|---|---|---|---|
| 16.1 | **「客户端 ↔ Agent 接线」归属波次** | 🔴 | 待办＝翻转 `client/components/TabAi/TabAi.uvue:216` 的 `USE_MOCK_CHAT=true` + 真端点联调；后端链（`api/chat.py:113` → `services/external/agent.py`）**已通、非 mock** | **用户拍板：归「功能修复波」**（与 64 条运行时缺陷同域、同验收口径；不并入令牌波，避免污染"纯样式"边界） | ✅ 已定 |
| 16.2 | **令牌波首步方式** | 🟠 | 令牌收敛规模＝7400 行样式 / ~962 颜色位点，且强依赖平台机制（变量作用域未验证） | **用户拍板：方案文档先行** ⇒ 产出 `.trae/specs/design-token-convergence-wave/spec.md`（输入分布实测 + 平台能力 + A/B/C 对比 + P1–P5 分期 + **6 项待拍板**）；**未拍板前不动令牌/样式代码** | ✅ 已产出（待拍板） |
| 16.3 | **波次顺序** | 🟠 | — | **用户拍板：先令牌波，再功能修复波**（Agent 接线随功能修复波） | ✅ 已定 |
| 16.4 | **令牌波关键调研结论（供拍板参考）** | 🟠 | ① 官方：App ucss **支持 `var()`**，官方示范的声明位是 **`app.uvue` 里的 class**（`:root`/`page` 不可用，本仓 0 处尝试）；② 本仓实锤 **App 端样式不继承**（`App.uvue:95-113` "勿改回"注释）⇒ **"变量作用域 / 跨文件可见性"未验证（V1/V2）**，是方案成败点；③ 前沿：**W3C DTCG Format Module 2025.10 已成首个稳定版**（`$value`/`$type` + 路径别名），但本仓 `design_tokens.json` **非** DTCG 形态；④ `theme.json` **只管 pages.json/tabbar，不管页面 css** | 详见 spec.md §2/§3/§7；**6 项待拍板**＝DTCG 与否 / 探针优先与否 / 基准 750rpx vs 390px / 分期 P1–P5 / dark mode / P3 目视挂账 | ⏸ 待拍板 |

| 16.5 | **令牌波 V1/V2 能力探针已实测** | 🟠 | 按推荐先跑探针（用户跳过拍板 ⇒ 默认执行推荐项）：`App.uvue` 全局 class 声明变量 + 页面/外置 css 使用 ⇒ 一次冷编译 + 扫全部 `*.style.bytes` + **字节级还原**（探针文件与备份全等、`git status` 干净） | **结论**：① ✅ 编译接受 `var()`；② ✅ 声明进 App 级产物（`GenAppSharedData.style.bytes` 含 `#123456`）；③ ✅ 使用侧**原样透传**（不内联）；④ ✅ 外置 css 跨文件同样透传；⑤ ❓ **运行期取值仍无设备不可判** ⇒ **方案 A 具备可行性依据，像素级验收=设备阻塞项（挂账）**。V3（`@media`）未跑（dark 不在本波）。详见 spec.md §2.2 | ✅ 已跑 |

| 16.6 | **令牌波尺寸面实测更正（推翻上一版方案的前提）** | 🟠 | 上一版 spec 曾判"`design_tokens.json` 是画布 px、代码 rpx 是整齐数、二者不同源、换算规则未知"——**实测推翻**：`rpx = 画布px × 750/390(1.9231)` 后**保留 1 位小数**（与 `about.uvue` 注释 `SCALE=750/390=1.9231` 一致），**72.0% 尺寸位点（1988 处/130 种）精确落在画布整数格**（23.1=12px、38.5=20px、26.9=14px…）；**28.0%（774 处/98 种）是 rpx 原生整数**（24/20/16/32/40rpx …）⇒ **"两套基准并存"** | 尺寸面取舍进 §7 第 3 项（保留两族[推荐] / 统一到画布格[需目视] / 只给高频建语义令牌）；**尺寸面不擅自跨基归一**（归一会真的移动像素） | ✅ 已更正 |

| 16.7 | **令牌波 P1a 完成：颜色/渐变/效果/字体 DTCG 令牌集** | 🟠 | 按推荐默认推进（用户跳过拍板）：新增 `client/design_tokens.dtcg.json`（DTCG 形态 **143 叶子 / 14KB**）= 唯一令牌源；既有 `design_tokens.json`（画布期、0 引用）保留为历史输入 | **覆盖度审计（回源比对）**：hex **760/762 = 99.7%**（残余 `#f3e8dc`/`#f6ebe7` 各 1 次，按"散单留字面量"待 P3）、`rgb()/rgba()` **190/190 = 100%**、渐变 **10/10 = 100%**；18 个既有 color token 名**全部保留为别名引用**（语义与色阶解耦，P3 迁移调用方无需改名）。**两处自我纠错已留证**（渐变曾用推测值 → 改为实测生成 + 色多集回源校验） | ✅ P1a 完成 |

| 16.8 | **令牌波 P4 门禁建成 + 口径同源化** | 🟠 | ① 新增 `scripts/audit_token_literals.py`：**硬编码色字面量棘轮**（hex/rgb/渐变；复用轴 6 `strip_comments` ⇒ 代码口径），基线写入既有 `audit_harness_baseline.json` 的 `token_literals` 键（35 文件；`hex 638 / rgb 171 / gradient 10`）；棘轮＝总数或单文件任一上升即 CRITICAL、下降即 WARN、per_file 归零即 CRITICAL；接入 `audit_harness` 轴 `token_literals` + `review_agent`（快/全量）。② **口径同源化**：此前 spec 的 762/190 含**注释里的色值提及**，现全文与门禁统一为**代码口径**（hex 638/50 种、rgb 171/64 种、渐变 10/10；尺寸 2693 处/226 种） | 反向探针：临时加 `#123abc`+`rgba(1,2,3,0.4)` ⇒ 独立脚本与轴**均 EXIT=1**（报 638→639 / 171→172 + 单文件上升，共 4 条 CRITICAL）⇒ 字节级还原；`audit_harness all` **无 CRITICAL**（INFO 19）；覆盖率改用代码口径后结论不变（hex 636/638=99.7%、rgb 171/171=100%、渐变 10/10、尺寸 2570/2693=95.4%） | ✅ P4 完成 |

| 16.9 | **令牌波 P2 完成：单页试点（`about.uvue`）机制端到端打通** | 🟠 | 采用 `var(--令牌, 原字面量)` **带回退**形式 ⇒ 运行期变量解析与否**视觉都零变化**（无设备也能安全试点）：`App.uvue` 新增 `.tk-root` 声明块（6 令牌，**取值由脚本从 DTCG 源解析生成**）；`about.uvue` 11 处字面量改为带令牌回退；门禁补语义「**回退值不算硬编码**」（`_strip_var_fallbacks`，括号配平） | ① 冷编译 ✅（24.7s，**ucss 接受 `var(令牌, 回退值)`**）；② **产物取证**：`GenAppSharedData.style.bytes` 125→341B 含 `tk-root` 与 `--c-text-primary`→`#3a2e25`；`GenPagesAboutAboutSharedData.style.bytes` 含 `var(--c-text-primary, #3A2E25)`；③ 棘轮记账：`about.uvue` hex 11→0、总数 638→**633** ⇒ 已下调基线；④ `audit_harness all` 无 CRITICAL。**仍待设备**：运行期真解析与否。**P3 设计点登记**：声明块须生成器产出 + 门禁需"生成区"概念（两方案待定） | ✅ P2 完成 |

| 16.10 | **令牌波 P3 执行器就绪 + 全仓改写计划（dry-run）** | 🟠 | 新增 `scripts/token_codemod.py`（**默认 `--plan` 不改文件**；改写形式 `var(--令牌, 原字面量)` 带回退）；命名 deterministic（不猜角色，语义精修留后） | **计划**：可改写 **3402 处**（hex 634 / rgb 179 / 尺寸 2579 / **渐变 10/10**）、**37 文件**、归并 72 条；**残余 213 处 / 85 种**分三类（尺寸同格变体／物理 px／无令牌散单）并各有处置。**映射器修掉三坑**：① 梯度正则在内层括号截断（1/10→10/10）；② 颜色未归一（8 位 hex、α=0/1、`0.10`/`0.078` 写法）；③ 同格变体不得静默改写。分批 6 批（每批 改写→冷编译→产物取证→基线下调→独立提交） | ✅ 工具+计划就绪 |

| 16.11 | **令牌波 P3 批次① 落地 + 写器缺陷当场拦下** | 🟠 | P2 的两个设计点已实现：① 声明块由 `--write-block` 生成（**全部 325 令牌**一次声明进 `App.uvue` 生成区）；② 门禁认生成区（`_strip_generated` 必须在剔注释**之前**，否则标记先被删）。批次①：`styles/record-sheet.css` 改写 **392 处**（计划 397，5 处带符号尺寸跳过）+ `RecordSheet.uvue` 根挂 `tk-root` | ① 冷编译 ✅（38.8s）；② 产物取证：`GenAppSharedData.style.bytes` 125→**15267B** 含 325 声明与 `tk-root`；RecordSheet 产物 16941B 含 `var(--c-paper-50, #faf9f5)`；③ 棘轮：hex 633→**552**、rgb 171→**149**，基线已记账；④ `audit_harness all` 无 CRITICAL。**🛠 当场拦下会致视觉回归的缺陷**：首轮把 `-46rpx` 写成 `-var(...)`（非法 CSS、编译不报、棘轮反显示"变少"）⇒ 加 `_signed()` 跳过 + 写前自检 `[-+]var\(` 命中即拒写；教训已登记 | ✅ 批次① 完成 |

| 16.12 | **令牌波 P3 批次②③ 落地 —— 全仓样式令牌化收口** | 🟠 | 批次②：其余 5 个外置 css 改写 **1158 处**（+5 宿主挂 `tk-root`）；批次③：`--write-uvue-all` 对 **26 个 .uvue** 的 `<style>` 改写 **1947 处**（24 个自动挂根类）。命名仍 deterministic（不含语义角色） | **棘轮累计**：hex **638→67（−89%）**、`rgb()` **171→4（−98%）**、渐变 **10→0（−100%）**；② 幂等性实测（重跑 0 处）；冷编译 ✅（64.7s / 89.7s）；`audit_harness all` 全程无 CRITICAL。**🛠 批次③ 拦下"整体失效"缺陷**：写器改了自带生成区 → `--c-ink-900: var(--c-ink-900,…)` 自引用环（令牌失效、编译不报）⇒ 加 `_non_generated_ranges()` 跳过生成区 + 重生成覆盖 + 断言区内 `var(`=0。**余 19 文件**＝§4.3 分类的刻意留残。下一步：语义精修 + P5（动态 `:style=`/`theme.json`） | ✅ 批次②③ 完成 |

| 16.13 | **功能修复波开波 + 客户端↔Agent 接线完成（本波第 1 项）** | 🔴 | 按用户指令「将客户端与 Agent 接线纳入下一个波中，**完成接线**」：新开功能修复波（入口/登记簿 `.trae/specs/functional-fix-wave/spec.md`，缺陷清单单一来源仍为 ledger §9.2，不复制）。**接线补完两处真实缺口**：① `TabAi.USE_MOCK_CHAT` **翻转为 false**（此前产品实际跑在"样张对话"占位态）+ 新增**门禁锁**（翻回即 CRITICAL）；② **历史从不回填**——后端端点 docstring 要求"客户端进入会话时回填"而客户端从未调用、导致重启后会话接续但界面空白 ⇒ 新增 `chat_api.fetchChatHistory` + `TabAi.onMounted` 回填（失败/空保持空列表，**不伪造历史**） | ① 冷编译 ✅（59.7s）；② `pytest backend/tests/test_chat.py` → **7 passed**（含历史端点、agent 不可用不伪造、超时 504、IDOR 404）；③ `audit_harness all` 无 CRITICAL，新导出 `fetchChatHistory` 有 2 处引用（非零调用）；④ **门禁锁反向探针**：翻回 true → `audit_harness client` **EXIT=1** 且点名 → 字节级还原。**⚠️ 未验证项（如实标注）**：`agent/` 依赖 `dbus-python`/`PyGObject`/`pycairo` 为 **Linux-only** ⇒ Windows 起不来服务（8300 拒绝、`agent.health()` reachable=false）⇒ **后端↔agent 真实外呼未在本机验证**，需 Linux devbox/部署环境或真机 | ✅ 接线完成（运行时待环境） |

| 16.14 | **功能修复波 A 段簇① · D04-1 已修（P0 · 云侧 L1 日界）** | 🔴 | **根因比审计原文更深**：不是单纯"端云未对齐"，而是**云侧自相矛盾** —— `echo._local_now()` 用 `datetime.now().astimezone()`（服务器本地）而聚合 L1 默认 `tz=0`（UTC）；且 `deploy/` **未设 TZ** ⇒ 容器 UTC ⇒ 连前者也等于 UTC。**修**：新增 `app/core/timeutil.py` 为云侧"本地日"**单一来源**（`APP_LOCAL_TZ`，默认 `Asia/Shanghai`，**不读容器 TZ**）；`l1_daily_aggregate(tz_offset_minutes: int \| None = None)` 默认改走本地口径（显式 `0` 仍是 UTC）⇒ 两个调用点**零改动**自动修正；`echo._local_now()` 改为委托 ⇒ 云侧日界只剩一源；config + `env_template` 模板项同步 | ① `pytest test_agg_reference + test_aggregation` → **18 passed**（含两条新锁：默认口径落本地自然日 / echo 与聚合偏移必须相等）；② **反向探针**：把默认改回 `0` ⇒ 新用例失败且报错正是 D04-1 症状（`assert '2026-07-17' == '2026-07-18'`）⇒ 用例非空转，字节级还原；③ ruff 全过。**未覆盖（登记 §5.10 · 10.5）**：跨时区用户需 per-user 偏移（云侧无处存，未擅自加字段） | ✅ D04-1 已修 |

| 16.15 | **功能修复波 A 段簇① · D04-2 已修（P0）＋ AGG-016 门禁从"端侧单跑"变真双跑** | 🔴 | **三层根因**：① 云侧 `RawPhoto` **缺 phash 字段**（端侧早有）；② 云侧 `preprocess` **缺去重步**（端侧首步即去重）；③ **真身**——`gen_agg_fixtures.py` **只产端侧 UTS 夹具**、**云侧无任何夹具消费方** ⇒ "端云双跑/12 项零漂移"实为端侧单跑，叠加 `run_validation` 常量自比恒真断言（D04-14）形成"已校验"错觉。**修**：契约补 `phash`；`_dedup` 置于云侧 `preprocess` 首步（与端侧逐字同语义）；`_to_raw_photo` 补 `phash ← perceptual_hash`（否则按唯一 id 空转）；**新增 `backend/tests/test_agg_fixtures_parity.py`** 让云侧跑同一份夹具（比对语义与 `agg_check.uts` 对齐） | ① `pytest`（parity+reference+aggregation）→ **33 passed**（13 用例在云侧逐条对上）+ 反向保护断言；② **反向探针**：撤掉云侧去重 ⇒ `[dedup-phash-duplicate] 端云分叉` **失败** ⇒ 门禁有鉴别力，字节级还原；③ `pytest -k "agg or event"` → **137 passed**，另 4 项 `TestS3Aggregation` 为 **Redis 未起（Docker 守护进程未运行）＝环境性**。**如实标注**：`uq_contents_user_hash` 已使库内同哈希不能并存 ⇒ 生产收益主要是契约/防线对齐，但门禁真双跑是实打实的。余：D04-14（恒真断言）、D04-15（approx/corrected 覆盖） | ✅ D04-2 已修 |

| 16.16 | **功能修复波 A 段簇① · D04-14 已修（P1 · AGG-016「假校验」）+ 两处连带修复** | 🔴 | **比审计更糟**：AGG-016 有**两条**假校验 —— ① `AGG_CONFIG["l0"]["eps_s_m"] == 500.0`（常量自比恒真）；② `aggregate(photos)` 与**自己**对比（确定性 ⇒ 恒真）却命名为"端云同参双跑"，**端侧根本没参与**。**修**：新增 `end_constants.py` 读**端侧真源** `client/utils/agg/agg_config.uts`（含 `6000.0/3600.0` 表达式解析、不用 eval）与云侧 `agg_types` 逐项比对、缺项检测；②改名其真实语义「聚合幂等」；①换真比对 + **防空转**断言；新增 CI 硬门禁 `test_cloud_and_end_constants_match_via_source`。**连带**：数据生成器锚点由 UTC 正午→**本地正午**（修 D04-1 日界后场景3「一日游」跨本地午夜的**数据摆放**问题）；场景9 补 `phash` 让去重真跑 | ① 端侧解析 **7 项**全对、比对 **`不一致 = []`**（端云当前逐项一致，**首次真实结论**）；② `run_validation` → **18 项全绿**（含三条 AGG-016 真通过）；③ **反向探针**：云侧 500→600 ⇒ **精确点名** `L0_EPS_S_M(600.0) ≠ end.L0_EPS_S_M(500.0)` 失败 ⇒ 有鉴别力，字节级还原；④ `pytest`（generator/reference/parity/aggregation/cli）→ **55 passed**。**🛠 修门禁时被自己的"防空转"断言抓住**：正则吞掉端侧行尾 `//` ⇒ 解析 0 项、门禁空转报绿；已修。余：D04-15（approx/corrected 用例） | ✅ D04-14 已修 |

| 16.17 | **功能修复波 A 段簇① 收口 · D04-15 已修（P0）＋ 自己门禁的"自证"缺陷被探针抓到** | 🔴 | ① 夹具 13 例只间接触达"超上限且无众数"，`approx`/`corrected` **零覆盖** ⇒ 补 3 用例，全部按「**关掉分支 ⇒ 结果就变**」设计（corrected⇒1簇6张 vs 5张+散片；approx⇒2簇 vs 1簇8张；degraded⇒1簇3张 vs 0簇）。② **又抓到我自己门禁的自证缺陷**：第一版云侧门禁用 `build_cases()` 现场算期望，而生成器期望正是**调用云侧实现**算出 ⇒ 关掉分支时两侧一起变、三个探针**全绿**却什么都没抓到 ⇒ 生成器改为一并落盘**冻结快照**（`backend/tests/data/agg_fixtures.json`，输入+期望都冻结），门禁跑冻结输入/比冻结期望 | ① 夹具 **17 用例/95 照片/19 簇/24 日卡**；`pytest`（parity+reference+aggregation+generator+cli）→ **59 passed**；云侧冻结门禁 **19 passed**；端侧 UTS 夹具重生成后**冷编译 ✅**；② **四个分支探针全部真红**：corrected/approx/degraded/dedup（关掉即失败、字节级还原一致）；③ 新增两条反向保护（快照新鲜度 + 三态/重复哈希用例存在性）。**踩坑留证三条**：金标准不得与被测实现同源（自证）；用例须证"不同走法⇒不同结果"（第一版 degraded 用例无鉴别力，探针实测未抓到⇒重设计）；单位坑 `DRIVE_SPEED_MS=33.3 **m/s**（120km/h）` 非 33.3km/h + 时间偏移逐张累积。**⇒ 簇①（端云聚合同源）四项 D04-1/2/14/15 全部收口** | ✅ 簇① 收口 |

| 16.18 | **功能修复波 A 段簇② 收口 · D08-1/2/3/5 已修（P0/P1 · 软删 30 天承诺 + 离线字段写）＋ 契约三问拍板** | 🔴 | **先拍板**（grill 三问）：① **单一软删写路径**；② **双向写穿**（REST 编辑也写 SFV + 变更日志）；③ D08-3 投影白名单**仅 `remark`**。**缺陷原貌**：软删是两条互不相通的轨道——REST 删只置 `contents.deleted_at` **不写 `deleted_logs`**（清理只在 `deleted_logs` 选 ⇒ **永不彻底清除**＝D08-2）；sync 删只写 SFV 墓碑+`deleted_logs` **不置 `deleted_at`**（REST 读**仍存活**、回收站**看不到**、30 天后又**突然消失**＝D08-1）；离线字段写只落 SFV **从不投影权威表**（改备注用户**永不可见**＝D08-3）；冲突分支无 `continue` ⇒ 败方旧值穿透写进变更日志（＝D08-5）。**根因**：同一业务语义在两处入口各自实现，权威表/同步账本/审计日志写入散落、无人统管；既有门禁只断言"墓碑+`deleted_logs`"把分歧**固化成预期行为**。**修**：新增 [sync_writes.py](file:///d:/GuangH-App/backend/app/services/sync_writes.py) 为**单一写入语义路径**（`finalize_soft_delete`／`project_field`／`restore_content`／`hard_delete_records`），REST `DELETE`·`PATCH` 与 `push_ops` 同调；`deleted_logs` 幂等；restore 反向清墓碑 + pending 审计日志→`restored`（否则 30 天后**误删已恢复内容**）；**D08-1 更深一层**：`push_ops` 判"实体存在"只认 SFV 行，而照片经 REST/分片链路上云**从不写 SFV 行** ⇒ 离线删被误判 `entity 不存在` 静默丢弃 ⇒ 改"**权威行存在即实体存在**" | ① 新增 `backend/tests/test_content_lifecycle_sync.py`（**13 用例**）→ **13 passed**；② 无回归：`test_sync + test_reconcile + test_cleanup_job` → **24 passed**；`test_contents + test_content_upload + test_photo_content + test_content_safety + test_data_chains_12 + test_event_sync` → **104 passed**；③ **7 条反向探针全部真红**（撤 `deleted_at` 投影／撤"权威行即存在"（复现 `entity 不存在`）／撤 `deleted_logs`（含端到端永不清理）／撤字段投影／撤白名单／冲突补写日志／撤审计日志中和），字节级复原、`grep PROBE` 无残留；④ 端点 docstring 变更 ⇒ `gen_openapi.py` 重导（240153 字符），`review_agent` ✅ + lessons 登记。**如实标注**：客户端镜像丢弃 `value`（D08-4/P1 未修）⇒「他端**自动收敛**」待 D08-4，本批只保证**云端**权威版本与 **LWW 基准**；**白名单收窄只对存在权威行的 content 生效**（无权威行沿用通用账本，避免误伤扩展面）。**新发现（未修，见 spec §6.7）**：REST **创建**不写变更日志 ⇒ 他端 pull 不到新内容（本次拍板仅"改备注/删除"） | ✅ 簇② 收口 |

## §4.17 功能修复波续执行 · 7 批（2026-09-25 会话）

| # | 事项 | 级别 | 内容 | 结论/证据 | 状态 |
|---|---|---|---|---|---|
| 17.1 | **存储键命名空间单一来源**（D02-2/D09-2 P0 + D09-14） | 🔴 | 白名单在 5 处各写一份字面量，`wechat/` 漏网 ⇒ 微信原件**票据签发成功、归属校验通过、端点恒 401**（永远下发不出来）；`.amr` 无 MIME 映射 ⇒ 语音按 `image/jpeg` 下发 | 新增 `services/storage_keys.py`（UPLOAD/SERVER/MEDIA/CONTENT 四组 + `is_media_key_allowed`/`user_scoped_prefixes`/`wechat_object_key`），5 处全改派生；新增 `tests/test_storage_keys.py` **11 例**（含**源码级反漂移断言**：残留本地字面量即红；monkeypatch 反向探针：拿掉唯一来源立即 401） | ✅ `da8b318` |
| 17.2 | **护栏 fail-safe 收口**（D10-1/2 P0 + D10-3/4/5/12） | 🔴 | ① fail-closed 分支返回**缺 `action` 键**，而调用方按 `action` 分派 ⇒ **明确判"拒发"却原文入库**；② 托管护栏 200+空响应判 `pass=True`（唯一漏网 fail-open）；③ 4 条入库链绕过策略选择器；④ 回流词只进软表（声明称"自动入规则表"）；⑤ ASR 丢弃 `masked_text`（PII 原文回传） | 新增 `verdict_action`（**pass 优先、缺键亦判 reject**）作为判定唯一解读；托管空/畸形响应改抛 RuntimeError 走 chat 兜底；4 链改走 `llm_ops.moderate`（并补齐**规则层连续性**：托管可用时此前整段跳过规则层）；`moderate_managed` 退化为薄委托；回流词按"有无 category"分**硬/软**两表；新增 `tests/test_guard_failsafe.py` **16 例** + 源码级防漂移断言 | ✅ `2c1dc05` |
| 17.3 | **检索过滤器键集单一来源 + 未知键 fail-closed**（D05-3 P0） | 🔴 | 两套独立实现、`_to_filter` 的 if/elif **链尾无 else ⇒ 未知键静默丢弃**；隔离类键（`user_id`）一旦拼错/漏同步即**跨用户召回且无任何信号** | 新增 `services/search_filters.py`（`FILTER_KEYS` 7 键 + `ISOLATION_KEYS` + `normalize_filters` 抛 `UnknownFilterKey` + `assert_full_coverage`）；两实现均先归一 + 覆盖自检；`rag._search_impl` 在降级 try **之前**校验（防被 `except` 伪装成"降级"）；PG 隔离值改以**过滤器**为准；新增 `tests/test_search_filters.py` **16 例** | ✅ `b1f2488` |
| 17.4 | **画像枚举集随镜像分发 + 启动自检**（D07-1 P0） | 🔴 | 枚举集 JSON 在**仓库根 `docs/`**，镜像只 `COPY backend/` ⇒ 容器内首次 `get_schema()` 抛 `FileNotFoundError` ⇒ 画像/访谈/标注首调 **500**（宿主开发看不出） | `Dockerfile.backend` COPY 两份 JSON 到 `/app/profile_enums` + `ENV PROFILE_ENUM_DIR`；加载器缺件报**可执行**错误；`lifespan` **fail-fast**。验证＝用**同一 COPY 指令**做真实镜像构建取证（两文件 127401/430244B 齐备，随即清理）+ `tests/test_profile_enum_distribution.py` **5 例**（含 Dockerfile 源码级防漂移） | ✅ `adfd4e4` |
| 17.5 | **微信主链收口（簇③）**（D09-1 P0 + D09-3/4/7/8/12） | 🔴 | ① 回调**不传 `user_id`**：绑定表**从未映射 ORM** ⇒ baseline 迁移把它当遗留空表 **DROP** ⇒ F6 主链（收→媒体→内容→记忆）**整条不通**，只落一行消息；② 微信「删掉」不触碰关联 Content；③ 把**回调 Token 当应用 Secret**（gettoken 必 40001）；④ async 端点里同步阻塞 30s 媒体下载；⑤ `WECOM_*` 被 `extra=ignore` 静默丢弃；⑥ 缺 MsgId 逃逸为 500（企微无限重推） | 补 `UserWechatBinding` ORM + **幂等重建迁移** `d9a0b1c2d3e4` + `services/wechat/binding.py`（resolve/bind/list/unbind，**openid 冲突默认拒换绑**防劫持）+ 回调按 `from_user` 解析 + 绑定/解绑端点（`WECHAT_004/005`）+ 独立 `wechat_corp_secret`（回退时 WARNING）+ `WECOM_*` AliasChoices + 回调 offload 线程池（自带会话）+ 缺 MsgId 改 **403**；新增 `tests/test_wechat_binding.py` **14 例** | ✅ `5f5c37c` |
| 17.6 | **一次性生成链标记 + 路径可移植**（D12-5 P0 结构） | 🟠 | `scripts_ardot2uvue.py`/`gen_design_data_v4.py` 硬编码本机路径（且 `ROOT_DIR` 指向**已不存在**的工作树）、静默跳过、手写/生成边界无标记 | 两文件补「一次性件（历史）」头注（输入为外部产物、输出为 `uvue_gen/`/`design_data_v4/` 生成物、全仓零引用）；路径改 env + 默认 + **缺输入显式报错** | ✅ `634b4b4` |
| 17.7 | **新建内容写变更日志**（D08-18 · 簇② 遗留） | 🟠 | 变更日志只在 push_ops（离线改/删）与 REST 改/删时写，**创建不写** ⇒ 他端 pull 无源（reconcile 能报差异却无 op 可重放） | 新增 `sync_writes.log_content_created`（`OP_CREATE` 单一出口）+ 4 条新建路径统一调用（POST /contents、multipart/分片照片、分片 voice、微信侧）；新增 `tests/test_sync_create_log.py` **3 例**（含"另一设备 pull 能看到本端新建"）；顺带修 `test_wechat_media` 手工 teardown（改公共 `db_user`） | ✅ `b7a6e04` |
| 17.8 | **A 段 P0 清单处置完毕（17/17）** | 🟢 | 本会话 7 批 + 前序簇①② ⇒ 17 条 P0 **全部处置** | 逐条状态与提交见 `refactor-ledger.md` §9.1 顶部状态行；**剩余＝P1/P2 + 客户端编译门项 + 凭证/实网项 + 产品决策项**（`functional-fix-wave/spec.md` §4 末「如实标注」） | ✅ 已分诊 |

### §5.11 拍板结果（2026-09-25 · 用户「全同意」＝按 §5.11 各条建议执行）

| # | 拍板 | 落地动作 | 状态 |
|---|---|---|---|
| 11.1 | ✅ **D10-8 生成态输出护栏 → 选项 B + C**（规则层全量 + 对话固定过一次 LLM 级 + 照片按比例确定性抽样） | 新增 `services/llm_ops/output_guard.py`（`rule_screen`/`llm_screen`/`screen_generated`/`sampled`）+ 四条链接线（照片描述命中即不落库、事件标题退回确定性标题、画像开放值只丢该条、对话回复不落库不返回＝新错误码 `CHAT_004`）+ 新配置 `GENERATED_GUARD_SAMPLE_RATE`（默认 0.1，含 env 模板） | ✅ 已收口（`ce71eb7`） |
| 11.2 | ✅ **D10-9 软敏感 → 选项 C + D**（按"主动 vs 被动"分治 + 软话题词表扩容） | `DetectionResult` 增 `ok` 字段 + `detect_event_sensitive_status()`；检测器没跑通 ⇒ `sensitive_status="待复核"`（echo/notify 两条**主动提及**路径天然跳过，入库/被动检索/原文零改动）；软表扩 20 个口语词；**mock 模式视为可用**（避免开发/测试库被整库标脏＝选项 B 的副作用） | ✅ 已收口（`58e2a71`） |
| 11.3 | ✅ **D07-12 → 选项 B 现在做、A 排二期**（只做"真删除"） | `services/profile_admin.clear_profile` + `DELETE /api/v1/interview/profile`（真删维度/历史/未命中答案/低置信池/证据锚点；**保留** `profile_sensitive` 保护性数据并在响应回报）+ 客户端 `clearInterviewProfile` 接线 + **弹窗文案改为与行为一致** | ✅ 已收口（`56dac34`） |
| 11.4 | ✅ **D02-9 剩余 → 以数据定**：不放开 GIF、不收紧 HEIF | 库内实测分布 `.jpg 1686 / .wav 644 / .png 437 / .heic 194 / .webp 188`（**`.gif` 0、`.heif` 0**）⇒ ① `.gif` 不可达条目从 MIME 表删除（＋测试断言"不支持则不留条目"）；② HEIF 族（`.heic`/`.heif`）受理面与 MIME 面**都保留**（194 个 `.heic` 在用＝有 iPhone 用户）；③ `.heif → image/heif` 失真已修 + "受理白名单 ⊆ MIME 表"机器断言 | ✅ 已收口（`3137132` + 本批） |

> **仍留二期（未拍板即未动）**：`D07-11`（`locked` 真实语义）需与 11.3 的选项 A（改值/确认/锁定）同批；
> `D08-4`（客户端镜像丢弃 `value`）正确修法＝本地字段值库，随 XView 自定义基座窗口。

## §5.10 待拍板（2026-09-24 新增，承 §5.9）

| # | 待拍板事项 | 背景 | 建议 |
|---|---|---|---|
| 10.1 | **64 条功能与安全缺陷的归属波次** | Phase A 深审另识别 64 条**运行时正确性/安全**缺陷（本波只登记不修）——P0 10 条含：端云 L1 时区/预处理去重分叉（D04-1/2）、软删 30 天双轨与承诺未落地（D08-1/2/3）、微信回调未传 `user_id` 主链不通（D09-1/2）、护栏 fail-open 原文入库（D10-1/2）、镜像无 `docs/` 致画像首调 500（D07-1） | 建议**另开"功能修复波"**（勿并入结构重构波，以免破坏"行为等价"边界）；三个必修簇见 `refactor-ledger.md` §9.2 |
| 10.2 | **B9 是否与 B5 一起压在编译门可用后** | B9 改依赖方向、B5 需编译门；两者都属"改动面大、验证成本高" | 建议 B9 先行（后端可在本机全量验证），B5 待编译门 |
| 10.3 | `backend/sql/schema.sql` 与 ORM/alembic 谁是权威 | 承 §5.9；B4 实跑 `alembic check` 漂移含**破坏性** `remove_fk`/`remove_index` | 建议：**ORM 为权威**，`schema.sql` 降级为"CI 建库快照 + 漂移检测输入" |
| 10.5 | **跨时区用户的云侧日界（per-user tz 偏移）** | D04-1 修复后云侧日界＝`APP_LOCAL_TZ`（默认 Asia/Shanghai，**单城口径**）。但端侧 `aggregateToEvents(uploads, tzOffsetMin)` 传的是**设备偏移**，云侧**无处可存**该偏移 ⇒ 用户出境/异地时端云日界仍会分叉。（本次未擅自加字段/改契约） | 建议二期：`users.tz_offset_minutes`（或随 `POST /events/sync` 上报）+ 云侧优先用它、缺省回落 `APP_LOCAL_TZ`。MVP 单城可不动 |

### §5.10 拍板结果（2026-09-24 · 用户授权"其余均按你的推荐来"）

| # | 拍板 | 落地动作 / 后续 |
|---|---|---|
| 10.1 | ✅ **另开「功能修复波」承接 64 条运行时正确性/安全缺陷**（不并入本结构重构波）——**2026-09-24 用户确认「另开功能波」** | 保持本波「严格行为等价」边界不被破坏。**建议再分流**：P0 中"独立可修、低回归面"的（镜像缺 `docs/` 致画像首调 500 · D07-1；护栏 fail-open 原文入库 · D10-1/2）可先做**P0 小批**；端云分叉（D04-1/2）、软删双轨（D08-1/2/3）、微信主链（D09-1/2）需各自专用窗口（改行为 + 需真机/凭证）。清单见 `refactor-ledger.md` §9.2/§9.7 |
| 10.2 | ✅ **B9 先行**（后端本机可全量验证）→ **已于本轮完成**；B5 依赖编译门 → **编译门本轮已恢复可用，B5 正在执行** | B9 三子批 `44a837c`/`32b4a89`/`8286327` 全部完成并验证；B5 采用"纯移动 + 编译门 0 error"逐子批推进（B5a `play.uts` 已完成） |
| 10.3 | ✅ **ORM/alembic 为权威**；`backend/sql/schema.sql` 降级为"CI 建库快照 + 漂移检测输入" | ① `schema.sql` 头部加注"由迁移生成/对齐，勿手工改"；② CI 建库后加 `alembic check`（或"已知漂移白名单"）以防再漂移；③ **补破坏性漂移迁移需专用窗口**（`remove_fk`/`remove_index` 会自动删约束），本波**不执行**（保持行为等价），登记为待办 |
| 10.4 | ✅ **L-12 设计令牌收敛 → 另立「令牌波」**（本结构波**不做**）——2026-09-24 用户问「令牌收敛很复杂吗？复杂的话另立」⇒ 判定**复杂**，故另立 | 判为复杂的四点依据：① **规模大**——`.uvue` style 合计 **7391 行 / 32 文件**、**763 处色字面量**、660 处靠人工抄写巧合相等；② **令牌值本身已漂移**（26/56 去重值缺失）⇒ 须先**定标**再谈收敛，否则是"把错误集中化"；③ **App 端样式不继承**（ucss 子集，父子属性隔离）⇒ 无法用 `:root`+继承，必须**构建期注入/生成**机制；且本机 HBuilderX **无 scss 编译插件**，`<style lang="scss">` 不可用 ⇒ 机制选型本身是个设计决策；④ **验收必须目视/真机**（当前无设备）⇒ 无法在"行为等价 + 本地可验"的本波边界内闭环。**令牌波范围**：定标（补齐/校正 token 值）→ 机制选型（构建期注入 or CSS 变量 or 生成器）→ codemod（字面量→令牌）→ 逐页目视回归。承 `refactor-ledger.md` L-12/D12-1/2/3、D12-5（生成器硬编码路径） |

## §5.11 待拍板 · 产品决策项（2026-09-25 功能修复波遗留 · **拍板前不动代码**）

> **性质**：这四项的共同特征是「**代码侧无对错，取决于产品/合规取舍**」——任选一支都能实现，
> 但选错方向会白做或引入新的成本/风险。故本波**只登记不猜**（与 D10-1 那类"代码就是错的"缺陷区分开）。
> **拍板后**：把结论回填本表 → 在 `.trae/specs/functional-fix-wave/spec.md` §4 建批次 → 附验证证据。

| # | 决策项 | 现状（代码为凭） | 选项 | 建议与理由 |
|---|---|---|---|---|
| 11.1 | **生成态 LLM 输出是否过护栏**（`D10-8`，P1） | 用户可见的 **4 条生成链**当前**全不过**护栏：`ai_tagging.generate_photo_description`（详情页 AI 描述）、`llm_ops/event_merge`（事件名/描述 → 时间轴卡片）、`llm_ops/annotate`（画像**开放新值** ≤8 字 → 画像页）、`api/chat`（Agent 回复 → 对话页）。护栏只作用于**用户原文**（`contents`/`asr`/`wechat`/`echo`）。注：本波已把 4 条**入库**链接到策略入口（D10-3），故"输出侧再查一次"＝每次生成多一次 LLM 调用 | **A** 全部同步过（最稳/最贵）·**B** 只过**规则层**（本地词表+正则，零成本，挡明显违规）·**C** 规则层同步 + **LLM 抽样/异步复核**（成本可控，需"事后隐藏"链路）·**D** 不动（内测 100 人） | 建议 **B + C**：① 4 条链**同步走规则层**（成本≈0，且与"用户原文已过规则层"口径一致）；② `api/chat` 因**直接对用户可见**再加一次托管护栏（单次对话可接受）；③ 照片 `ai_description` 走**异步抽样**（量大）。**D 不建议**：UGC/AI 生成内容审核是上架硬要求，且"生成态"是唯一完全无防线面。**成本量级**（B 之外若加 LLM）：100 用户 × 30 张/天 ≈ 3000 次/天额外调用 |
| 11.2 | **软敏感 fail-open 的处置**（`D10-9`，P1 · 候审） | `llm_ops/guard.py`：托管检测器 `available()` 为假 → `pass_=True`；`except` → 记 warning + **降级放行**；`detect_event_sensitive` 失败返回 `[]`。消费方 `pipeline_ext/sensitive.py` ⇒ 不写 `sensitive_tags`、`sensitive_status` 保持"正常"。而 `echo._is_sensitive` 的"便宜检查"依赖该标记，**LLM 二次查用的是硬规则**（不认"分手/离世"这类**软**话题）⇒ **LLM 故障时敏感话题可能被回响/关怀追问主动提及** | **A** 保持 fail-open（体验优先）·**B** 全链 fail-closed（LLM 故障 ⇒ 全量标敏感）·**C** **按"主动 vs 被动"分治**：故障时**主动提及**（回响卡/关怀推送）一律停，**被动检索**照常；并加故障告警 + 短期熔断缓存 ·**D** 扩容**软话题规则词表**兜底 | 建议 **C + D**：与铁律「护栏 fail-safe：不可用时默认**拒发**」在**主动触达**面保持一致（"主动提及"本来就是更像"发"的动作），同时避免 B 的"故障期回响功能整体失效"。**B 的风险**：LLM 抖动会把"全部内容"标成敏感，用户侧表现为回响突然消失且原因不明 |
| 11.3 | **画像人工编辑/确认/锁定/清除端点缺失**（`D07-12`，P1）→ 连带 `D07-10`（清除文案与能力不符）/ `D07-11`（`locked` 无真实语义） | 后端**只有读**与敏感话题写：`GET /interview/profile`、`POST|DELETE /profile/sensitive`（grep 全 `api/` 无其它画像写端点）。于是：① B1 定稿铁律**「用户操作优先：手动合并/确认后自动算法永不覆盖」在画像域无载体**（`interview.py:14` 自注"B1-6 后续"）；② 客户端"清除画像数据"的 confirm 文案写「将删除全部画像维度与证据锚点，此操作不可恢复」，实际**只清本地 storage**（代码注释自认"后端端点未登记，**不伪造远端清除成功**"，toast 也如实标"（本地）"）⇒ **确认弹窗这层仍是失实宣称**；③ ORM 有 `locked` 字段（`models/profile.py:83`）但 `echo._profile_hit` 不读它 ⇒ 锁定无意义 | **A** 做全 4 类端点（改值/确认/锁定/清除）·**B** 只做 **DELETE /profile 清除**（+ 级联证据锚点）·**C** 不做，但**改文案**（把承诺降级为"清除本机设置"） | 建议 **B 现在做、A 排二期**：① B 是"用户可主张的权利"（删除自己的画像）且能同时修掉 ②的失实宣称，改动面小（1 端点 + 级联 + 测试）；② A 若要真兑现"人工优先"，必须同时改 `profile_annotator`/`echo` 的 upsert 覆写语义（＝`D07-11`），**一处改动两处风险**，宜单独立项（内测期"只能看不能改"尚可接受，但**失实文案不可接受**）。**C 是最低限度**：若两条都不做，至少要把 confirm 文案改口 |
| 11.4 | **照片扩展名三表收敛的剩余两问**（`D02-9` 剩余，P2） | **已修**：`.heif` 补 MIME（`image/heif`）+ 新增"受理白名单 ⊆ MIME 表"机器断言（2026-09-25）。**剩余**：三表仍不一致 —— 受理白名单 `ALLOWED_PHOTO_EXTS = {jpg,jpeg,png,webp,heic,heif}`；`file_magic` 魔数只在 jpeg/png/webp/heic；`media_url._CONTENT_TYPES` 含 `.gif` 而受理层**根本放行不了 GIF** ⇒ 该条目**不可达** | **A** 放开 `.gif`（受理 + 魔数 + 缩略图取首帧）·**B** 收紧 `.heif`（与客户端解码能力对齐）·**C** 保持现状，仅删 `.gif` 不可达条目 | 建议 **先量化再定**：跑一次库内扩展名分布（`SELECT` 统计 `cos_key` 后缀）——若 `.heif` 占比可忽略则 **B**；若内测用户有 iPhone（HEIC/HEIF 常见）则保持受理面并**保留** `.heif`。**A（支持动图）不建议**：缩略图管线要取首帧、分享/导出链路都要跟着改，收益（GIF 照片）远小于成本；**C 的"删 .gif 表项"**属纯清理，可随时做但收益≈0 |
