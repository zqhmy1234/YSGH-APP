# 远期待办总账（Backlog Ledger）

> **用途**：grill 拍板中所有「本组不做 / 留远期 / 待后端排期」项的持久账本。
> 工作日志（.workbuddy/memory/*.md）是流水账会过 30 天被蒸馏，**这里才是远期项的唯一真值源**。
> 规矩（2026-09-01 峰宝质询后立）：凡拍板出「不做」的项，必须当场写入本账，注明来源、触发条件、验收标准。每完成一项打 ✅ 并注明落地位置。
> 建账：2026-09-01，来源=缺失页面系统性设计 F1-F8 各组 grill。
> ⚠️ **2026-09-23 清理影响**：本表若干行的证据出处 `实施排期表_缺失页面实现_20260902.md`、`B10_TabBar闪屏根治_spec_开发计划.md` 等已于该日删除（`git log --diff-filter=D -- <路径>` 可回看）。**行内结论与状态不受影响**，仅出处文档不再随仓；如需原始排期明细请从 git 历史取。
> **2026-09-23 销项清理**：已闭环项已从本表**删除**（不保留划改行、不再挂 ⏳）——A2/A3/A5/A9/A10 · B4/B5/B7/B8 · C1/C4 · F5 后端端点/F6 · P-5/P-6 · B10 全链（仅留 P4 尾项）。被删行的原文可用 `git log -p -- docs/远期待办总账.md` 回看，证据见对应提交说明。

## A. 后端契约缺口（待后端排期）

| # | 项 | 来源拍板 | 触发条件 / 前置 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| A1 | **LLM 动态打标链路**（chips 真值来源） | F1 组：「标签=AI 动态智能生成，不限死枚举」 | 后端排期；SetFit 5 类（情绪/灵感/混合/引用/待办）与设计稿 chips 两套体系，SetFit 不作 chips 真值 | detail 页 chips 显示 AI 生成的智能标签 | ⏳ |
| A4 | **评论/感想体系**（含回响「写一句此刻的想法」） | F6 组（2026-09-01）：弹层写想法入口本组不做；注意 messages 回响卡文案已承诺该能力，届时同步接真或改文案 | 评论体系产品定义（挂载对象=content？独立表？） | 回响弹层可写感想并持久化 | ⏳ |
| A6 | **Content.duration 前端显示**（后端半 ✅ 出参已含 duration；**残项＝前端语音卡空闲态槽位未接**） | W0-2 施工实锤 + 09-09 真机复验 🟡 清单第 1 条 | 前端接线（timeline 需下发 duration） | index/search/detail 语音卡空闲态即显真实时长（如 0:32）；⚠️ 09-09 记录后未再复核 | ⏳ |
| A7 | **照片 AI 描述 + 照片分类链路**（F1g AI 描述行端点 + 照片手选分类入库） | W0-3 施工实锤（2026-09-02）：全后端无照片描述端点，F1g 描述行省略；照片上传后无 classify 端点，标注页手选分类暂不入库 | 后端排期；前端 pgrid 档位网格/空态/删除角标已按画布落地，端点好后加描述行+接线 submitCorrection | 照片标注页显示 AI 描述行；手选分类真入库 | ⏳ |
| A8 | **Content.remark 落库**（后端半 ✅ 列存在 + 回读测试 `test_ba1_fields.py`；**残项＝分片上传 complete 路径不收 remark**） | W0-3 施工实锤 + 09-09 真机复验 🟡 清单第 3 条 | `complete` 收参或改走 PATCH update | 备注随内容真入库、detail 页可回显；⚠️ 09-09 记录后未再复核 | ⏳ |
| A11 | **回响弹层真照片通路**（content_type=photo 时显真图） | W1-3 施工实锤（2026-09-02）：EchoSheet 图片区暂为画布渐变演示占位；echo/today 契约只有 content_id 无图 URL，图片走 header token downloadFile / 预签名 URL（图片通路硬约束已拍板） | 预签名 URL 方案落地（A9 邻项）后接线 | 照片类回响弹层显示真图，非照片类保持渐变占位 | ⏳ |

## B. 客户端远期功能

| # | 项 | 来源拍板 | 触发条件 / 前置 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| B2 | **分享真实能力**（微信好友/朋友圈/复制链接面板项） | F7 组：MVP 先 UI，面板项占位；主按钮=保存卡片图 | 开放平台凭证 + 三模板卡片渲染落地 | 面板项可真实唤起分享 | ⏳ |
| B3 | **时间胶囊 AI 推荐封存** | F7 组：基础版纯手动；AI 推荐封存留远期（原案 P2+ 依赖画像成熟） | 画像系统（B1 域）成熟 | AI 主动推荐「值得封存」的记忆和时机 | ⏳ |
| B5 | **手机号换绑 / 微信绑定解绑管理** | F8 组：最小范围，绑定管理依赖后端能力 | 后端绑定能力（企微 key 三件套 + AppID/Secret 实网验收，关联 US-31/32/33） | security 页绑定/解绑/换绑全链路可用 | ⏳ |
| B6 | **定位服务**（信息卡地点行：定位权限 + 逆地理编码） | W0-3 施工实锤（2026-09-02）：画布 F1d 信息卡含时间/地点两行，项目无定位服务，先只留时间行 | 定位权限产品定义（隐私合规）+ 逆地理编码方案调研 | 记录标注页信息卡显示真实地点（如「上海 · 杨浦」） | ⏳ |
| B11 | **画像详情页**（manage 页 profile-card 点击落点） | W6 批（2026-09-04 峰宝拍板「去掉可点+登记远期」）：画像详情页无设计稿，profile-card 已移除 pressable | 先出画像详情页设计稿（审批通过后施工）；页面形态=画像描述全文+三项统计+性格标签汇总 | profile-card 恢复可点，跳真实画像详情页 | ⏳ |
| B12 | **17 处诚实 toast 三分类清账**（除导出→A9/W3、用户协议→W4 两处随对应波次接真外） | W6 批（2026-09-04 峰宝默认拍板「登记远期，验机前可改判」）：收藏云同步 detail:293/favorites:439、记忆编辑 detail:304、删除记忆 detail:312（关联 A3）、补充说明 PATCH detail:369、绑定管理×3 security（关联 B5）、附件选择 ai:302、自定义日期 CapsuleSheet:93、卡片图 ShareSheet:82、开源许可页 about:85 | 逐项后端能力上线（A2/A3/A5/A10）或产品拍板该交互去留 | 占位清单全行有归宿，无一处假交互留存 | ⏳ |

## C. 实锤 / 验收悬账

| # | 项 | 来源 | 验收标准 | 状态 |
|---|---|---|---|---|
| C2 | US-20 / US-52 录音中断恢复人工实测 | 用户故事半通项（状态机代码就绪） | 来电打断 + 30min 自动结束真机实测 | ⏳ |
| C3 | US-44 上架前接阿里云内容安全 | 用户故事半通项（当前敏感识别顶替） | 上架审核前接入 | ⏳ |

## 行内待办（当前组拍板要做，非远期）

- F5：index/search/detail 三处就地播放（downloadFile 带 header + InnerAudioContext）
- F7/F8 代码侧：security.uvue 状态化、detail.uvue 四按钮 toast 诚实化

- **B10 P4 留尾（唯一未尽）**：老四页仍在 pages.json（入口已全收编＝事实退役），观察数日无回潮后删除 + 全量回归。

| P-1 | **timeline 后端游标分页**（性能批 P2 拍板：前端先按日分批 20 组观察，后端分页大项） | 性能批 §P（2026-09-04） | 数据量显著增长后首拉变慢 | timeline 请求带 cursor，首拉 <300ms | ⏳ |
| P-2 | **图片磁盘缓存 LRU + 可见区预加载**（现仅会话级 Map，冷启/重启全重下） | 性能批 §P（2026-09-04） | 用户反馈重启后图片全重载 | 二次冷启缩略图零重下 | ⏳ |
| P-3 | **timeline 虚拟滚动 list-view 重构**（先观察分批效果） | 性能批 §P（2026-09-04） | 滚动 jank 基线恶化（现 0.99%） | 长列表 60fps | ⏳ |
| P-4 | **启动链路深度并行化**（字体子集化后重测再定：ensureLogin/flushOpQueue/fetchAll 串行链） | 性能批 §P（2026-09-04） | 冷启动仍 >2s | 冷启动 <1.5s | ⏳ |

## R9 批次登记（2026-09-06）

| 项 | 内容 | 来源拍板 | 触发条件 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| R9-A1 | **ST-DBSCAN 聚合语义审查**：「刚跑完步」语音与生日歌照片（photo+text）被聚进同一事件——时空邻近但语义不相关的聚合合理性待审。当前不拆（用户暂未投诉），但若真机复验出现「事件标题/内容驴唇不对马嘴」反馈需立项：引入语义相似度门（ASR text vs 事件主题）作为 ST-DBSCAN 合并前置条件，或在 L1 日卡聚合阶段做语义隔离 | R9 计划批次 6（2026-09-06） | 真机出现聚合语义违和反馈，或 ST-DBSCAN 调参批次开工时 | 聚合结果语义连贯：语音类碎片与不相关照片事件不再强行合并；已有事件需迁移方案（避免重复聚合污染） | ⏳ |
| R9-A2 | **语音建库 taken_at=NULL**：`_register_voice_content` 未设 taken_at（COALESCE 回退修复过事件时间显示，但列本身仍空）——后续按 taken_at 的查询/聚合（回响、去年今日）都会漏掉这批语音 | R9 批次 6 盘点（2026-09-06） | 后端语音链下次改动顺手修（register 时落 now() 或端侧录音时间） | 新建 voice 行 taken_at 非空；存量 NULL 行一次性回填 created_at | ⏳ |

## A 类实施批次进展（2026-09-07 夜 · 峰宝拍板开工，三 subagent 并行）

> 状态刷新（表内行状态以本节为准，逐项复验后改主表）：
> - **A2 收藏** 🟡 后端 W2-2 早已实现（主表「零命中」口径过期）+ FE1 前端接真（detail 星标 toggle/favorites 页）——待真机复验
> - **A3 删除+回收站** 🟡 后端 W2-1 早已实现 + FE1 接真（detail showModal 删除/trash 列表恢复清空，USE_MOCK_TRASH 清除）——待真机复验
> - **A5 胶囊** 🟡 BA2 全量落地（capsules 表+封存/列表/开启/撤销+scan 五路由+到期推送惰性触发幂等产消息）+ FE2 CapsuleSheet 真封存——待真机复验
> - **A6 duration** 🟡 后端落库（voice register+POST /contents 两点）+出参就位；**前端空闲态显示未接（剩余）**
> - **A7 照片 AI 描述+分类** 🟡 描述半项落地（管线内联 qwen3-vl+detail AI 卡 aiNote）；**手选分类 submitCorrection→classify/corrections 接线未做（剩余）**
> - **A8 remark** 🟡 后端（POST /contents）+文本/语音降级链前端接真；**语音主链（分片上传 complete）remark 无通路（剩余，建议后端 complete 收 remark 或补 content update 端点）**
> - **A9 余量（用量字节）** 🟡 size_bytes 落库（voice/photo+update 回填）+stats total_bytes+storage 用量卡接真，USE_MOCK_STORAGE 清除——待真机复验
> - **A10 content_id** 🟡 notify 落库+MessageOut 出参+messages 点击跳详情——待真机复验
> - **A1 LLM 打标** 🟡 打标任务管线挂钩（qwen-flash，失败显式）+detail chips 数据源切 tags_json——开发环境无 LLM Key 走显式「配置缺失」路径，待 Key 后复验
> - A4 / A11 ⏳ 不变（BA4 待产品拍板）
> - 附带修复：R9-A2 voice taken_at 存量回填 ✅（migration f2a3b4c5d6e7，回填后余 0）；alembic env.py disable_existing_loggers 坑修复

## A/B 批端到端联调测试批次（2026-09-08 · 峰宝拍板「apifox 式连环用户场景」，两 subagent 并行）

> 产出：`backend/tests/test_ab_scenarios.py` 29 用例全绿（S1 语音全旅程 11 + S2 胶囊生命周期 8 + S3 聚合 R9-C 回归 4 + S4 越权矩阵 4 + S5 回响链 2），三级断言（HTTP+出参+DB 回查），teardown 自清零残留；客户端 BB3 波形三宿主接线同步落地（见 B5a）。
>
> **联调实证的契约偏差（测试以实际行为为准，缺口登记如下）**：
> - **A9 补充**：POST /contents voice 单段路径**不回填 size_bytes**（仅分片 complete 路径回填）→ A9 的 total_bytes 依赖测试 DB 补齐；建议后端 voice register 路径补 size_bytes
> - **详情路由缺口（新）**：无 `GET /contents/{id}` 单条详情路由，客户端用 `GET /contents?content_id=` 列表过滤替代——能用但非 REST 正统，待后端排期补
> - timeline 出参 voice 为**单对象**（非计数字段）+ **不下发 duration**（客户端 InnerAudioContext 补齐，_batch_event_voices docstring 明示）——非缺陷，契约备忘
> - **C 类新悬账**：测试库 `event_edit_log.id` 缺 bigserial 序列（schema.sql 定义 bigserial，实际建表漂移无 default 无序列）→ merge/split/confirm 全 IntegrityError，test_event_ops.py 存量 7 用例同因失败。2026-09-08 已手工幂等补建 `event_edit_log_id_seq`+SET DEFAULT 对齐（本地库已修，**schema.sql 对齐迁移未落**，新环境重跑仍会炸）

## B 类实施批次进展（2026-09-08 凌晨 · 峰宝拍板开工，三 subagent 并行 + 主控契约衔接）

> 状态刷新（逐项复验后改主表）：
> - **B4 掩码显示** 🟡 后端 GET /users/me 落地（服务端掩码，完整号码不出网）+ security.uvue 接真——待真机复验
> - **B7 lastSyncAt** 🟡 sync_client 落盘 + storage 同步卡显真值——待真机复验（跑一次同步链后看）
> - **B1 问 AI 预填** 🟡 detail askAI 携带上下文（storage 三键）+ ai.uvue 接收（主控统一契约，读后即清）——待真机复验
> - **B8 mock 清账** 🟡 TIMELINE/DATA/ECHO 三开关清除；USE_MOCK_CHAT 按 B2 拍板保留（对话域后端不存在）；发版前 grep USE_MOCK 复查保留清单
> - **B12 toast 清账** 🟡 detail 补充说明/编辑接 PATCH 真；favorites 占位 toast 核对消亡+演示条目显式收口；security 绑定×3 卡 B5b、ShareSheet 卡卡片图渲染、ai:302 卡附件后端——随依赖解锁逐个清
> - **B5a 真实波形** 🟡 路线 B 后端预计算落地（GET /contents/{id}/waveform，chunk 遍历容错）+ VoiceWave 可选 prop 向后兼容 + **三宿主接线完成**（2026-09-08：contract PATH_WAVEFORM + play.fetchWaveform 静默降级 + index/search 播放懒拉 + detail 进页即拉，非响应式 Map+version ref 模式；favorites/theme-detail 留尾巴）——待真机复验（播语音→28 根 bar 高度与伪波形不同且逐条不同）
> - **B9 呼吸动画** 🟡 组件内实例唯一 id 前缀方案（多实例零冲突）+ UniElement.animate 随进度切 bar——待真机复验（scaleY 原点/极短音频观感）
> - B2/B3/B5b/B6/B11 ⏳ 不变（卡凭证/画像/产品定义/设计稿审批）
> - **B10 单页组件化** ✅ **根治完成（2026-09-09）**：A 方案 P1 金丝雀→P2 四 tab 全壳内→P4 老页删除（e2e3c26），峰宝真机终验「全部通过，没有闪帧」；详见工作树台账 §EE + `docs/B10_TabBar闪屏根治_spec_开发计划.md`
> - 附带解 A8 半项：PATCH /contents/{id} remark 端点（语音主链 remark 可后续走 update 通路）

## 真机复验清账波（2026-09-09 峰宝两批复验，A/B 批 🟡→✅ 批量升级）

> 昨晚 19 项清单「其他都验证通过」+ 六案整改后今晨 9 项回归 8/9 过（第 4 项「搜成功出 3 条」定性=正常语义命中非 bug）+ B10 四 tab 零闪。据此升级：
> - **✅ 复验通过**：A2 收藏 / A3 删除回收站 / A5 胶囊全链 / A9 用量字节（5.51MB 真值）/ A10 消息跳详情 / B1 问 AI 预填 / B4 掩码 / B7 上次同步 / B8 mock 清账 / B5a 真波形逐条不同 / B9 呼吸动画 / B12 编辑备注 PATCH / B10 闪屏根治
> - **🟡 后端 A 类剩余硬缺口（未完项）**：
>   1. **A6 duration 前端显示**——后端已落库出参，前端语音卡空闲态槽位未接（timeline 不下发 duration 的备忘）
>   2. **A7 照片手选分类**——submitCorrection→classify/corrections 接线未做（AI 描述半项✅）
>   3. **A8 语音主链 remark**——分片上传 complete 路径不收 remark（需 complete 收参或走 PATCH update）
>   4. **A1 LLM 打标复验**——开发环境无 Key 走显式缺失路径，待真实 Key 后复验标签质量
>   5. **A9 补充**：POST /contents voice 单段路径不回填 size_bytes（联调实证，建议后端 register 路径补写）
>   6. **详情路由缺口**：无 GET /contents/{id}（客户端以 ?content_id= 过滤替代，非 REST 正统）
>   7. **C 类悬账**：event_edit_log.id 序列漂移——本地已手工补建，**schema.sql 对齐迁移未落，新环境重跑仍炸**
>   8. **A4 评论/感想 + A11 回响真照片**：⏳ 待产品拍板（不变）
> - **台账过期口径修正**：C5「测试音回退拆除」实已于 R9（2026-09-06）完成（audio_player 404=fail 显式「音频暂不可用」，wav 已不存在）→ 视同 ✅；C1 openapi 全量实证已随 B 批两轮完成（63 路由）→ 视同 ✅

## 波 E 合并收口（2026-09-10）登记

### B 类 · 轮盘录音 UI 补回（✅ 已销账 2026-09-10 15:10——任务系误报，轮盘从未丢失）
- **销账实证（主窗逐条看图核实现行 develop `client/components/RecordSheet/RecordSheet.uvue`）**：波 E 裁决窗「分支侧改用波形、全局零 wheel-dot」的判断**不成立**——RecordSheet 内 `VoiceWave/wave-bar` 引用=0（VoiceWave 实际用于 index/search/detail 的**播放侧**波形条，与录音轮盘不是一个东西）；录音侧现行实现即轮盘且是**历经三代真机修复的最终版**：
  - 拍板① 完全透明轮盘+细碎大小不一白点+中央透明珠：✅ L1566 `.wheel` 注释「轮盘本体完全透明（拍板：轮廓无色）」、L1580 `.wd` 白点+DROP_SHADOW 悬浮投影（视觉铁律在码）、L1625 `.hub` 磨砂玻璃珠（白60%+深棕投影）
  - 拍板② 点击顺时针旋转+小圆点向心移动留轨迹：✅ L1001 `startWheelAnims()` 外层 `rotate(0→360deg)` 9s linear infinite 公转 + `dotRender()` 主点 DOT_TRACK 三角波向心振荡、幽灵点 150/300ms 滞后采样=轨迹留痕（21 点=7 主+7×2 幽灵）
  - 拍板③ 无波形装饰条+计时保留：✅ 零 VoiceWave + `.rec-timer` 在
  - 拍板④ 录完切预览态、主按钮「确定」：✅ L1262 `voiceStage='preview'` + L96 `cta-main` 文案「确定」
  - **R3/R3b/R3c 三代注释含峰宝 09-04/09-05 验机修复记录**（rpx 丢弃、CSS 声明格式）——证明该实现在真机上迭代到正确形态，非纸面代码
- **教训**：merge 冲突裁决「取基底判丢失」必须**打开双侧文件看实际内容**，不能靠 class 命名族推断功能归属——本单误报浪费了一个"待做"账位+一次交接。
- **残留验收**：真机目检一轮（动画流畅度/幽灵轨迹观感，你插线时顺手即可）；`0944f89` 旧版 `vi-wheel` 实现=已退役资产不再需要提取。

### 波 E 完成情况（2026-09-10）
- **merge commit `cb52d224`**（父：`d8fef536` + `558f16a`）已推 `origin/develop`，现由 `ad5d64f5` 继承
- **9 个冲突逐条处置**：4 后端文件（`events.py`/`media.py`/`errors.py`/`content.py`）取分支侧（经实证分支侧为 develop 侧超集：多 `_batch_event_voices`、P2-4 软删过滤、P2-6 日志降级、漏登的 `MEDIA_003`）；4 文档（`_diff_ledger.md`/`lessons.md`/`progress.md`/`session-handoff.md`）union 合并保留双方；`RecordSheet.uvue` 见上
- **manifest.json 两者兼得**：`appid=__UNI__2650A2A` + `abiFilters=["arm64-v8a"]`（分支侧云打包真值）＋ 补回 `app-android`/`app-ios` 平台结构块
- **验证门**：精准域 84 passed + 补充域 63 passed = **147 passed / 0 failed**（全量 821 未跑：Docker Desktop 未运行 + 不做无差别全量回测）
