# 远期待办总账（Backlog Ledger）

> **用途**：grill 拍板中所有「本组不做 / 留远期 / 待后端排期」项的持久账本。
> 工作日志（.workbuddy/memory/*.md）是流水账会过 30 天被蒸馏，**这里才是远期项的唯一真值源**。
> 规矩（2026-09-01 峰宝质询后立）：凡拍板出「不做」的项，必须当场写入本账，注明来源、触发条件、验收标准。每完成一项打 ✅ 并注明落地位置。
> 建账：2026-09-01，来源=缺失页面系统性设计 F1-F8 各组 grill。

## A. 后端契约缺口（待后端排期）

| # | 项 | 来源拍板 | 触发条件 / 前置 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| A1 | **LLM 动态打标链路**（chips 真值来源） | F1 组：「标签=AI 动态智能生成，不限死枚举」 | 后端排期；SetFit 5 类（情绪/灵感/混合/引用/待办）与设计稿 chips 两套体系，SetFit 不作 chips 真值 | detail 页 chips 显示 AI 生成的智能标签 | ⏳ |
| A2 | **收藏 favorite 端点** | F4 组实锤：全后端 grep favorite 零命中，audit「承接 favorite 端点」不实 | 后端排期 | detail 收藏持久化 + 收藏页真数据 | ⏳ |
| A3 | **内容删除端点**（软删除 30 天）+ App 端删除链路设计文档补缺 | F7 组：删除假删拍板「补后端端点 + 审设计文档」；US-49 是同步层软删，App 端删除入口在用户故事清单零定义；**W1-1 施工补注（2026-09-02）**：回收站列表/恢复/清空 UI 已落地（`pages/storage/trash.uvue`，USE_MOCK_TRASH=false 空态），全部依赖本项端点 | 后端排期 + 设计文档补写；端点好后 trash.uvue onRestore/onClearAll/goTrash 真数据接线 | detail 删除 → 软删 30 天 → 回收站可见可恢复可清空 | ⏳ |
| A4 | **评论/感想体系**（含回响「写一句此刻的想法」） | F6 组（2026-09-01）：弹层写想法入口本组不做；注意 messages 回响卡文案已承诺该能力，届时同步接真或改文案 | 评论体系产品定义（挂载对象=content？独立表？） | 回响弹层可写感想并持久化 | ⏳ |
| A5 | **时间胶囊端点**（封存/查询/到期开启；2026-09-02 01:09 峰宝拍板**全量含到期推送**，已入排期表 W2-3 不再属远期，留此行跟踪至落地） | 2026-09-02 排期摸底：grep `capsule\|胶囊` 全 backend 零命中，detail 胶囊按钮假 toast；F7b 面板联调前置 | 排期表 W2-3（见 `docs/实施排期表_缺失页面实现_20260902.md`）；F7b 前端 UI 可先行 | W1-8 封存后可查询，到期后胶囊可开启 | ⏳ |
| A6 | **Content.duration 字段**（音频/语音时长入库 + VoiceInfo/timeline 下发） | W0-2 施工实锤（2026-09-02）：Content 模型无 duration，前端语音卡空闲态 duration 槽位只能空串占位，总时长须等播放后 onTimeUpdate 才知 | 后端排期；前端槽位已留好（非活动态显 duration，活动态显 fmtVoiceTime(totalSec)） | index/search/detail 语音卡空闲态即显真实时长（如 0:32） | ⏳ |
| A7 | **照片 AI 描述 + 照片分类链路**（F1g AI 描述行端点 + 照片手选分类入库） | W0-3 施工实锤（2026-09-02）：全后端无照片描述端点，F1g 描述行省略；照片上传后无 classify 端点，标注页手选分类暂不入库 | 后端排期；前端 pgrid 档位网格/空态/删除角标已按画布落地，端点好后加描述行+接线 submitCorrection | 照片标注页显示 AI 描述行；手选分类真入库 | ⏳ |
| A8 | **Content.remark 字段**（备注入库） | W0-3 施工实锤（2026-09-02）：Content 模型无 remark，F1d 备注卡只能 UI 保留不提交 | 后端排期；前端 remark-input UI 已留好，端点好后 saveVoice/saveText/savePhoto 附带 remark | 备注内容随内容真入库，detail 页可回显 | ⏳ |
| A9 | **导出端点 + 用量字节聚合** | W1-1 施工实锤（2026-09-02）：排期表「导出复用 US-42 已验证链路」**证伪**——旧 /api/v1/export 已随 W10.4 换页移除，docs/openapi.json 45 路径无 export/storage/usage/trash；后端 Content 无 size/bytes 字段，用量卡降级为条目数聚合（fetchTimeline(null) 聚合 photoCount/contentCount/voice）。**2026-09-04 W3 卡E03 导出半项落地**：stash 49bed24 考古复刻95%，/api/v1/export 重挂（限流+鉴权+分块），storage onExport 接真，test_export 3 绿（e49a215）；待真机复验 | ~~后端排期~~ 导出半项✅；**剩余=用量字节聚合**（需后端 Content 加 size 字段+用量卡改真实字节）；限流 _DOMAIN_SCOPES 登记、导出文件另存/分享 UX 待拍板 | 导出真机可用（复验中）；用量卡显示真实空间占用 | 🟡 |
| A10 | **MessageOut 增加 content_id 关联字段**（复盘/语音完成/回响消息跳转内容详情） | W1-3 施工实锤（2026-09-02）：messages 消息路由只「就地展开」显示完整正文——MessageOut 无 content_id，无法跳 detail；回响弹层「重温这段记忆」已按 contentId 跳转（/echo/today 有 content_id），普通消息缺同等能力 | 后端排期；前端 openMessage 路由骨架已留好，加一行 navigateTo 即接通 | 复盘/语音完成消息点击可直接进对应内容详情 | ⏳ |
| A11 | **回响弹层真照片通路**（content_type=photo 时显真图） | W1-3 施工实锤（2026-09-02）：EchoSheet 图片区暂为画布渐变演示占位；echo/today 契约只有 content_id 无图 URL，图片走 header token downloadFile / 预签名 URL（图片通路硬约束已拍板） | 预签名 URL 方案落地（A9 邻项）后接线 | 照片类回响弹层显示真图，非照片类保持渐变占位 | ⏳ |

## B. 客户端远期功能

| # | 项 | 来源拍板 | 触发条件 / 前置 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| B1 | **问 AI 带上下文预填**（detail → AI 页带记忆标题/摘要） | F7 组定案，实现列远期 | B2 对话域接线（AI 后端开发）时一并设计上下文携带 | 从 detail 问 AI，开场白自动带上当前记忆 | ⏳ |
| B2 | **分享真实能力**（微信好友/朋友圈/复制链接面板项） | F7 组：MVP 先 UI，面板项占位；主按钮=保存卡片图 | 开放平台凭证 + 三模板卡片渲染落地 | 面板项可真实唤起分享 | ⏳ |
| B3 | **时间胶囊 AI 推荐封存** | F7 组：基础版纯手动；AI 推荐封存留远期（原案 P2+ 依赖画像成熟） | 画像系统（B1 域）成熟 | AI 主动推荐「值得封存」的记忆和时机 | ⏳ |
| B4 | **GET /users/me 真掩码显示** | F8 组：先「真实状态化」顶住，端点好后升级显真号码掩码 | 后端补端点 | security 页显示 138****XXXX 真掩码 | ⏳ |
| B5 | **真实音频波形**（解码 PCM 按幅度渲染竖条） | W0-2 拍板：三处波形用 genWaveHeights 确定性伪波形（按 contentId 种子），真实波形需原生/JS 解码音频流幅值 | 音频链路真机验收稳定后；uni-app x 解码方案调研（暂无现成 API） | 波形与真实音频幅值一致，逐条不同 | ⏳ |
| B5 | **手机号换绑 / 微信绑定解绑管理** | F8 组：最小范围，绑定管理依赖后端能力 | 后端绑定能力（企微 key 三件套 + AppID/Secret 实网验收，关联 US-31/32/33） | security 页绑定/解绑/换绑全链路可用 | ⏳ |
| B6 | **定位服务**（信息卡地点行：定位权限 + 逆地理编码） | W0-3 施工实锤（2026-09-02）：画布 F1d 信息卡含时间/地点两行，项目无定位服务，先只留时间行 | 定位权限产品定义（隐私合规）+ 逆地理编码方案调研 | 记录标注页信息卡显示真实地点（如「上海 · 杨浦」） | ⏳ |
| B8 | **演示数据开关清理**（发版前统一切真数据） | **峰宝拍板（2026-09-02）**：拿不到真数据也要做 UI 验证，假数据保留——「之后改成真数据端点就好了」。现有开关：`storage.uvue USE_MOCK_STORAGE`（真数据为空时注画布演示值 23 条+「5 分钟前」）、`trash.uvue USE_MOCK_TRASH=true`（F2b 三条演示行）、`messages.uvue USE_MOCK_ECHO`（F6a 演示回响卡）、`search.uvue USE_MOCK_DATA`、`index.uvue USE_MOCK_TIMELINE`、`ai.uvue USE_MOCK_CHAT、favorites.uvue USE_MOCK_FAVORITES（W1-7 收藏演示条目）、detail/CapsuleSheet USE_MOCK_CAPSULE（W1-8 本地暂存演示态）` | 各端点上线（A2/A3/A9/A10）逐个关开关接真值；**发版检查清单必含「grep USE_MOCK 全部 =false」** | 全部 mock 开关关闭且真数据贯通后才能提审 | ⏳ |
| B7 | **sync_client lastSyncAt 本地持久化** | W1-1 施工实锤（2026-09-02）：sync_client.uts 现成 pendingSyncCount/isSyncPaused/runSyncChain/reconcileNow，无 lastSyncAt 持久化，存储页同步卡「上次同步」只能显「—」 | 同步域接线时补本地 storage 持久化（每次 runSyncChain 成功落时间戳） | 存储页同步卡显示真实上次同步时间 | ⏳ |
| B9 | **VoiceWave 波形呼吸动画**（当前根 bar scaleY 0.5↔1 呼吸） | §I A' 拍板批次（2026-09-02 峰宝拍「A 矢量动画」）降级静态：呼吸目标 bar=liveBarIdx **随播放进度动态变化**，命令式 animate 需监听 props 变化换目标元素；且组件被 index/search/detail 三宿主多实例挂载，uni.getElementById 有全局 id 冲突风险（wave-bar-live 类已留语义标记，台账 §K） | RecordSheet 轮盘 UniElement.animate() 先例真机跑通（§K 验机过）+ 解决多实例元素定位（函数 ref / 宿主注入唯一 id） | 播放中当前根 bar 呼吸缩放，进度推进时动画正确切换目标 bar | ⏳ |
| B11 | **画像详情页**（manage 页 profile-card 点击落点） | W6 批（2026-09-04 峰宝拍板「去掉可点+登记远期」）：画像详情页无设计稿，profile-card 已移除 pressable | 先出画像详情页设计稿（审批通过后施工）；页面形态=画像描述全文+三项统计+性格标签汇总 | profile-card 恢复可点，跳真实画像详情页 | ⏳ |
| B12 | **17 处诚实 toast 三分类清账**（除导出→A9/W3、用户协议→W4 两处随对应波次接真外） | W6 批（2026-09-04 峰宝默认拍板「登记远期，验机前可改判」）：收藏云同步 detail:293/favorites:439、记忆编辑 detail:304、删除记忆 detail:312（关联 A3）、补充说明 PATCH detail:369、绑定管理×3 security（关联 B5）、附件选择 ai:302、自定义日期 CapsuleSheet:93、卡片图 ShareSheet:82、开源许可页 about:85 | 逐项后端能力上线（A2/A3/A5/A10）或产品拍板该交互去留 | 占位清单全行有归宿，无一处假交互留存 | ⏳ |

## C. 实锤 / 验收悬账

| # | 项 | 来源 | 验收标准 | 状态 |
|---|---|---|---|---|
| C1 | 后端起服务后 `curl /openapi.json` 全量实证 | 老铁律「端点存疑一律实证，勿信文档旧口径」；2026-09-01 后端进程未启动，F1/F5/F6 多项核实基于源码静态阅读 | openapi 47+ 路由逐项对拍 _resource_endpoint_matrix.md | ⏳ |
| C2 | US-20 / US-52 录音中断恢复人工实测 | 用户故事半通项（状态机代码就绪） | 来电打断 + 30min 自动结束真机实测 | ⏳ |
| C3 | US-44 上架前接阿里云内容安全 | 用户故事半通项（当前敏感识别顶替） | 上架审核前接入 | ⏳ |
| C4 | F4 收藏页后端支撑缺口已报备（同 A2） | 2026-09-01 F7 组 grill 报备 | =A2 | ⏳ |
| C5 | **audio_player 测试音回退拆除**（§O P4 拟合：static/test-voice.wav 2.5s，downloadFile 404/fail 即回退播放+toast 标注） | §O 批（2026-09-03 峰宝授权「随便拟合一条音频用于测试」） | 后端 `GET /api/v1/media/audio/{content_id}` 有真实可播音频（见行内待办 F5 端点） | 删 playTestFallback 两处调用+函数与 wav 文件，真实音频全链可播 | ⏳ |

## 行内待办（当前组拍板要做，非远期）

- F5：后端补 `GET /api/v1/media/audio/{content_id}`（鉴权+流式）——F5 组拍板「本组补端点」
- F5：index/search/detail 三处就地播放（downloadFile 带 header + InnerAudioContext）
- F6：回响弹层（重温卡片）+ 按类型路由（关怀→AI 页 / 回响→弹层 / 复盘语音完成→就地展开）
- F7/F8 代码侧：security.uvue 状态化、detail.uvue 四按钮 toast 诚实化

- **B10 · TabBar 切页闪屏根治**（来源：§P8 峰宝复验 2026-09-03 拍板前缓解）：当前自定义 TabBar 用 redirectTo 切 Tab = 先销毁后重建，间隙露原生窗口底（已用 globalStyle+四 Tab 页 backgroundColor #F8F7F4 对齐缓解，闪灰不闪黑）。根治方案二选一待拍板：① pages.json 注册原生 tabBar（switchTab 保活页实例，切页零重建）② 四 Tab 页合并单页+组件切换（栈恒 1 页更彻底，改造量大）。验收标准：连切四 Tab 无任何底色闪烁。

- **B10 预研完成（2026-09-03，待拍板后执行）**：技术路线核实——uni-app x 官方支持 pages.json 原生 tabBar + uni.switchTab（官方文档明确「也支持了 tabbar 和导航栏设置」，且原生 tabBar 页显示一次即驻内存、再切只触发 onShow 不触发 onLoad = 根治机制成立）。业界成熟解法（uni-app 社区验证）：**原生 tabBar 驱动 switchTab + 启动隐藏原生条（uni.hideTabBar）+ 现有自定义 TabBar 组件视觉接管**——改造量远小于单页组件化方案，TabBar 组件/宿主绑定/RecordSheet 浮层全部复用。上机前必须验证两项：① uni.hideTabBar 在 uvue Android 可用性（文档未单列，hello uni-app x demo 可查）② 原生 tabBar 存在时页面高度含 tabBar 占位，与现有玻璃 TabBar 沉浸式覆盖布局（z-index 999 盖内容）冲突的适配方式。推荐拍板选项①（原生 tabBar 驱动），单页组件化（选项②）作为兜底不推荐（改造大、RecordSheet 全局浮层契约要重验）。

- **B10 ❌ 选项①上机证伪（2026-09-03 峰宝真机）**：原生 tabBar 注册 + App.onLaunch/onShow 双保险 hideTabBar 在 app-uvue Android 不干净生效——真机截图实锤：原生条（红「时间轴」+灰三项）仍渲染在屏幕最底，自定义玻璃 TabBar 被顶离屏底悬在半空，页面视口/层级错乱，且页面交互大面积失效（TabBar 中央 + 无反应、空状态拍下第一张无反应）。**处置：四处全回退**（pages.json tabBar 块 / TabBar.uvue 四 go 改回 redirectTo / App.uvue 两处 hideTabBar 删除 / detail askAI 改回 redirectTo），已重推包。**根治方案回池待拍板**：①已证伪；剩 A) 四 Tab 页合并单页+组件切换（栈恒 1 页，改造大、RecordSheet 浮层契约要重验）B) 官方 glassEffect 玻璃 tabBar（iOS26 液态玻璃，仅图片图标，Android 可用性未核实）。闪屏缓解现状：globalStyle+四页 backgroundColor #F8F7F4 对齐（闪灰不闪黑）。

- **B10 方案 B 澄清（2026-09-03 峰宝质询后纠正）**：峰宝指出 glassEffect 只是视觉形态（磨砂玻璃渲染效果），问它能不能解决闪屏——**答：不能**。闪屏根因是 redirectTo 页面级销毁重建（路由层），glassEffect 不触碰路由，只改 TabBar 背景渲染形态；且原生 tabBar 注册路线在 uvue Android 已证伪（见上条），B 方案连自身可行性都未过验证。**09-03 复验确认闪屏依旧后，B 从「根治案」降级移出，唯一根治路线 = A 单页组件化**。若 A 改造量暂不接受，可先做缓解加强：统一四页 + globalStyle 背景色差（现已做 #F8F7F4，闪灰不闪黑）。

- **B10 · A 方案节奏拍板（2026-09-03 峰宝）**：「等本批过了再排 A」——§W 批（directPick 首帧根治 + 面板圆角 + CapsuleSheet 补刀）真机复验通过后，A（四 Tab 单页组件化）作为独立波次开工，改造量评估见 09-03 会话（~15 调用点 + 生命周期重构 + v-if/v-show 取舍，2-3 天级 + 全页面回归）。验收标准：连切四 Tab 无闪屏 + RecordSheet 浮层契约全验。

| P-1 | **timeline 后端游标分页**（性能批 P2 拍板：前端先按日分批 20 组观察，后端分页大项） | 性能批 §P（2026-09-04） | 数据量显著增长后首拉变慢 | timeline 请求带 cursor，首拉 <300ms | ⏳ |
| P-2 | **图片磁盘缓存 LRU + 可见区预加载**（现仅会话级 Map，冷启/重启全重下） | 性能批 §P（2026-09-04） | 用户反馈重启后图片全重载 | 二次冷启缩略图零重下 | ⏳ |
| P-3 | **timeline 虚拟滚动 list-view 重构**（先观察分批效果） | 性能批 §P（2026-09-04） | 滚动 jank 基线恶化（现 0.99%） | 长列表 60fps | ⏳ |
| P-4 | **启动链路深度并行化**（字体子集化后重测再定：ensureLogin/flushOpQueue/fetchAll 串行链） | 性能批 §P（2026-09-04） | 冷启动仍 >2s | 冷启动 <1.5s | ⏳ |
| P-5 | **theme-detail.uvue:106 fetchTimeline(null) 残留**（性能卡 PE 域外发现） | 性能批 §P（2026-09-04） | 主题详情页打开慢 | 改明细端点或窄化参数 | ⏳ |
| P-6 | **static/images 6 PNG + test-voice.wav 清理拍板**（审计误报实有引用：ai.uvue/detail.uvue/audio_player.uts） | 性能批 §P（2026-09-04）；**09-04 峰宝拍板：留着——它们是视觉验证的一部分** | 视觉验证完成后峰宝再拍是否清理 | 引用与资产同去同留 | 🟢 已拍板保留 |

## R9 批次登记（2026-09-06）

| 项 | 内容 | 来源拍板 | 触发条件 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| R9-A1 | **ST-DBSCAN 聚合语义审查**：「刚跑完步」语音与生日歌照片（photo+text）被聚进同一事件——时空邻近但语义不相关的聚合合理性待审。当前不拆（用户暂未投诉），但若真机复验出现「事件标题/内容驴唇不对马嘴」反馈需立项：引入语义相似度门（ASR text vs 事件主题）作为 ST-DBSCAN 合并前置条件，或在 L1 日卡聚合阶段做语义隔离 | R9 计划批次 6（2026-09-06） | 真机出现聚合语义违和反馈，或 ST-DBSCAN 调参批次开工时 | 聚合结果语义连贯：语音类碎片与不相关照片事件不再强行合并；已有事件需迁移方案（避免重复聚合污染） | ⏳ |
| R9-A2 | **语音建库 taken_at=NULL**：`_register_voice_content` 未设 taken_at（COALESCE 回退修复过事件时间显示，但列本身仍空）——后续按 taken_at 的查询/聚合（回响、去年今日）都会漏掉这批语音 | R9 批次 6 盘点（2026-09-06） | 后端语音链下次改动顺手修（register 时落 now() 或端侧录音时间） | 新建 voice 行 taken_at 非空；存量 NULL 行一次性回填 created_at | ⏳ |

## A 类实施批次进展（2026-09-07 夜 · 峰宝拍板开工，三 subagent 并行）

> 状态刷新（表内行状态以本节为准，逐项复验后改主表）：
> - **A2 收藏** 🟡 后端 W2-2 早已实现（主表「零命中」口径过期）+ FE1 前端接真（detail 星标 toggle/favorites 页）——待真机复验
> - **A3 删除+回收站** 🟡 后端 W2-1 早已实现 + FE1 接真（detail showModal 删除/trash 列表恢复清空，USE_MOCK_TRASH 清除）——待真机复验
> - **A5 胶囊** 🟡 BA2 全量落地（capsules 表+封存/列表/开启/撤销+scan 五路由+到期推送惰性触发幂等产消息）+ FE2 CapsuleSheet 真封存——待真机复验
> - **A6 duration** 🟡 后端落库（voice register+POST /contents 两点）+出参就位；**前端空闲态显示未接（剩余）**
> - **A7 照片 AI 描述+分类** 🟡 描述半项落地（管线内联 qwen3-vl+detail AI 卡 aiNote）；**手选分类 submitCorrection→classify/corrections 接线未做（剩余）**
> - **A8 remark** 🟡 后端（POST /contents）+文本/语音降级链前端接真；**语音主链（分片上传 complete）remark 无通路（剩余，建议后端 complete 收 remark 或补 content update 端点）**
> - **A9 余量（用量字节）** 🟡 size_bytes 落库（voice/photo+update 回填）+stats total_bytes+storage 用量卡接真，USE_MOCK_STORAGE 清除——待真机复验
> - **A10 content_id** 🟡 notify 落库+MessageOut 出参+messages 点击跳详情——待真机复验
> - **A1 LLM 打标** 🟡 打标任务管线挂钩（qwen-flash，失败显式）+detail chips 数据源切 tags_json——开发环境无 LLM Key 走显式「配置缺失」路径，待 Key 后复验
> - A4 / A11 ⏳ 不变（BA4 待产品拍板）
> - 附带修复：R9-A2 voice taken_at 存量回填 ✅（migration f2a3b4c5d6e7，回填后余 0）；alembic env.py disable_existing_loggers 坑修复
