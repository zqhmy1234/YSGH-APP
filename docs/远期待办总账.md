# 远期待办总账（Backlog Ledger）

> **用途**：grill 拍板中所有「本组不做 / 留远期 / 待后端排期」项的持久账本。
> 工作日志（.workbuddy/memory/*.md）是流水账会过 30 天被蒸馏，**这里才是远期项的唯一真值源**。
> 规矩（2026-09-01 峰宝质询后立）：凡拍板出「不做」的项，必须当场写入本账，注明来源、触发条件、验收标准。每完成一项打 ✅ 并注明落地位置。
> 建账：2026-09-01，来源=缺失页面系统性设计 F1-F8 各组 grill。

## A. 后端契约缺口（待后端排期）

| # | 项 | 来源拍板 | 触发条件 / 前置 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| A1 | **LLM 动态打标链路**（chips 真值来源） | F1 组：「标签=AI 动态智能生成，不限死枚举」 | 后端排期；SetFit 5 类（情绪/灵感/混合/引用/待办）与设计稿 chips 两套体系，SetFit 不作 chips 真值 | detail 页 chips 显示 AI 生成的智能标签 | ⏳ |
| A2 | **收藏 favorite 端点** | F4 组实锤：全后端 grep favorite 零命中，audit「承接 favorite 端点」不实 | 后端排期 | detail 收藏持久化 + 收藏页真数据 | ⏳ |
| A3 | **内容删除端点**（软删除 30 天）+ App 端删除链路设计文档补缺 | F7 组：删除假删拍板「补后端端点 + 审设计文档」；US-49 是同步层软删，App 端删除入口在用户故事清单零定义 | 后端排期 + 设计文档补写 | detail 删除 → 软删 30 天 → 回收站可见 | ⏳ |
| A4 | **评论/感想体系**（含回响「写一句此刻的想法」） | F6 组（2026-09-01）：弹层写想法入口本组不做；注意 messages 回响卡文案已承诺该能力，届时同步接真或改文案 | 评论体系产品定义（挂载对象=content？独立表？） | 回响弹层可写感想并持久化 | ⏳ |
| A5 | **时间胶囊端点**（封存/查询/到期开启；2026-09-02 01:09 峰宝拍板**全量含到期推送**，已入排期表 W2-3 不再属远期，留此行跟踪至落地） | 2026-09-02 排期摸底：grep `capsule\|胶囊` 全 backend 零命中，detail 胶囊按钮假 toast；F7b 面板联调前置 | 排期表 W2-3（见 `docs/实施排期表_缺失页面实现_20260902.md`）；F7b 前端 UI 可先行 | W1-8 封存后可查询，到期后胶囊可开启 | ⏳ |
| A6 | **Content.duration 字段**（音频/语音时长入库 + VoiceInfo/timeline 下发） | W0-2 施工实锤（2026-09-02）：Content 模型无 duration，前端语音卡空闲态 duration 槽位只能空串占位，总时长须等播放后 onTimeUpdate 才知 | 后端排期；前端槽位已留好（非活动态显 duration，活动态显 fmtVoiceTime(totalSec)） | index/search/detail 语音卡空闲态即显真实时长（如 0:32） | ⏳ |

## B. 客户端远期功能

| # | 项 | 来源拍板 | 触发条件 / 前置 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| B1 | **问 AI 带上下文预填**（detail → AI 页带记忆标题/摘要） | F7 组定案，实现列远期 | B2 对话域接线（AI 后端开发）时一并设计上下文携带 | 从 detail 问 AI，开场白自动带上当前记忆 | ⏳ |
| B2 | **分享真实能力**（微信好友/朋友圈/复制链接面板项） | F7 组：MVP 先 UI，面板项占位；主按钮=保存卡片图 | 开放平台凭证 + 三模板卡片渲染落地 | 面板项可真实唤起分享 | ⏳ |
| B3 | **时间胶囊 AI 推荐封存** | F7 组：基础版纯手动；AI 推荐封存留远期（原案 P2+ 依赖画像成熟） | 画像系统（B1 域）成熟 | AI 主动推荐「值得封存」的记忆和时机 | ⏳ |
| B4 | **GET /users/me 真掩码显示** | F8 组：先「真实状态化」顶住，端点好后升级显真号码掩码 | 后端补端点 | security 页显示 138****XXXX 真掩码 | ⏳ |
| B5 | **真实音频波形**（解码 PCM 按幅度渲染竖条） | W0-2 拍板：三处波形用 genWaveHeights 确定性伪波形（按 contentId 种子），真实波形需原生/JS 解码音频流幅值 | 音频链路真机验收稳定后；uni-app x 解码方案调研（暂无现成 API） | 波形与真实音频幅值一致，逐条不同 | ⏳ |
| B5 | **手机号换绑 / 微信绑定解绑管理** | F8 组：最小范围，绑定管理依赖后端能力 | 后端绑定能力（企微 key 三件套 + AppID/Secret 实网验收，关联 US-31/32/33） | security 页绑定/解绑/换绑全链路可用 | ⏳ |

## C. 实锤 / 验收悬账

| # | 项 | 来源 | 验收标准 | 状态 |
|---|---|---|---|---|
| C1 | 后端起服务后 `curl /openapi.json` 全量实证 | 老铁律「端点存疑一律实证，勿信文档旧口径」；2026-09-01 后端进程未启动，F1/F5/F6 多项核实基于源码静态阅读 | openapi 47+ 路由逐项对拍 _resource_endpoint_matrix.md | ⏳ |
| C2 | US-20 / US-52 录音中断恢复人工实测 | 用户故事半通项（状态机代码就绪） | 来电打断 + 30min 自动结束真机实测 | ⏳ |
| C3 | US-44 上架前接阿里云内容安全 | 用户故事半通项（当前敏感识别顶替） | 上架审核前接入 | ⏳ |
| C4 | F4 收藏页后端支撑缺口已报备（同 A2） | 2026-09-01 F7 组 grill 报备 | =A2 | ⏳ |

## 行内待办（当前组拍板要做，非远期）

- F5：后端补 `GET /api/v1/media/audio/{content_id}`（鉴权+流式）——F5 组拍板「本组补端点」
- F5：index/search/detail 三处就地播放（downloadFile 带 header + InnerAudioContext）
- F6：回响弹层（重温卡片）+ 按类型路由（关怀→AI 页 / 回响→弹层 / 复盘语音完成→就地展开）
- F7/F8 代码侧：security.uvue 状态化、detail.uvue 四按钮 toast 诚实化
