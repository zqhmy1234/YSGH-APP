# 实施排期表：缺失页面 25 帧 → 代码落地（2026-09-02 定稿）

> **状态**：峰宝已批准排期框架（2026-09-02 00:2x）；§七 两项已圈定（2026-09-02 01:09）——W2-3 **全量含到期推送**、施工分支 **新 worktree `.wt/missing-pages`（feature/missing-pages-impl，基于 develop 含 wrap1 合并）**，W0-4 已开工。
> **输入**：25 帧画布设计（F1×11 / F2-F4×6 / F7×2 / F5-F6×3 / F9×3）+ audit 20 处占位 toast + 远期总账 13 项
> **排序原则**：后端端点 → 客户端接线/新页面 → 真机验证，同层内按用户触达频次排
> **姊妹账本**：远期项唯一真值源 = `docs/远期待办总账.md`（本表只收「要做」项）

---

## 〇、排期前新发现（摸底实证）

| # | 发现 | 证据 | 处置 |
|---|---|---|---|
| 1 | **时间胶囊后端零端点**——grep `capsule|胶囊` 全 backend 零命中；detail 胶囊按钮是假 toast | 2026-09-02 grep 实证 | 新增 **W2-3 胶囊端点**；已补入远期总账 A5 |
| 2 | 四份画布快照已过期（ai/ai_reply/portrait_manage/account_security，W10.4 后未重导） | W9 快照时效制度 | 对应页面施工前先重导快照，不进排期项 |
| 3 | 本窗口 git 操作仍冻结（18:13 拍板 git 交其他 Agent） | 工作日志 | 本排期只写代码文件，commit/push/分支由峰宝安排 |

---

## 一、P0 主闭环补洞（回听闭环 + 诚实化，最先做）

| 项 | 内容 | 来源帧 | 依赖 | 涉及文件 | 验收标准 | 规模 |
|---|---|---|---|---|---|---|
| W0-1 ✅ | 后端补 `GET /api/v1/media/audio/{content_id}`（鉴权 + 流式响应）——**2026-09-02 01:3x 落地**：主树 `backend/app/api/media.py`（Bearer + IDOR 防护 + MEDIA_003；整文件直出，真流式留待 COS 后端）；py_compile + 导入级验证过、路由序实证在 `/{key:path}` 兜底之前；curl 实证待 W2-4 联调；矩阵已迁入新 worktree 并增补记录 | F5 拍板「本组补端点」 | 无 | 主树 `backend/app/api/media.py`（或新文件）+ openapi | curl 带 token 取回音频可播；无 token 401 | M |
| W0-2 ✅ | 就地播放三处接线：播放器封装（downloadFile 带 header + InnerAudioContext）+ index/search/detail 统一「播放钮变暂停 + 波形金色跳动 + 进度行」——**2026-09-02 21:0x 代码落地（worktree missing-pages + 主树 backend），真机验证待 W2-4 联调**：①播放引擎 `utils/audio_player.uts`（全局单例：同刻只播一条、同条暂停/续播、异条切换；downloadFile header 带 Bearer 落临时文件 → InnerAudioContext 播本地文件；会话级 fileCache；onTimeUpdate 驱动进度；回调签名对齐项目先例 `(res: any)`）②F5a 波形组件 `components/VoiceWave/`（运行时 props 声明防 PropType 泛型编译坑；已播 #C4913C/未播 #E5DCC8 + live bar 呼吸 scaleY 动画，CSS 动画不支持时优雅降级）③index/search/detail 三处接线（index 两处语音卡 + search n2_591 + detail F5b 语音卡 top 861.5rpx、后续四元素按 F5b 帧 558/700/852/882pt 下移 +211.6rpx 内联 style 免层叠歧义；三页 onUnload stopVoice 防跨页残留）④genWave 三处收口 audio_player.genWaveHeights ⑤后端 timeline 下发 voice 成员（主树 events.py `_batch_event_voices` ROW_NUMBER 防 N+1 + VoiceInfo 补 content_id 键；客户端 timeline.uts parseVoice + buildDayGroups 补拷贝防 photoIds 前科）| F5a/F5b | W0-1（联调）；开发可并行 | 新组件 `components/VoicePlayer/` + `index.uvue:268` + `search.uvue:330` + `detail.uvue` 语音区 | 真机点播放真出声，三处视觉一致（代码侧视觉已按 F5a 对齐；真机声验待联调） | M |
| W0-3 | RecordSheet form-layer 全还原：语音轮盘四态（F1a-c + CSS 双层螺旋动效）+ 标注态 F1d + 文字 F1e/f + 照片 F1g-k（网格自适应 302/146/96 档位） | F1 全谱系 11 帧 | 无（纯前端） | `components/RecordSheet/RecordSheet.uvue`（现 692-880 自拼样式整体替换） | 与 F1 帧逐帧对拍；录音→标注→保存全链真机走通 | L |
| W0-4 | ✅ **已完成（2026-09-02 01:2x，worktree missing-pages）** 占位诚实化一炉：detail 四按钮 + security 状态化（显「手机号登录/未绑定」不显假号）+ profile 错绑修复（:47 `@tap=goPrivacy`→`goStorageBackup`）+ ai 附件文案 | F7/F8 拍板 + audit §2 | 无 | `detail.uvue:156-199`、`security.uvue:77-86`、`profile.uvue:47`、`ai.uvue:300` | ✅ grep「开发中/待接入」仅剩 search:335（W0-2）/messages:163（W1-3）两条待波次交代 | S |

> P0 逻辑：W0-2+W0-3 合力修「录音能录不能回听」+「记录页最丑」两大断链；W0-4 零依赖可随时插队。

## 二、P1 新页面落地（audit 缺口 4 页 + F9/F6/F7 功能页）

| 项 | 内容 | 来源帧 | 依赖 | 涉及文件 | 验收标准 | 规模 |
|---|---|---|---|---|---|---|
| W1-1 | 存储与备份页（四卡：用量环图/同步状态只读/回收站入口/导出）+ 回收站二级列表页 | F2a/F2b | 无（导出复用 US-42 已验证链路） | 新页 `pages/storage/*` + pages.json + profile 入口接线 | 与 F2a/F2b 对拍；导出真机可用 | M |
| W1-2 | 隐私政策页（章节锚点）+ 关于页 | F3a/F3b | 文案已有（`docs/隐私政策草稿_v0.1`） | 新页 `pages/privacy/*`、`pages/about/*` + profile goPrivacy/goAbout 接线 | 与 F3 帧对拍；合规上架硬门槛解除 | S |
| W1-3 | 回响弹层（先弹层再进详情）+ 消息按类型路由（关怀→AI 页 / 回响→弹层 / 复盘·语音完成→就地展开） | F6a | 无（GET /echo/today + dismiss 真端点已通） | `messages.uvue:162` openFeatured + 新弹层组件 | 真机回响卡弹层出图，「今年不再提醒」dismiss 真生效 | M |
| W1-4 | 主题详情页（fetchTimeline 全量拉回前端按 L2 标题过滤，零后端）+ manage「重新生成」诚实化为「刷新画像」 | F9a | 无 | 新页 `pages/portrait/theme-detail` + `manage.uvue:390` onThemeTap/onRefreshProfile | 主题 chips 点击真进详情流；刷新重拉真数据 | S |
| W1-5 | 编辑标签面板（L1 索引层 + 手动固定 locked）+ 画像隐私设置页（参与开关 UI 本地态先行 + 清除入口强确认 + 敏感话题入口） | F9b/F9c | 无（locked 后端字段已入远期账，前端先落） | `manage.uvue` + 新页/弹层 | 与 F9b/F9c 对拍；开关/清除交互完整 | M |
| W1-6 | 分享面板 + 保存卡片图（canvas 渲染三模板长图→相册；微信/朋友圈/复制链接灰显占位） | F7a | 无 | `detail.uvue` share + 分享组件 + 三模板 | 真机保存到相册出完整品牌卡 | M |
| W1-7 | 收藏页前端（时间轴卡片流 + 类型筛选 chips + 星标 + 空态） | F4a/F4b | 数据依赖 W2-2 favorite 端点（先用占位数据开发，后切真） | 新页 `pages/favorites/*` + detail 收藏持久化 | 与 F4a/F4b 对拍；端点好后收藏→列表全通 | M |
| W1-8 | 时间胶囊面板前端（时机四选项 + 封存确认，纯 UI） | F7b | 联调依赖 W2-3（端点未好前保存走占位提示） | `detail.uvue` addCapsule + 弹层组件 | 与 F7b 对拍；端点好后封存真持久化 | S |

## 三、P2 后端契约补齐（可与 P1 并行，联调前完成）

| 项 | 内容 | 来源 | 依赖 | 验收标准 | 规模 |
|---|---|---|---|---|---|
| W2-1 | 内容删除端点（软删除 30 天）+ **App 端删除链路设计文档补写**（US-49 是同步层软删，用户故事清单零定义——实锤漏项） | F7 拍板 / A3 | 无 | detail 删除→软删→回收站（W1-1）可见 | M |
| W2-2 | 收藏 favorite 端点（全后端 grep 零命中实锤） | F4 拍板 / A2 | 无 | detail 收藏持久化 + W1-7 列表真数据 | S |
| W2-3 | 时间胶囊端点（封存/查询/到期开启；**全量含到期推送**——uni-push 链路一并实现，不留后续） | 新发现 / 远期总账 A5（2026-09-02 01:09 峰宝拍板全量） | 无 | W1-8 封存后可查询；到期后胶囊可开启且推送可达 | L |
| W2-4 | 后端起服务 + `curl /openapi.json` 全量实证对拍 `_resource_endpoint_matrix.md` | 悬账 C1 铁律 | 后端进程可用 | 47+ 路由逐项三态回刷矩阵 | S |

## 四、P3 真机验收收口（全量完成后的关卡）

| 项 | 内容 | 验收标准 |
|---|---|---|
| W3-1 | 灌真数据前关闭 mock 开关：`index.uvue:277/279 USE_MOCK_TIMELINE`、`search.uvue:84 USE_MOCK_DATA`（AI 契约接线不在本排期，`USE_MOCK_CHAT` 保持并登记） | grep 三开关状态与拍板一致 |
| W3-2 | deploy_one.sh 推包（1.0.41 真身 adb + 25 分钟轮询硬上限）→ 人肉验机清单交付峰宝（验机期严禁占 adb） | 25 帧逐帧对拍通过 + 三查审计 20 处占位全部清零或有真实交代 |
| W3-3 | 收口四件：台账 `_diff_ledger.md` 登记 / `_resource_endpoint_matrix.md` 回刷 / `progress.md` 时间线 / 远期总账打勾 | 四文件全部更新，无静默完成 |

## 五、依赖关系速览

```
W0-1(音频端点) ──→ W0-2(就地播放)      W0-3(表单层)/W0-4(诚实化) 独立随时做
W2-2(favorite) ──→ W1-7(收藏页联调)     W1-7 前端可先行
W2-3(胶囊端点) ──→ W1-8(胶囊联调)       W1-8 UI 可先行
W2-1(删除) ──→ W1-1 回收站真数据        W2-4(openapi 实证) 后端全部就绪后收口
P0 + P1 + P2 全部 ──→ W3 真机验收收口
```

## 六、明确不进本排期（远期总账对照，防误入）

- **A1** LLM 动态打标（chips 真值）/ **A4** 评论感想体系 —— 待后端排期
- **B1** 问AI带上下文 / **B2** 分享真实唤起 / **B3** 胶囊 AI 推荐 / **B4** users/me 掩码 / **B5** 绑定管理 —— 触发条件未到
- **C2** 录音中断实测 / **C3** 内容安全接入 / **C4**（=A2）—— 独立验收线
- F9 locked 后端字段 / 画像参与开关后端 —— F9 组拍板已入远期账
- 4b/UTS 插件线（fix4b 窗口）、git 操作 —— 其他窗口负责

## 七、范围项拍板记录（2026-09-02 01:09 峰宝圈定）

1. **W2-3 胶囊端点**：不留后续，**全量实现含到期推送**（uni-push 链路一并做），规模升 L。
2. **施工分支**：client 侧开**新 worktree `D:/GuangH-App/.wt/missing-pages`**（分支 `feature/missing-pages-impl`，基于 develop）；后端改动仍在主树 `backend/`（唯一来源）。
