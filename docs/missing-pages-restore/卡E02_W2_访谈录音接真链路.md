# 卡E02 · W2 访谈录音接真链路（并行开发卡）

> 来源：执行计划 v2 W2 波；2026-09-04 峰宝批准并行开发模式。
> **状态**：🟦 施工中 → 完成后主控改 ✅

## 独占文件域（禁改其他任何文件）

- `client/pages/interview/interview.uvue`
- `client/utils/voice.uts`（仅当接线确需小改时才动，改动最小化并在报告说明）

只读参考：`client/components/RecordSheet/RecordSheet.uvue:983-1072`（现成语音链路调用写法）

## 公共禁区（对齐 00 公共铁律「并行卡铁律」节）

1. 禁一切 git 写操作——代码改完留着，主控统一提交
2. 禁编译推包（HBuilderX CLI/adb 不碰）、禁 deploy 脚本
3. 不改 `_diff_ledger.md` / `progress.md` / `feature_list.json` / `AGENTS.md`
4. 禁止新增 mock/假数据

## 任务

把记忆访谈页的假录音器换成真语音链路：录音→转写→答案真文本。

**背景**：interview.uvue:129-151 现状是 setInterval 计秒的假录音器（从未调录音 API），停止后 `answers[k]='（语音回答 X 秒）'` 占位字符串。全链路 `utils/voice.uts` 已完整存活（startRecord/stopRecord/transcribeWav→POST /api/v1/asr/transcribe，响应含 text/emotion），后端零改动。

**改动点**：
1. interview.uvue 引入 voice.uts：onMicTap 开始真录音（startRecord，wav 产物）
2. 停止→stopRecord→transcribeWav(路径)→`answers[k]=res.text`（替换占位字符串）
3. 转写失败：诚实 toast + 保留 textAnswer 文字输入兜底（现有输入框），不许静默
4. 保留 voice.uts 500ms 最短录音守卫语义；录音中 UI 沿用现有「正在录音 X″」提示态
5. 可选：res.emotion 有值时在答案区轻量展示情绪标签（对齐 RecordSheet chip 样式，非必须）
6. voice.uts 仅接线必需时小改（如失败回调缺失），最小化并报告

## uvue 铁律（施工中强制）

1. 同文件多处修改必须串行 Edit，每刀后 grep 验刀
2. CSS 禁复合/后代/伪类选择器
3. 新语法先 Grep 项目先例；对象一律 class+constructor+new
4. onUnload/onBackPress 清理录音资源防泄漏

## 自查清单（完成定义）

- [ ] grep startRecord/transcribeWav 真实调用链（在 onMicTap/onStop 用户事件里真触发）
- [ ] grep 确认「语音回答 X 秒」占位字符串清零
- [ ] 对照 RecordSheet:983-1072 核对参数顺序与错误分支
- [ ] `git status` 确认仅触碰独占文件域
- [ ] 输出结构化报告：改动文件:行号清单、每刀验证证据、未解决问题
