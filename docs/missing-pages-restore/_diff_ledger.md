# 缺失页面返工 · 降级台账

> 每条降级标注卡号 + 帧号。集成 Agent 据此评估还原度。

---

## 卡05 · F1e 写几句·空态

### D-05-1 · 正文占位文案换行
- **JSON 真值**：`今天遇到了什么想记下来的？\n比如：陪妈逛了趟菜市场，她挑了茄子`（两行，lineHeight 28px）
- **实现**：textarea placeholder 单行 `今天遇到了什么想记下来的？比如：陪妈逛了趟菜市场，她挑了茄子`（空格分隔）
- **原因**：uni-app x textarea placeholder 对 `\n` 支持不确定；双行占位需 overlay 方案，超出本卡范围
- **影响**：占位文案视觉上少一行换行，信息完整

### D-05-2 · 时钟/地点图标为 CSS 几何近似
- **JSON 真值**：VECTOR 节点（Figma 导出精确 SVG path，时钟=圆+指针，地点=标准 pin 水滴形）
- **实现**：纯 CSS view 绘制——时钟=圆边框+竖线(时针)+横线(分针)；地点=圆环+中心圆点
- **原因**：白名单仅允许 components/RecordSheet/ 下新增文件，无法在 static/icons/ 新增 SVG；hens-svg 模块未验证可用性
- **影响**：图标可识别但非像素级还原；地点 pin 缺少水滴尖角

### D-05-3 · 时间/地点为静态 mock 值
- **JSON 真值**：`今天 17:42` / `上海 · 江边`（设计稿静态值）
- **实现**：`timeDisplay` / `locationDisplay` ref 初始化为设计值，未接设备时间/GPS
- **原因**：铁律 §5 mock 模式；设备时间/GPS 接入属功能卡范围，非 UI 还原卡
- **影响**：显示固定值，真机上不会随时间/位置变化

### D-05-4 · 分类 chips 宽度为 padding 自适应
- **JSON 真值**：固定宽度 96/112/64/34px（日常生活/家人与朋友/旅行/＋）
- **实现**：CategoryChips 子组件用 padding-left/right 自适应宽度，＋ 按钮固定 65rpx
- **原因**：子组件需复用 F1d（分类标签不同），固定宽度不通用
- **影响**：chip 宽度与设计稿偏差 ±5rpx，视觉差异极小

### D-05-5 · RECORD 冲突组并发覆盖
- **情况**：本卡执行期间，同文件 RecordSheet.uvue 被其他 Agent（F1a/F1h/F1i）多次覆盖，导致 Phase 1 改动（import/data/form-save v-if）丢失，需二次补写
- **当前状态**：全部 F1e 改动已基于最新文件状态重新应用并验证
- **集成风险**：若其他 Agent 在本卡之后再次覆盖，F1e 改动可能再次丢失；建议集成 Agent 合并时逐条核对 F1e 标记块
- **铁律违反风险**：冲突组要求同一 Agent 串行，当前多 Agent 并行为既定项目节奏，非本卡可控

---


## 卡10 · F1j 照片·三图（3/9）

### D-10-1 · 网格从"均分算法"改为逐帧真值定制
- **JSON 真值**：3 张 = 单行 96px/张、间距 7px、圆角 8px、卡片 342x136
- **实现**：新增大小图双模式——小图(3/5-9张)=185rpx/间距13/圆角15/卡片高262rpx；大图(2/4张)=281rpx/间距19/圆角19。删除旧 PHOTO_GRID_GAP 均分逻辑（旧算法 3 张算出 181rpx 与真值 96px 不符）
- **影响**：F1i(2张) 仍为 281rpx 大图单行，与既有一致；此改动让 3/5-9 张网格对齐画布

### D-10-2 · 删除角标动态化
- **JSON 真值**：小图模式 18x18px、top 4px、right 4px、圆角9、fill rgba(58,46,37,0.6)；大图模式 20x20px、top 6px、right 4px、圆角10
- **实现**：photoDeleteSize/Top/Radius/Font 由 computed 派生（小图35rpx/上8/圆17，大图38rpx/上12/圆19），× 字号统一 23rpx（12px）
- **影响**：替换原固定 38rpx/12rpx 角标；F1i 大图角标值与既有一致

### D-10-3 · 确定钮改为随内容流放置
- **JSON 真值**：确定钮位于信息卡之后（F1j canvas y=550），非固定底栏
- **实现**：photo-confirm 由 absolute bottom 改为 margin-top 38rpx 随流；删除 180rpx 占位，改 140rpx 底部留白
- **影响**：共享模板改动，F1h/F1i/F1k 确定钮同样从"贴底"变为"随流"，更贴合各自画布（F1h/F1k y=756）

### D-10-4 · 新增照片态状态栏（9:41 + 电池）与 Home 指示条
- **JSON 真值**：状态栏时间(24,16)15 Medium #3A2E25、电池(342,20)24x12 r4、Home指示条(125,828)140x5 r3
- **实现**：photo-head 内新增 .ph-status-time/.ph-status-battery（绝对定位，与 F1a/F1c 同值），photo-layout 增 .photo-home-indicator；photo-head padding-top 24→115rpx 使导航行落到 y=60px
- **影响**：此前照片态无状态栏（真值缺口）；本改动惠及全部照片帧 F1h/F1i/F1j/F1k

### D-10-5 · 添加格条件 4→5
- **JSON 真值**：4 张为 2×2 大图（无添加格，见 F1k），6 张九宫格才显示 ＋（见 F1h）
- **实现**：v-if 由 `>=4 && <9` 改为 `>=5 && <9`
- **影响**：4 张时不再显示添加格，对齐 F1k

### D-10-6 · Mock 张数可切换
- **实现**：新增 USE_MOCK_PHOTO_COUNT，buildMockPhotoList() 按张数轮换两张示例图；默认 4（F1k 演示态），改 3 即可对照 F1j、改 2 对照 F1i、改 6 对照 F1h
- **影响**：发版前 grep USE_MOCK_ 统一清理（总账 B8）

---

## 卡11 · F1k 照片·四图田字（4/9）

### D-11-1 · 4 张 = 2×2 大图网格
- **JSON 真值**：2×2 网格 146px/张、间距 10px、圆角 10px、卡片 342x342
- **实现**：大图模式（photoIsSmall=false）281rpx/间距19/圆角19/卡片高 658rpx；flex-wrap 自然排布 2×2
- **影响**：与旧实现"4张按3列181rpx"完全不同，已按画布修正

### D-11-2 · 删除角标大图规格
- **JSON 真值**：20x20px、top 6px、right 4px、圆角10、opacity0.6
- **实现**：photoDeleteSize=38rpx/top12/圆19；与 F1i 既有角标一致
- **影响**：无回归

### D-11-3 · 确定钮随流 y=756
- **JSON 真值**：信息卡(620..736)之后，确定钮 y=756
- **实现**：随 D-10-3 共享改动，margin-top 38rpx（≈20px 间距，画布 20px）
- **影响**：F1k 内容铺满至 810px，确定钮贴近 Home 指示条区域，与画布一致

### D-11-4 · 节标 照片·4/9
- **实现**：`照片 · {{ photoList.length }}/9` 动态文案（F1j=3/9、F1k=4/9 自动成立）
- **影响**：无

---

## 共享模板改动提示（影响 F1h/F1i）
RECORD 组照片帧共用模板。F1j/F1k 本次带来以下**共享改动**，集成 Agent 复核 F1h/F1i 时请注意：
1. photo-head 新增状态栏（9:41/电池），padding-top 24→115rpx（导航行对齐 y=60px）
2. 确定钮由固定底栏改为随内容流（F1h/F1i 画布中确定钮同样位于信息卡之后）
3. 删除角标/照片圆角/间距改为大小图双模式动态化（F1i 2 张仍是大图 281rpx，视觉不变）
4. 添加格条件 >=4 改为 >=5（影响 F1h 6 张仍显示＋，F1i 2 张本就不显示）
5. 新增照片态 Home 指示条
