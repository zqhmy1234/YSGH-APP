# 00 · 公共铁律（25 张帧卡共用，开工前必读）

> 2026-09-02 峰宝拍板：F 组 25 帧全部按画布 JSON 真值重新还原。旧三卡版
> `docs/缺失页面并行施工任务卡_20260902.md` **作废**（PNG 路线），本目录 25 卡取代。

## 1. 真值铁律（最高优先级）
- 每卡唯一真值 = `.wt/missing-pages/uvue_gen/<帧名>_canvas.json`（2026-09-02 14:30 从 ardot 文件 **719545763760845** 全树重导，readDepth 12，42 帧齐全）。
- 开工先逐节点读 JSON：层级/坐标/颜色/文案/gap/padding/字体全部以 JSON 为准。
- PNG（`/c/Users/ghf/.workbuddy/tmp/f*_export/`）只作辅助校验。**禁止凭规范推断/记忆还原**；「无 JSON 所以用 PNG」= 降级，必须先报告不可自作主张。

## 2. 换算与解析
- `SCALE = 750/390 = 1.9231`（画布 390 宽 → 750rpx）；画布 x/y 是**相对父节点**的，递归累加成绝对坐标。
- `gap` 引擎会静默丢弃 → 全部换算 margin；文本字体信息在节点顶层 `fontName/fontSize/lineHeight`。
- CSS 硬限制（无继承/渐变三参/复合选择器禁/box-shadow 可用）与 UTS 编译坑见技能 `ardot-to-uvue-css-restore`；一页一闭环/四关完成定义/八条铁律+铁律九见 `uvue-restore-sop`。

## 3. UTS 零先例禁令（写前先 Grep 项目先例，零先例一律不写）
type 字面量构造 / 函数默认参数 `= []` / 模板 `!.` 断言 / 三停靠渐变 / defineExpose / 复合 `.a.b`·后代·伪类选择器 / `0 as number`（写字面量）/ function 声明 sort（用箭头）/ 根类 font-family（每 text 自写）/ gap 属性（换 margin）。

## 4. 白名单与冻结
- 只准改卡内「目标文件白名单」列出的文件，**白名单外零字节**（含 pages.json / api.uts / manifest.json / pages.json 现有条目）。
- **git 全操作冻结**：不 add/commit/push/stash/checkout，改动留工作区，主窗口统一收口。
- **禁跑 cli 编译、禁碰 adb**——并发互杀；编译门由主窗口串行收口（vapor 三链验证法）。

## 5. mock 与诚实化
- 需要演示数据一律 `USE_MOCK_<域>` 常量模式：真数据优先，为空才注入演示值；发版前统一 grep 清理（总账 B8）。
- 一切降级/做不到，逐条追加登记 `.wt/missing-pages/_diff_ledger.md`（只准追加自己条目，禁改他人条目）。

## 6. 完成定义与汇报
- 完成 = 结构还原（帧节点全覆盖）→ 孤儿 CSS 0 → 对照 JSON 自审（每节点核对）→ 台账清零或有据降级。真机对拍统一 W3 由主窗口做。
- 每卡交回：①完成清单 ②降级清单（注明台账行）③ `git status` 白名单自查证明 ④一句话质量自评分（75 分线）。

## 7. 冲突组（同组必须同一 Agent 串行，禁止并行互踩）
| 组 | 卡 | 共享文件 |
|---|---|---|
| RECORD | F1a–F1k（11 卡） | components/RecordSheet/RecordSheet.uvue |
| FAV | F4a+F4b | pages/favorites/favorites.uvue |
| DETAIL | F5a+F5b+F7a+F7b | pages/detail/detail.uvue |
| MANAGE | F9b+F9c | pages/portrait/manage.uvue |
| 独占 | F2a/F2b/F3a/F3b/F6a/F9a | — |

## 8. 并行卡铁律（2026-09-04 增，E 系卡 E01~E05 专用，峰宝拍板「主控只管 git 提交审查」）

> E 系卡 = 执行计划 v2（`.wt/missing-pages/_execution_plan_20260904.md`）波次的并行开发卡，
> 与 §7 帧卡不同：E 系不按画布帧还原，按功能波次施工。除本节修订外，§1~§6 全部适用。

**文件域所有权矩阵（每卡只准改本卡域内文件，域外零字节）**：

| 卡 | 波次 | 独占可改文件域 |
|---|---|---|
| 主控（北嘎霖） | W0/收口 | 全部 git 操作、backend models.py（W0 cherry-pick）、台账/progress/feature_list/总账 |
| 卡E01 | W1 | client/pages/index/index.uvue |
| 卡E02 | W2 | client/pages/interview/interview.uvue + client/utils/voice.uts（最小改动） |
| 卡E03 | W3 | backend/app/**（export 新文件+main.py 注册行，models.py 禁碰）+ storage/privacy.uvue + api.uts |
| 卡E04 | W4 | client/pages/about/** + pages.json（仅加 agreement 路由行） |
| 卡E05 | W5 | client/pages/favorites/favorites.uvue + client/utils/config.uts |

**E 系补充铁律**：
1. 禁 git 全写操作（卡E03 允许 git log/show **只读**考古）；禁编译推包/adb；pytest 只写不跑（主控统一执行）
2. 禁改 `_diff_ledger.md` / `progress.md` / `feature_list.json` / `AGENTS.md`——要登记的事写进卡报告，主控代录
3. 禁止新增 mock；删除 mock 须 grep 全项目零残余后才算完成
4. 交回物=改动文件:行号清单 + 每刀 grep 验证证据 + git status 白名单自查 + 未解决问题清单
5. 卡面标记的「待峰宝拍板」项一律不做（越权=返工）
