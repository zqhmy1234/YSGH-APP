# 卡E04 · W4 用户协议页（并行开发卡）

> 来源：执行计划 v2 W4 波；2026-09-04 峰宝批准并行开发模式。
> **状态**：🟦 施工中 → 完成后主控改 ✅

## 独占文件域（禁改其他任何文件）

- `client/pages/about/**`（新建 agreement.uvue + about.uvue）
- `client/pages.json`（仅新增路由行）

只读参考：`client/pages/privacy/privacy.uvue`（PolicyBlock 版式）

## 公共禁区（对齐 00 公共铁律「并行卡铁律」节）

1. 禁一切 git 写操作——代码改完留着，主控统一提交
2. 禁编译推包、禁 deploy 脚本
3. 不改 `_diff_ledger.md` / `progress.md` / `feature_list.json` / `AGENTS.md`
4. pages.json 只在 pages 数组新增 agreement 一行，禁动其他行

## 任务

开发「用户协议」内容页，about 页入口从诚实 toast 变真跳转。

**背景**：about.uvue:70 现为 toast「用户协议待定稿后上线」；隐私政策页 privacy.uvue 已有成熟版式（PolicyBlock 结构+滚动容器）可复用。

**改动点**：
1. 复用 privacy.uvue 版式新建 `pages/about/agreement.uvue`，头部注释标真值来源与草稿性质
2. 协议文案草稿（记忆整理 APP 场景，用户可读中文，节标题+条款结构）：服务说明 / 数据收集范围（照片·文字·语音碎片）/ 数据存储与安全（本地 SQLite+云端加密+不向第三方出售）/ 用户权利（查看·导出·更正·删除）/ 未成年人保护 / 协议变更与生效 / 联系渠道。文案=草稿供峰宝审，页内正常展示
3. pages.json pages 数组新增 agreement 路由（风格对齐现有条目）
4. about.uvue:70 改 `navigateTo('/pages/about/agreement')`，删占位 toast

## uvue 铁律（施工中强制）

1. 同文件多处修改串行 Edit，每刀 grep 验刀
2. CSS 禁复合/后代/伪类选择器；样式对齐 privacy.uvue 既有类
3. 新语法先 Grep 项目先例

## 自查清单（完成定义）

- [ ] grep agreement：路由注册+about 跳转调用都在
- [ ] PolicyBlock 复用无孤儿样式；导航栏返回可用
- [ ] `git status` 确认仅触碰独占文件域
- [ ] 输出结构化报告：改动文件:行号清单、文案节结构、每刀验证证据、未解决问题
