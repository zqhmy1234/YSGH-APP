# 卡E03 · W3 导出端点恢复+接线（并行开发卡）

> 来源：执行计划 v2 W3 波；2026-09-04 峰宝批准并行开发模式。总账 A9 关单卡。
> **状态**：🟦 施工中 → 完成后主控改 ✅

## 独占文件域（禁改其他任何文件）

- `backend/app/**`（export 相关新文件 + main.py 注册行）
- `client/pages/storage/storage.uvue`
- `client/pages/privacy/privacy.uvue`
- `client/utils/api.uts`

只读参考：git 历史（旧 /api/v1/export 实现）、`docs/远期待办总账.md` A9 行

## 公共禁区（对齐 00 公共铁律「并行卡铁律」节）

1. 禁一切 git **写**操作（git log/show 只读考古允许）——代码改完留着，主控统一提交
2. 禁编译推包、禁 deploy 脚本；pytest 编写允许，**执行由主控统一跑**
3. 不改 `_diff_ledger.md` / `progress.md` / `feature_list.json` / `AGENTS.md`
4. main.py 只加注册行，不动其他区域；models.py 一律不碰（主控 W0 cherry-pick 在改）

## 任务

恢复被 W10.4 换页移除的 `/api/v1/export` 端点并接通前端导出按钮（总账 A9 关单）。

**背景**：旧端点曾验证可用（US-42 拍板链路），随 W10.4 换页被删；storage.uvue:186 onExport 现为诚实 toast「待后端排期」。用户确认旧实现存在过——先 git 考古再重建。

**改动点**：
1. 考古：`git log --all -- '**/export*'` 找旧实现（主仓库 D:\GuangH-App），评估复刻 or 重写（只读）
2. 后端重建 `/api/v1/export`：导出当前用户全部记忆数据（events/contents/tags 等打包 JSON 下载），复用现有鉴权与表模型，token 鉴权对齐其他 router 写法
3. `backend/app/main.py` 注册新 router（只加注册行）
4. `client/utils/api.uts` 加 PATH_EXPORT 常量 + 导出请求函数（header 带 token，对齐现有封装范式）
5. `storage.uvue:186` onExport 改真调用：下载进度/成功 toast、失败诚实提示
6. `privacy.uvue:87`「数据导出…（即将上线）」文案去掉「（即将上线）」
7. 新增 export 端点 pytest 用例（鉴权 401/正常导出/空数据三例）

## 工程铁律

1. 同文件多处修改串行 Edit，每刀 grep 验刀
2. Python 语法自查（py_compile）+ ruff 无新增违规
3. 密钥纪律：不硬编码任何凭证

## 自查清单（完成定义）

- [ ] 报告给出 `curl /openapi.json` 预期新增 1 路由的 diff 说明
- [ ] grep PATH_EXPORT 引用链完整（常量→函数→storage 调用点）
- [ ] pytest 三用例文件就位（执行留给主控）
- [ ] `git status` 确认仅触碰独占文件域
- [ ] 输出结构化报告：改动文件:行号清单、考古结论（旧实现 SHA 与复用度）、每刀验证证据、未解决问题
