# 忆述光华 · 客户端微观任务拆解（大规模 Plan · 2026-08-24）

> 宏观计划（三波）→ 本文件为可执行任务卡。每张卡含：编号/任务/说明/依赖/验证/预估。
> 分工：北嘎霖写码，峰宝 HBuilderX 编译 + 真机部署 + 验收。任务编号：`<波次>-<模块>-<序号>`。
> 波次代号：B=第一波(W3-4) / S=第二波(W5-10) / T=第三波(W11-14)。

---

## 第一波（W3-4）：F1 数据链路 + F8 时间轴

### 模块 B-BE 后端（multipart 端点）
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| B-BE-1 | 新增 `POST /api/v1/contents/upload` | multipart（file 照片 + meta JSON：taken_at/gps/perceptual_hash/source）→ storage 存 COS（dev minio/fake）→ contents 建记录(cos_key) → enqueue_high(process_content) | — | curl 冒烟：上传→落库→管线触发 | 0.5d |
| B-BE-2 | 元数据校验 + 去重 + 护栏 | 复用 contents.py 逻辑（perceptual_hash 409 / moderate 护栏 / 类型白名单） | B-BE-1 | 单测覆盖 409/护栏/鉴权 | 0.5d |
| B-BE-3 | pytest 补全 | upload 端点单测（成功/去重/护栏/未授权）+ 全量回归 | B-BE-2 | pytest 全绿（215+新） | 0.5d |

### 模块 B-CL 客户端骨架
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| B-CL-1 | HBuilderX uni-app x 工程初始化 | D:\GuangH-App\client\ 空工程（manifest/pages.json/static/）；**HBuilderX 中创建并验证能运行** | HBuilderX 5.15 | HBuilderX 打开→运行到模拟器不报错 | 0.5d |
| B-CL-2 | 环境配置 | baseURL 开关（dev=本机：模拟器 10.0.2.2:8000 / 真机局域网 IP）；mock 开关 | B-CL-1 | 配置切换生效 | 0.2d |
| B-CL-3 | 认证封装 | mock wechat login（code=dev-client）→ token 对；加密 SharedPreferences 存 token；请求拦截带 Bearer + 401 自动 refresh | B-CL-1 | 调通任一受保护 API | 0.5d |
| B-CL-4 | 网络层封装 | 统一请求（超时/错误码/全局 toast/ApiError 映射） | B-CL-3 | 错误场景 toast 正确 | 0.5d |

### 模块 B-UT UTS 插件（相册监听）
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| B-UT-1 | 插件骨架 | uni_modules/yishu-photo-watch：interface.uts + app-android/index.uts + AndroidManifest（READ_MEDIA_IMAGES 等权限） | B-CL-1 | HBuilderX 编译通过 | 0.3d |
| B-UT-2 | PhotoObserver 平移 | research/poc/android PhotoObserver.kt → utssdk/app-android/（Hybrid Mode），ContentObserver + MEDIA_SCANNER 兼容 | B-UT-1 | 真机 logcat 见回调 | 0.5d |
| B-UT-3 | 游标去重 | SharedPreferences last_seen（照片 id/time），已处理跳过 | B-UT-2 | 二次启动不重复触发 | 0.3d |
| B-UT-4 | 静默窗口攒批 | 3-5s 无新照片→回调 uvue 层（IO 线程） | B-UT-2 | 连拍折成 1 批 | 0.3d |
| B-UT-5 | 自定义基座 + 真机验证 | HBuilderX 制作自定义基座（权限一次配全），nova 11 部署 | B-UT-1..4 | **nova 11 需插上** | 0.5d |

### 模块 B-UP 上传链路
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| B-UP-1 | 逐张 multipart 上传 | 3 并发、失败重试 2 次、进度回调 | B-BE-3, B-UT-4 | 50 张全部上传成功 | 0.5d |
| B-UP-2 | 传完刷新 | 全部完成→拉 timeline 刷新 | B-UP-1 | 上传完时间轴自动更新 | 0.3d |

### 模块 B-F8 时间轴 UI
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| B-F8-1 | 时间轴页面 | L1 日卡片 + L2 主题分组；UI 规范落地（撕边卡片/底部投影/锈红强调/≥30% 留白） | B-CL-4 | 真机截图 | 1d |
| B-F8-2 | timeline API 对接 | GET /api/v1/events/timeline（level 过滤）+ 游标分页 | B-BE-3 | 数据正确渲染 | 0.5d |
| B-F8-3 | 空状态 | 空白相纸插画 + 文案 + 引导动作 | B-F8-1 | 空数据页美观 | 0.3d |
| B-F8-4 | 前端自审 | 截图自审铁律：配色/留白/做旧/可点性 | B-F8-1..3 | 自审通过（≥75 分） | 0.2d |

### 模块 B-VA 验收
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| B-VA-1 | 测试照片注入 | generate_test_photos.py 生成 50 张 → adb push → MEDIA_SCANNER 广播 | B-UT-5 | POC-01 路径复现 | 0.3d |
| B-VA-2 | 30s 首批事件门禁 | 计时：授权相册→首批事件 ≤30s | B-UP-2 | 达标 | 0.2d |
| B-VA-3 | L1/L2 正确率抽查 | 对照生成集真值 | B-VA-2 | 达标（门禁 ≥ 阈值） | 0.3d |
| B-VA-4 | harness 收尾 | feature_list（F1/F8 客户端 DoD）+ progress 更新 + review_agent 全绿 | 全部 | review_agent passed | 0.2d |

**第一波合计：22 任务 · ~8 人日**

---

## 第二波（W5-10）：离线 + 同步

### 模块 S-AG 端侧聚合（UTS ST-DBSCAN）
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-AG-1 | UTS 算法层移植 | backend event_aggregation pipeline（预处理）+ st_dbscan（L0）+ 日聚合（L1）→ UTS 纯计算层 | 第一波完 | UTS 单测 | 2d |
| S-AG-2 | AGG-016 一致性单测 | 同一组用例 Python vs UTS 双跑，簇成员/日卡片一致 | S-AG-1 | 一致性全过 | 0.5d |
| S-AG-3 | Kotlin 桥接 | 相册读取/EXIF/定位元数据 → 算法层输入 | S-AG-1 | 真机聚合出卡片 | 0.5d |
| S-AG-4 | 增量触发 | 新照片→受影响时间窗重跑（不全量），结果进 XView | S-AG-3, S-XV | 增量正确 | 0.5d |

### 模块 S-EM 模拟器
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-EM-1 | 安装 system-image + AVD | Android 16 镜像（~1-2GB），AVD 创建 | — | 模拟器启动，adb 可见 | 0.5d |

### 模块 S-XV XView
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-XV-1 | SQLCipher 集成 | config.json 依赖 + UTS 桥接（POC-04 已验证） | — | 密文无明文/错钥拒绝 | 0.5d |
| S-XV-2 | 5 表结构 + 迁移 | events/photo_event/op_log/sync_cursor/photo_index + schema_version | S-XV-1 | 迁移脚本跑通 | 0.5d |
| S-XV-3 | 轻量 DAO | CRUD 封装 | S-XV-2 | DAO 单测 | 0.5d |
| S-XV-4 | 读写接线 | 端侧事件/照片索引/游标落 XView | S-XV-3, S-AG-4 | 离线数据可读回 | 0.5d |

### 模块 S-SY 同步
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-SY-1 | 后端 `POST /api/v1/events/sync` | 端侧 L1 事件批量提交，client_event_id 幂等 | — | pytest 幂等测试 | 1d |
| S-SY-2 | 后端 aggregate_user 重构 | 收端侧事件→关联照片→只跑 L2/L3；caption/CI 保留 | S-SY-1 | 全量 pytest 回归 | 1d |
| S-SY-3 | 第一波数据基线迁移 | 端侧初始化拉云侧 events（含 L1）为基线落 XView | S-SY-2, S-XV-4 | 基线一致 | 0.5d |
| S-SY-4 | 客户端同步协议 | op_log 队列（op_id 幂等）/指数退避/状态分级/游标 | S-SY-1 | 断网重连补传正确 | 1d |
| S-SY-5 | 三路触发 | 前台/相册监听/2h 定时（WorkManager UTS 桥接） | S-SY-4 | 三路均可触发 | 0.5d |
| S-SY-6 | LWW 冲突处理 | 字段级 LWW + 云端胜 + 提示（不弹窗） | S-SY-4 | 双端改同字段→云端胜 | 0.5d |

### 模块 S-ST STS/分片
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-ST-1 | 客户端 STS 直传 + 分片续传 | 后端 /upload/* 已就绪；客户端换上传函数（直传 COS 分片/断点） | B-UP-1, S-SY | 断电续传实测 | 1d |

### 模块 S-MO 手动操作
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-MO-1 | merge/split/confirm UI | 事件操作（后端 API 已就绪）+ 离线走 op_log | S-SY-4 | 操作 UI 自审 + 双端一致 | 1d |

### 模块 S-VA 验收（M4 前置）
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| S-VA-1 | 断网聚合→重连同步 E2E | 飞行模式聚卡片→联网→补传→L2/L3 补全 | 全部 | 全链路无丢失 | 0.5d |
| S-VA-2 | 双端一致验收 | 真机 nova 11 + 模拟器，同账号同数据对比 | S-EM-1 | 端间一致 | 0.5d |
| S-VA-3 | 断电续传实测 | 上传中断→恢复→续传成功 | S-ST-1 | 断点正确 | 0.3d |
| S-VA-4 | M4 前置门禁 + harness | feature_list/progress/review_agent | 全部 | 门禁达标 | 0.3d |

**第二波合计：21 任务 · ~12 人日**

---

## 第三波（W11-14）：多入口 + 玩法层

### 模块 T-NA 导航
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| T-NA-1 | 四宫格 Tab | 时间轴/记录/搜索/我的；iconfont 图标（相册页/铅笔纸/放大镜/印章）选中态变色 | B-F8-1 | 真机截图 | 0.5d |

### 模块 T-TX 文字入口
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| T-TX-1 | 文字记录页 | 输入→POST /contents(text)→分类异步 job 轮询→分类标签展示 | T-NA-1 | 分类正确 | 0.5d |
| T-TX-2 | 纠错触点 | 分类标签可点改→三层裁决（异步+轮询）→回写 | T-TX-1 | 纠错生效 | 0.5d |

### 模块 T-AU 语音入口
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| T-AU-1 | 录音 UI | 点击开始/停止、时长上限（≤8MB≈4min）、前台服务（POC-02） | T-NA-1 | 灭屏录音不中断 | 0.5d |
| T-AU-2 | ASR 上传转写 | POST /asr/transcribe→转写 loading→结果展示 | T-AU-1 | 转写正确 | 0.5d |
| T-AU-3 | 可编辑 + 情绪标签 | 转写可编辑→确认入库；情绪标签展示（关怀文案空态降级） | T-AU-2 | 编辑入库生效 | 0.5d |

### 模块 T-SR 搜索
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| T-SR-1 | 搜索页 | 搜索框 + 提交 → POST /search | T-NA-1 | 结果返回 | 0.5d |
| T-SR-2 | 结果卡片 + 溯源 | 事件/照片/文字混合卡片 + 来源高亮 | T-SR-1 | 溯源可见 | 0.5d |
| T-SR-3 | 以图搜图 | 相册选图 → POST /search/image | T-SR-1 | 图片命中 | 0.5d |
| T-SR-4 | 降级态 | degraded/errors 契约（后端待补，客户端先约定） | T-SR-1 | 降级提示正确 | 0.3d |

### 模块 T-PL 玩法层
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| T-PL-1 | 回响卡片 | 首页"去年今日"（GET /echo/today）+ 划掉（dismiss）；角贴+泛黄 | B-F8-1 | 每天≤1 条 | 0.5d |
| T-PL-2 | 冷启动访谈 | 首次启动三问（questions→answers→画像）+ 三层披露 + 可跳过 | T-NA-1 | 引导完整走通 | 0.5d |
| T-PL-3 | 消息中心 | 复盘 22:00 push + 回响 in-app 列表；应用内拉取（打开/切前台刷新） | T-PL-1 | 列表正确 | 0.5d |

### 模块 T-VA 验收
| 编号 | 任务 | 说明 | 依赖 | 验证 | 预估 |
|---|---|---|---|---|---|
| T-VA-1 | 全入口 E2E | 文字/语音/搜索/回响/访谈/消息全链路真机走查 | 全部 | 无阻断 | 0.5d |
| T-VA-2 | M5 就绪度自检 | 对照测试清单 P0 客户端项 + feature_list/progress/review_agent | 全部 | 就绪度达标 | 0.3d |

**第三波合计：15 任务 · ~8 人日**

---

## 第四波（W15+）：Windows F9（单列，不拆明细，另行规划）
- 范围：Electron + Python sidecar（PP-OCRv5 20MB）+ JSON-RPC 2.0 v1 + 批量导入 200/断点续传 + 三档降级 + AI 引擎适配器 + 删除告知（B5e 定稿）

---

## 执行纪律（沿用 AGENTS.md）
- 一次只做一个任务；未验证不声称完成
- 每个任务完成后：证据记入 feature_list/progress；提交走 review_agent（client/ 已排除 Python 工具链）
- UI 相关任务必须截图自审（≥75 分）
- 外部 Key 只经 Infisical；mock 开关（MOCK_EXTERNAL_AI）按环境
- 波次裁剪一律通过；决策裁剪才向峰宝说明

## 风险清单（跨波次）
1. nova 11 未连接 → 第一波 B-UT-5/B-VA 阻塞（峰宝插上）
2. 微信 appid/secret 缺失 → 正式登录/M3 收消息阻塞（合规申请待启动）
3. 团队真值数据（50 查询/碎片/录音/照片事件真值）→ 评测校准依赖
4. 模拟器镜像下载（S-EM-1，1-2GB）→ 第二波 W5 先装
5. UTS/Python 算法一致性（S-AG-2）→ 同参双跑单测兜底
6. aggregate_user 重构回归（S-SY-2）→ 全量 pytest 回归兜底
