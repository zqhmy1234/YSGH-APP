# 交接提示词：R2 云打包 → 真机复验 → R3 收口（接手即读）

> 2026-09-01 · 队长窗交接件。任务本体、卡点、排查路径、纪律全在此文；按序执行即可。

## 你是谁、任务是什么
接手「忆述光华」项目（工作目录 `D:\GuangH-App`）**4b 修复批次**的 R2/R3 阶段。前任已完成全部代码修复，手头三件事按序：
1. **解决云打包卡点**，出 fix/4b 的自定义基座 APK
2. **aapt 尸检验证 D-18/D-19**（不需要设备，先做）→ 设备回连后跑 R2 复验电池组
3. R2 全绿后走 R3 收口（merge/契约再生/数据清洗）

## 来龙去脉（一分钟版）
- 收尾真机验收挖出 20 张缺陷单（tracker19 §4 唯一来源），执行计划 `docs/4b修复批次执行计划_20260829.md`（用户批）分波次修
- 08-29：后端半区+原生能力对修完（fix/4b 分支，worktree `.wt/fix4b`）；期间「暗物质审计」揪出三单「已修+复验过但代码从未提交」（D-02/D-03、文案库 v1、U3/D4 弹窗链），前两单已补落，第三单悬置待拍板
- 09-01：wrap1 窗把 8 页像素还原 UI + **UTS 5.24 迁移**（utils 21 文件 .ts→.uts、Vapor 启用）合流 develop@`cea5025`；主账由队长窗追平（台账 §1.11/O-4）
- 随后 fix/4b **rebase 到新基线全绿**（8/8），解冻实施剩余客户端半五枚（D-22c/D-07/D-08/D-10/D-12/D-05/D-14），受影响测试与 12 页冷编译全过
- **现在就差云打包出包做真机复验——卡住了（见下）**

## 为什么必须云打包（不许绕）
D-19 修法 = FGS service 声明迁到**工程根 `client/AndroidManifest.xml`**（云打包唯一合并点；插件内 manifest 不参与，ask.dcloud.net.cn/question/214927 实证）；D-18 探测依赖 **nativeResources marker 资产**（只进云包）。**标准调试基座两者都不含——只有云包 APK 能证明修法成立**。这就是「已修待复验」里「复验」的定义。

## 当前棋盘（精确态）
```
develop   @ 5fdf591   # cea5025 合流 + 记账批 + R2 验证脚本；origin 已被他窗推送同步
fix/4b    @ 381a210   # worktree .wt/fix4b；全部提交见 git log develop..fix/4b（12 枚）
fix4b-pre-rebase @ acce7ef   # rebase 保险，R3 后议弃
salvage/main-worktree-dirty-20260829 @ 2bad295  # 禁删！他窗未认领工作（回捞命令在 tracker O-3）
```
- 容器 `yishu-redis`/`yishu-qdrant` UP（**机器重启会随 Docker Desktop 休眠，测试前先拉起**）；PG 原生服务 UP；8000 后端未起
- HBuilderX 5.24 在跑，登录态正常（`2957338852@qq.com`）
- **设备 `DKS9K23526028855`（nova 11，峰宝日常机）当前已从 adb 消失**——需用户重插 USB/确认授权（先 `adb kill-server; adb start-server` 排除 server 互杀）
- **主区在途勿碰**（媒体票据窗+企微合规窗的地盘）：`backend/app/api/{media.py,events.py}`、`core/{config,errors,main}.py`、`schemas/content.py`、`services/external/{storage.py,media_url.py}`、`docs/决策台账.md`(dirty)、`.trae/`、`client/uni_modules/hens-svg/`(IDE 回潮噪音)、`overview.md`、`B批_文字碎片标注表.xlsx`、`23_微信入口攻坚*`、`隐私政策草稿*`

## 卡点：cli pack 秒吐「打包终止」（你的第一个目标）
```powershell
& D:\HBuilderX\cli.exe pack --project "D:\GuangH-App\.wt\fix4b\client" `
  --platform android --iscustom true --android.packagename com.yishu.guanghua --ignoreWarnings true
```
观测：两次分别 2 分钟 / 即时打「打包终止」后进程 hang；无 APK；账号在线；08-28 同参数曾成功（但基线不同，见疑点 3）。**排查顺序**：
1. 屏幕右上有待确认通知条（按钮「我知道了」+ 勾选「不再提示」）——5.24 可能新增 GUI 确认/协议环节；先清它（screen_observe 快照 15s 过期，observe 后立即 click）
2. 工程树里有**三个同名 `client` 节点**（主仓/wrap1/fix4b）——在 IDE 里激活 fix4b 的工程再发；残留 cli/pack 进程先杀，**禁止并发打包**
3. **manifest 疑点**：08-28 出包成功是旧 manifest；wrap1 后来重写过 manifest.json。曾见 IDE 在旧检出上自动补写 `app-android.distribute` 结构（当时被当噪音丢弃 checkout 回滚了！）——可能 5.24 打包恰需要这份结构。对照 `git show cea5025:client/manifest.json` 与 IDE 期望形态
4. CLI 始终黑盒就直接走 IDE 菜单「发行→原生App-云打包」看 GUI 真实报错（最快），或按 `--config configure.json` 显式传配置（文档 https://hx.dcloud.net.cn/cli/pack）
5. 出包标志：`client/unpackage/debug/android_debug.apk` 落地（约 200MB 级，云队列 5-15 分钟）

## R2 流程（包出后，脚本已备好）
**先读** `scripts/realdevice/r2_4b_verify.ps1`（分阶段 autopsy→install→backend→logs→manual）与其内嵌十项验收口径。关键动作：
1. `-Stage autopsy`（**无设备也可跑**）：aapt 查 DataSyncService + FOREGROUND_SERVICE 权限 + `assets/yishu/workmanager-marker.txt` + **包体积**（字体 26.4MB 留包的实测，交用户拍剩余去向）
2. 请用户接回设备 → **先关 EMUI 纯净模式**（否则 install 静默失败零报错）→ `-Stage install`（**reverse 三连铁律：每次 cli launch 后必补 `adb reverse tcp:8000 tcp:8000`** + 设备侧 curl healthz）
3. `-Stage backend`：从 `.wt/fix4b/backend` 起 uvicorn + dev worker（Windows 无 scheduler 形态）——**占 8000 前先跟媒体窗打招呼**（他们的 media 端点只在主区，打到我这测不了）
4. `cli launch --playground custom --native-log true` 起 App → 十项电池组：D-18/19 日志（`initBackgroundTasks ok`）、D-16 情绪空态（chip 不渲染）、**D-22 纠错学习 DB 判据（correction_log `new_label`=所点标签非 mixed + `contents.content_class` 回写 + 同文本再分类命中 `layer=personal`）**、D-07/08 语音链（短录音回放/断网重试找回）、D-05/14 断网重传（held 计数=实数、补传后事件出现）、D-21 四页滚动、gap 目验（按 `docs/audit_20260831_全页面系统性审查.md` §四 P0 清单）、US-25 复判
5. 证据三要素（设备+时间/文件/判定）落 `scripts/realdevice/evidence/`（r2_* 命名；该目录铁律本地不入库，**别「顺手清理」**）
6. 全绿 → tracker19 各行关单 + 数字变动走**五处同步**规则（AGENTS/08 文档需授权/progress/handoff/feature_list）

## R3 收口（R2 后）
`review_agent --full` → merge fix/4b→develop → `docs/openapi.json` 再生（/asr/channels、ArbitrateRequest 增量、GET /contents content_id 过滤、VoiceInfo、媒体票据若已落）→ correction_log 污染清洗（脚本+**人工确认**，方案 `docs/P2诊断_O1O2_20260829.md` §2.3）→ 记账 → worktree 清理（dashscope 可删；fix4b-pre-rebase/salvage 议）。

## 待用户拍板（接手时挂着的）
- **U3/D4 弹窗**：整链从未提交（只活在 `git show 49bed24:client/utils/text_recorder.ts` L288-300 + 旧 record.uvue 调用点），台账 5.3「hook 已就绪」是幻影 → A 回炉移植 / B 二期 / C R2 后评（前任倾向 C）
- 台账 §5.1 部署就绪包确认 / §5.2 自定义分类目标页 / §5.8 AI 对话后端端点归宿（现为客户端 mock，contract-first）
- 字体 26.4MB 剩余去向（等包体积实测）；D-06 录音看门狗（批次 3，机型适配+来电场景等价复现已定案：抢麦脚本+去电模拟，无需第二设备）
- S2 情绪校准（待真值数据 C 批）

## 硬纪律（违者本项目必翻车）
1. 提交永远 pathspec 限定（`git add -- <paths>` + `git commit -m … -- <paths>`），**禁 `git add -A`/reset/stash**（共享脏主区）；提交后 `git show --stat` 自查
2. pre-commit 快门禁不可绕（`--no-verify` 禁用）；任何门禁失败后必须先 `python scripts/lessons.py add` 才能过下一枚提交
3. **新铁律：任何「已修」宣称必须含 git 入库存在性检查**（新文件未 add = diff 审查永久盲区，已栽三单）
4. **绝不 push**（origin 同步归用户/他窗）
5. 改 client/ 前读 `skills/hbuilderx-uniappx-runloop/SKILL.md`（§4 UTS 错误速查表、§5 CLI 军规、§6 adb/纯净模式/云打包参数/D-18/19 坑、§7 5.24 迁移与多窗协调）；真机链读 `skills/android-media-e2e/SKILL.md`（EXIF 子 IFD、scan_file 扳机、取证工具箱）
6. PS 地狱三坑：`stash@{0}` 花括号被吞（单引号包裹）；内联 `python -c` 带引号必坏（一律写 .py 文件再跑）；**同一消息对同一文件发两个写操作（禁令，本会话犯过三次）**
7. 文件编辑模式：bytes 读 → CRLF 探测 → 归一 LF → 锚点 assert count==1 → 替换 → 还原 EOL；字符串内引号用「」或 chr(39)，别裸嵌套
8. 内存闸：重活前查 `Get-CimInstance Win32_OperatingSystem` 的 `FreePhysicalMemory`（编译/全量测试需 ≥2GB）
9. 密钥只走 Infisical（`skills/infisical-secrets/SKILL.md`），永不回显值；企微可信 IP=本机出口（现值 61.171.241.17，CGNAT 换网即变，`curl ip.sb` 自查通报负责人；报 60020 就是这个）
10. 峰宝（用户）拍板一切重大事项；真机是其日常手机——操作前确认前台、勿打断使用、PIN 永不代输
11. worktree 注册可能被清理 Agent 误注销（本会话已历一次）：症状=`git worktree list` 缺条目/目录报 not a git repository；修法=挪开残骸 `git worktree add <path> <branch>` + 回迁 `.env`；教训=**备份物禁放 .cowork-temp（ignore 区=清理靶区）**

## 本窗最后状态快照
R1 全部代码工作收官（fix/4b 12 枚链 + develop 记账追平至 5fdf591）、账实相符；剩余均为「打包→装机→复验→收口」执行链。祝接手顺利。
