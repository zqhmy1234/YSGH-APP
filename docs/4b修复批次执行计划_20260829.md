# 4b 修复批次执行计划（2026-08-29 制定 · "你+我"可修复范围）

> **范围判定依据**：tracker 19 §4/§5 缺陷台账 + 决策台账 §1.6/§5/§6。
> **不列入本计划**（团队阻塞）：S2 情绪校准（卡 C 批真值）｜US-31/32/33（卡企微/微信凭证）｜合规三申请（需用户主体资料）｜正式文案库本体（产品部）。
> **P-0 状态**：五项全部落定（08-29 用户拍板：①'device' ②永久不再提示 ③散单并批 ④D-06 走等价复现-已调研 ⑤O-1 只读先行）。**P-1 已建成（.wt/fix4b @e80176f，.env/models 就位）。下一步=P-2 诊断+R1 开工。**
> **口径**：每张 D 单的完整症状/根因/修复方向以 `docs/parallel-dev-收尾/19_wave3_真机补验跟踪表.md` §4 为唯一来源，本文只排执行序与分工，不复述。

## 0. 总览

| 波次 | 内容 | 谁 | 预估 | 前置 |
|---|---|---|---|---|
| P-0 | 拍板包（2 项卡代码 + 3 项确认） | **你**（口头即可） | 15 分钟 | 无 |
| P-1 | 隔离工作区建立 | 我 | 20 分钟 | 无 |
| P-2 | O-1/O-2 只读诊断（不改环境） | 我 | 半天 | 无 |
| R1 | 纯代码波（批次1+批次2+散单+改进项） | 我（**P-0 五项已齐 08-29**） | 1.5–2 天 | P-1 |
| R2 | 重打包 + 真机复验 | 我跑工具链，**你出手机/盯屏** | 0.5–1 天（需日间在场窗口） | R1 合入 |
| R3 | 收口登记（merge/--full/台账销项） | 我 | 半天 | R2 |

关键路径：**D-18/D-19 → R2 云打包复验**（正式包后台调度+保活两项高危判死项，解除后"正式包可用"才成立）。

## 1. P-0 拍板包（你 15 分钟，决定 R1 写法）

| # | 事项 | 选项（我的建议加粗） | 卡谁 |
|---|---|---|---|
| ① | **D-15 端侧 L1 语义**（自动生成的日事件卡片 title_source 现误标 'user'） | **改标 'device'**（与 generated_by='device' 对齐，避开 B3-5 用户背书保护误触） / 'template' | ✅ **已拍板（08-29）：'device'** →R1 落地 |
| ② | **"取消=不再提示"口径**（US-25 第三次纠错弹窗，dismissCorrectionPrompt hook 已就绪未接线 U3） | **取消=本设备永久不再弹**（老人防打扰，接现有 hook） / 本次跳过 | ✅ **已拍板（08-29）：永久不再提示** →接 hook |
| ③ | **散单并批确认**：D-05/D-10/D-14/D-21（均客户端向）并批次 2 一起修一起复验 | ✅ **已拍板（08-29）：并批** | R1 范围 |
| ④ | **D-06 来电验收条件**：你是否有第二台手机可给 nova 11 打测试电话？ | ✅ **已调研（08-29）：无需第二设备**——等价复现三级：①相机抢麦脚本（ck02_grab_trigger 彩排过）②去电模拟 `am start -a android.intent.action.CALL -d tel:<短号>`（有卡，Telecom 与来电同源）③看门狗单测；真来电=可选加测（决策台账 §1.8） | 完成 |
| ⑤ | **O-1 环境变更窗口**：numpy/torch 栈排查若需动全局 python（重建 venv/降版），现在是否可做（他窗服务是否空闲、storage.py 那路何时落地） | ✅ **已拍板（08-29）：只读诊断先行**，动环境等用户通知 | P-2 之后 |
| — | （不阻塞，攒着）部署就绪包确认、文案库基调 | 有空再审 | 无 |

## 2. P-1 隔离工作区（避免与他窗 164 个未提交文件互相踩踏）

- `git worktree add .wt/fix4b -b fix/4b`（基线 develop @ 677ea68）；**全程在 worktree 内改代码**，验收后 merge 回 develop——主工作区（他窗战场）零触碰。
- worktree 建后必做（教训 08-26）：拷 `backend/.env`、链/拷 `backend/models`、确认 `client/unpackage` 走全新编译。
- 真机工具链（cli launch / adb / 云打包）均支持指向 worktree 路径（`--project` 参数），R2 用 `.wt/fix4b/client` 出包——**顺带规避"他窗未提交改动被打包"风险**。

## 3. P-2 只读诊断（不改环境，先出证据）

- **O-1**（uvicorn 嵌入时刻瞬时原生崩溃，两次复发无 traceback）：复现脚本压热点路径抓 faulthandler/native 栈；`pip freeze` 对照 numpy 2.5.2/torch/sentence-transformers 兼容矩阵 → 出诊断报告；**需动环境才按 P-0⑤ 的窗口执行**。
- **O-2**（纠错裁决恒"混合"）：纯代码/数据级复现（API 直调三层裁决），定位阈值/映射缺陷；若结论是"需真值标定"则产数据需求单归团队，不强修。

## 4. R1 纯代码波（你无需在场；每项 = 修复 + 单测/编译 + 快速门禁）

**批次 1（高危判死对，优先）**
- **D-18** `yishu-background-tasks` 探测改插件自带 marker asset（dex 恒 false 根因），保留 Class.forName 兜底验证。
- **D-19** `yishu-photo-watch` UTS 插件补 `config.json` services 声明 + `AndroidManifest` 权限（FOREGROUND_SERVICE 等），DataSyncService 注册。
- 编译口径：`--cleanCache` 全量（warm cache 掩盖存量错误，教训族 5）。

**批次 2（语音链+伪造标签，含 P-0③ 已并入的散单）**
- **D-16** 三层默认值全改：`transcriber.py:80` 增强失败→emotion_source=none 时 API 返回 **null**；schema 同步；client 情绪 chip `source=none 不渲染`；notify 门控回归用例（低落≠平静才能触发）。**D-09 遗留**（健康检查暴露单腿）随本单处置。
- **D-07** 短录音入库路径二选一（tracker 口径：短录音带音频入库 / 管线放行无音频 voice——倾向**带音频入库**，与"永久不可回放"根因对齐）+ 转写超时随时长伸缩（30s 硬超时改异步轮询，76.9s 音频冷态 24.3s 实证）。⚠️ 存储域仅经现有 upload API，**不碰 `storage.py`**（他窗领地）。
- **D-08** 转写失败段入本地待发队列（复用离线队列机制）+ 卡片"重试转写"按钮。
- **D-10** `auth.ts:61,66` statusCode/content-type 守卫（非 JSON→可读"后端不可达"）；**TEMP code=`dev-client-w3e` 波尾还原**。
- **D-14** 上传成功才推进游标 + 照片上传并入持久队列 + 重传后补 handleBatch 端侧聚合（w3j 6 张永久丢失根修）。
- **D-05** 横幅 emits 宿主接线（held 手动上传成功后补聚合/上云）+ drain held/failed 去重（计数虚增）。
- **D-21** `manage.uvue` 补 scroll-view 容器 + `index.uvue` .page flex 高度约束显式化（两页滚动失效）。
- **D-12** `readExifTaken` dt==null 分支加告警日志（消灭静默回退链全盲）。
- **D-15**（待 P-0① 拍板）title_source 语义落库修正。
- **D-06** 代码半边：`AudioRecord.getInterruption`/activeRecording 轮询自检 + 零能量看门狗 + 抢占窗显式标注（禁止静默吞音）；**验收走等价复现**（决策台账 §1.8 定案）：①复用 ck02_grab_trigger 抢麦脚本测「检出+标注+提示」闭环 ②`ACTION_CALL` 去电模拟 Telecom 路径加测 ③看门狗逻辑单测。抢麦写零系平台政策（Android《共享音频输入》），watchdog 即正解。

**R1 出口门禁**：`review_agent --full`（worktree 内，跑前巡检内存 ≥4GB + 无并行重进程，教训族 8）。

## 5. R2 重打包 + 真机复验（日间窗口，你在场：装包弹窗/来电配合/人工 IME 项）

1. cli 云打包 worktree 工程（排队 10–15 分钟/轮，预留 2 轮 = 修一处验一轮）。
2. **批次 1 验收**：正式包日志 `initBackgroundTasks ok` + `aapt dump manifest` 见 DataSyncService + 会话 B FGS 通知复验 + 拔卡蜂窝回归（清单 01 半段）。
3. **批次 2 验收**（tracker 口径）：未识别不伪造"平静"（UI 无 chip + 通知不误触）/ <5min 录音落 COS 可回放可转写 / 转写失败段保留 wav+重试。
4. 散单复验：D-21 两页可滚、D-14 断隧道→恢复→补传不丢、D-10 端口占用不崩环。
5. D-06：等价复现验收（抢麦脚本必做 + 去电模拟加测）→ 通过即 ✅（标注「等价场景，真来电可选加测」）。
6. 证据三要素入 `scripts/realdevice/evidence/`（缺一写"待补"）。

## 6. R3 收口（半天）

- `git merge fix/4b` 回 develop（冲突若涉 storage.py 等他窗文件 → 停手协调）→ 拔除 worktree。
- 登记链：**tracker 19 §4 状态列**（各单 已修/已验/待 P6）→ `feature_list.json` F1/F2/F3/F7/F8 evidence 追加 → `progress.md` 新条目 → `决策台账.md`（§1.6 散单销项、③④拍板回填）→ 批次收尾 lessons。
- 复验若挖出新单：续号 D-22+ 登记 tracker 19 §4，不新开台账。

## 7. 可选加餐（都不阻塞，你点头才排）

- **F1 性能复测**（远期待办第一位）：18 项门禁复跑，看 6 项未达标在 D 单修复后是否自然改善（约半天，我独立可跑）。
- 部署就绪包过一遍给你划待确认点（配合 P-0 ⑤⑥ 销项）。
