# 卡 C3 · 图标 / 启动图 / 自有签名证书（2026-09-21）

> 本文件是 C3 的**可完成部分**交付报告。C3 的云打包与 aapt 尸检部分因**外部条件未满足**（无服务器公网地址 → 出包前置静态门第 2 条不过；HBuilderX 单队列需人工）**未执行**，明确挂起，不冒充完成。

## 一、结论速览

| 项 | 状态 | 证据 |
|---|---|---|
| 应用图标（4 档密度） | ✅ 已生成并接入 | `client/static/app-icon/icon-{72,96,144,192}.png`；`manifest.json` `icons` 段已填 |
| 启动图（3 档密度） | ✅ 已生成并接入 | `splash-{720x1280,1080x1920,2160x3840}.png`；`splashScreens.default` 已填 |
| Android 12 启动界面（背景色 + 中部 Logo） | ✅ 已接入 | `splashScreens.background = #F8F7F4`、`splashScreens.icon`（480/720/960，透明底） |
| 自有签名证书 | ✅ 已生成（**仓库外**） | `%USERPROFILE%\.yishu-signing\yishu-guanghua-beta.jks`，alias `yishu`，SHA256withRSA，有效期 10950 天 |
| 证书 MD5（备案用） | ✅ 已算出 | `48:D7:6D:BA:80:AC:66:CB:5E:CD:2B:8C:BE:99:25:FC` |
| 接入 HBuilderX 云打包 | ⛔ **挂起** | 需人工操作 + 公网地址 |
| 出包 + aapt 核对图标/签名 | ⛔ **挂起** | 同上 |
| 出包前置静态门 | ⚠️ **2/3**（第 2 条不过） | 见 §四 |

## 二、键名与路径基准的取证过程（为什么不是"照第一份文档抄"）

图标/启动图的 JSON 写法**两份官方文档都没给**（`manifest.html` 与 `manifest-android.html` 均明文写「建议在 HBuilderX 可视化界面操作，不推荐手动改源码视图」）。按项目铁律**不许猜**，故从两个内部权威源取证：

1. **HBuilderX 官方 manifest Schema**（决定性）：
   `D:\HBuilderX\plugins\hbuilderx-language-services\jsservice\src\schemas\manifestUniAppx.json`
   解析结果（`app-android.distribute`）：

   | 节点 | 直接子键 |
   |---|---|
   | `icons` | `hdpi` / `xhdpi` / `xxhdpi` / `xxxhdpi`（72/96/144/192）——**密度键是直接子键，没有 `android` 包装层** |
   | `splashScreens` | `default` / `background` / `icon` / `brand` |
   | `splashScreens.default` | `ldpi` / `mdpi` / `hdpi` / `xhdpi` / `xxhdpi` / `xxxhdpi` |
   | `splashScreens.icon` | `xhdpi` / `xxhdpi` / `xxxhdpi`（480/720/960） |
   | `splashScreens.background` | `string`（Android 12+ 生效，默认白） |

   **推翻了一处文档误读**：`manifest.html` 把层级写成 `splashScreens.android.default`，而 schema 与仓库现存 `splashScreens: {default: {}}` 都表明**没有 `android` 层**。（`hookManifestJson` 里的 `icons.android` 属于**旧 `app` 节点**的迁移源，与 `app-android` 节点无关——两者不矛盾。）

2. **HBuilderX 中文语言包**：`"tooltip.iconSavePath.desc": "图标保存路径：unpackage/res/icons"` —— 这是**「自动生成图标」功能**的落点提示，同时语言包里存在 `"不替换手动设置的图标"`，说明**手动指定项目内路径是被支持的正常用法**。

**路径基准的最终选择与残余风险**：官方未规定基准目录，故采用**项目根相对路径**（HBuilderX 工程根 = `client/`，由 `cli project open --path D:\GuangH-App\client` 确定）：

```
static/app-icon/icon-72.png
```

- **不用 `unpackage/res/icons/`** 的理由：`unpackage/` 是**构建产物目录**（本仓 `.gitignore` 已忽略），把资源真源放进去会重演本项目已栽三单的「文件写了、git 里却不存在」= diff 审查永久盲区。放进 `static/` 才能受版本控制。
- **残余风险（明确登记）**：若云打包实际按其它基准解析，现象是**图标仍为默认图标**（不会报错）。届时修法是 **4+6 行字符串改写 + 重新打包**，成本很低。**出包后必须用 aapt 核对**（见 §五），这是该风险唯一的验收手段。

## 三、资产与 manifest 改动

### 3.1 新增资产（`client/static/app-icon/`，共 10 个 PNG，441.4 KB）

| 文件 | 尺寸 | 体积 | 用途 |
|---|---|---|---|
| `icon-72.png` | 72×72 | 5.2 KB | `icons.hdpi` |
| `icon-96.png` | 96×96 | 7.6 KB | `icons.xhdpi` |
| `icon-144.png` | 144×144 | 13.0 KB | `icons.xxhdpi` |
| `icon-192.png` | 192×192 | 18.4 KB | `icons.xxxhdpi` |
| `splash-720x1280.png` | 720×1280 | 38.4 KB | `splashScreens.default.xhdpi` |
| `splash-1080x1920.png` | 1080×1920 | 59.8 KB | `splashScreens.default.xxhdpi` |
| `splash-2160x3840.png` | 2160×3840 | 136.5 KB | `splashScreens.default.xxxhdpi` |
| `splash-icon-480.png` | 480×480 | 34.8 KB | `splashScreens.icon.xhdpi` |
| `splash-icon-720.png` | 720×720 | 54.1 KB | `splashScreens.icon.xxhdpi` |
| `splash-icon-960.png` | 960×960 | 73.8 KB | `splashScreens.icon.xxxhdpi` |

标识几何与 `deploy/download/assets/app-icon.svg` **同一套**（年轮/光晕：三层弧 + 中心点，品牌金 `#C4913C`→锈红 `#B05A3A` 45° 线性渐变，暖纸白 `#F8F7F4` 底）。落地页与 App 图标同源，视觉一致。

**刻意不配置的档位**：`splashScreens.default` 的 `ldpi`/`mdpi`/`hdpi` 三档（官方描述为"此类设备不常见，通常可以不用配置"）——省 3 个文件与约 100KB APK 体积。`brand`（Android 12 底部品牌图）同样不配（默认不显示）。

### 3.2 manifest 改动（**只动 `icons` 与 `splashScreens`，permissions 段零改动**）

```json
"icons": {
  "hdpi": "static/app-icon/icon-72.png",
  "xhdpi": "static/app-icon/icon-96.png",
  "xxhdpi": "static/app-icon/icon-144.png",
  "xxxhdpi": "static/app-icon/icon-192.png"
},
"splashScreens": {
  "default": {
    "xhdpi": "static/app-icon/splash-720x1280.png",
    "xxhdpi": "static/app-icon/splash-1080x1920.png",
    "xxxhdpi": "static/app-icon/splash-2160x3840.png"
  },
  "background": "#F8F7F4",
  "icon": {
    "xhdpi": "static/app-icon/splash-icon-480.png",
    "xxhdpi": "static/app-icon/splash-icon-720.png",
    "xxxhdpi": "static/app-icon/splash-icon-960.png"
  }
}
```

自证：`RECORD_AUDIO` 计数仍为 **2**（C1 成果未被触碰）；文件**无 UTF-8 BOM**（首字节 `123`=`{`，历史 BOM 事故不重演）；JSON 解析通过（`ConvertFrom-Json`）；10 个引用路径全部存在且像素尺寸与规格一致。

## 四、出包前置静态门（卡 C3 实施要点 4）实测结果

| # | 检查 | 结果 | 判定 |
|---|---|---|---|
| 1 | `export const ENV` | `config.uts:15` → `'prod'` | ✅ PASS |
| 2 | `PROD_BASE_URL` 不含公网 IP 占位 | `config.uts:40` → `'http://<公网IP>:8010'` | ❌ **FAIL**（另有 3 处「公网IP」占位文本） |
| 3 | `RECORD_AUDIO` 计数为 2 | 实测 2 | ✅ PASS |

**结论：静态门 2/3，按卡面纪律「任一条不符不得打包」→ 云打包明确挂起。**

原因不是 C3 未做，而是**外部前置缺失**：`PROD_BASE_URL` 的真实公网地址依赖服务器采购（用户已告知"服务器和域名暂时给不了"）。这不是可绕项——门的存在意义正是拦住"发出去的包所有页面全空且不报错"这一**零报错失败模式**。

**解冻条件（两步，均不涉及新开发）**：
1. 服务器就绪 → 把 `client/utils/config.uts:40` 的 `<公网IP>` 换成真实地址（`http://<真实IP>:8010`）；
2. 重跑本节三条门 → 三条全绿后即可进打包（打包本身需人工在 HBuilderX 操作，理由见 §六）。

## 五、签名证书

| 项 | 值 |
|---|---|
| 路径 | `%USERPROFILE%\.yishu-signing\yishu-guanghua-beta.jks`（**仓库之外**，故 `git` 无从误提交） |
| alias | `yishu` |
| 算法 | SHA256withRSA / RSA 2048 / PKCS12 |
| 有效期 | 10950 天（约 30 年，`-validity 10950`） |
| 主体 | `CN=YiShu GuangHua Beta, OU=Beta, O=YiShu GuangHua, L=Beijing, ST=Beijing, C=CN` |
| **证书 MD5** | `48:D7:6D:BA:80:AC:66:CB:5E:CD:2B:8C:BE:99:25:FC` |
| 无冒号小写写法 | `48d76dba80ac66cb5ecd2b8cbe9925fc` |
| SHA1 | `70:AC:5E:F2:71:57:56:79:3B:EF:89:31:32:0F:25:17:FB:95:97:D8` |
| SHA256 | `CF:B4:36:29:FF:9F:C8:A4:09:D3:DF:34:D3:38:31:1A:F8:44:13:8E:07:B3:E4:31:29:A3:0F:CF:05:C2:22:F2` |

同目录另有：`yishu-beta-cert.der`（导出证书，公开信息）与 `keystore-password-请转移后删除.txt`（**含密码**）。

### 必须由用户完成的三件事

1. **把密码转入密码管理器，然后删除** `keystore-password-请转移后删除.txt`。密码丢失 = **无法再为已发布的包签名更新**，只能卸载重装（用户数据全丢）。
2. **备份 keystore 文件本身**（与密码分开存放）。换机/重装会丢。
3. **备案填 MD5**（上表第一行带冒号写法，是 DCloud/微信类平台通用的展现格式）。

### 密钥纪律执行说明

- 密码**全程未在命令文本中出现**（在 PowerShell 内以随机变量生成、直传 `-storepass`/`-keypass`，随密码只写进本地文件），对话内**未回显**。
- 私钥内容从未离开上述目录，本报告只含**公钥指纹**（MD5/SHA1/SHA256）——属公开信息，可安全登记。
- `.gitignore` 追加防御性忽略（`*.keystore`；`*.jks` 原第 14 行已覆盖），并已 `git check-ignore -v` 双向验证：keystore 被忽略、`client/static/app-icon/*.png` 与 `client/manifest.json` **未被忽略**（后者是关键——资源必须可入库）。

### 两个遗留说明（不阻塞，但要知道）

- **主体字段是 ASCII 化占位**：`O=YiShu GuangHua`，非真实备案主体名。选择 ASCII 是为规避云打包链路对非 ASCII 的兼容风险；如日后上架/备案要求主体一致，需**重新生成证书**——届时已发布的包必须卸载重装才能升级（换签名即换身份）。内测阶段无此约束。
- **未启用 Android 自适应图标**：官方需在 `client/nativeResources\android\res\mipmap-anydpi-v26/` 手写 `icon.xml` + 前景/背景 vector（HBuilderX 5.25+）。当前用四档位图，Android 8+ 系统遮罩下表现正常；自适应图标可作为后续增强。

## 六、为什么云打包没有代跑

| 门槛 | 原因 |
|---|---|
| 静态门第 2 条 | 无真实公网地址（服务器未采购） |
| HBuilderX 单队列 | 全机仅一个 HBuilderX 实例，`cli launch --compile` / `cli pack` 并发会**互杀**（SKILL §1 军规），需人工协调与登录态 |
| `cli pack` 耗时与交互 | 出包过程长、可能触发登录/额度交互，不适合无人值守 |
| **本卡 §四纪律** | 「任一条不符不得打包」——现在打包等于绕过自己刚立的门 |

本轮**已把打包前的一切准备做尽**：资产、schema 校正后的配置、签名证书、MD5、忽略规则、静态门脚本。剩下的只是「填一个 IP + 点一次打包 + aapt 核对」。

## 七、出包后必做的核对（写在这里，避免到时忘）

```bash
# 1) 图标是否真的换掉了（§二残余风险的唯一验收手段）
aapt dump badging <apk> | findstr application-icon
# 期望看到 4 档 icon 条目，且不再指向默认图标

# 2) 签名是否为自己那枚（对比本报告 §五 MD5）
keytool -printcert -jarfile <apk> | findstr -i "MD5 SHA1 Owner"
# 或用 apksigner verify --print-certs <apk>

# 3) 权限是否落地（与 C1 联合验收）
aapt dump badging <apk> | findstr RECORD_AUDIO

# 4) 启动图/Android12 界面：真机冷启动目视（nova 11），截图留证
```

## 八、本卡未覆盖（留给主窗或下批）

- `client/pages/settings/settings.uvue` 的登出入口、`client/pages/account/security.uvue` 中「微信徽章 = 微信登录方式」的**文案失真**（设备码登录落地后该表述已不成立）——属 C2 落地后的遗留，不在 C3 白名单内。
- 云打包 + aapt 尸检 + 4b 批次（D-18/D-19 关单）——已拍板排在本轮打包之后。
