# 残留归档说明（2026-08-27 → 2026-08-28 Wave1 C3）

> 依据：`docs/parallel-dev-收尾/18_契约增量清单_拍板项.md` 拍板项 **D8**（用户 2026-08-27 拍板：中间产物移入 `backups/20260827_残留归档/` 保留；B1.md +40 行修订提交入库）。
> 执行：Agent C3（wave1-agentC3）｜归档前已确认**无运行中脚本/文档引用**这些文件（grep 仅见被归档脚本自身的 glob/注释自引用）。

> ⚠️ **gitignore 例外说明**：`.gitignore:106` 含 `backups/`（2026-08-21 安全加固，防 CLI dump 的 workspaceId 入库）。本目录的 12 个中间产物经 `git mv` 保留跟踪并随分支提交；本说明文件用 `git add -f` 显式纳入。**仅此窄例外**——目录内任何后续新文件（尤其含敏感信息的 dump）仍会被忽略，集成 Agent 合并时注意勿将该规则放松。

## 1. 归档清单（来源 → 去向）

### 1.1 跟踪文件（`git mv` 保留历史，随分支提交）

| 来源 | 去向 | 性质 |
|---|---|---|
| `docs/_l0_refine_A1.json` | `backups/20260827_残留归档/_l0_refine_A1.json` | L0 画像细化片段（9 片） |
| `docs/_l0_refine_A2.json` | `backups/20260827_残留归档/_l0_refine_A2.json` | 同上（任务卡记 8 个，实查 9 个：A1/A2/B/C/D/E/F/G/H） |
| `docs/_l0_refine_B.json` | `backups/20260827_残留归档/_l0_refine_B.json` | 同上 |
| `docs/_l0_refine_C.json` | `backups/20260827_残留归档/_l0_refine_C.json` | 同上 |
| `docs/_l0_refine_D.json` | `backups/20260827_残留归档/_l0_refine_D.json` | 同上 |
| `docs/_l0_refine_E.json` | `backups/20260827_残留归档/_l0_refine_E.json` | 同上 |
| `docs/_l0_refine_F.json` | `backups/20260827_残留归档/_l0_refine_F.json` | 同上 |
| `docs/_l0_refine_G.json` | `backups/20260827_残留归档/_l0_refine_G.json` | 同上 |
| `docs/_l0_refine_H.json` | `backups/20260827_残留归档/_l0_refine_H.json` | 同上 |
| `scripts/_merge_l0_refine.py` | `backups/20260827_残留归档/_merge_l0_refine.py` | L0 细化合并脚本（一次性产物） |
| `scripts/_merge_l1_refine.py` | `backups/20260827_残留归档/_merge_l1_refine.py` | L1 细化合并脚本（一次性产物） |
| `scripts/_expand_l1_and_gen_inputs.py` | `backups/20260827_残留归档/_expand_l1_and_gen_inputs.py` | L1 扩展+输入生成脚本（一次性产物） |

### 1.2 未跟踪文件（gitignored，物理移入，不入版本库）

| 来源 | 去向 | 说明 |
|---|---|---|
| `docs/画像维度枚举集_l1_骨架.json.merge_bak` | `backups/20260827_残留归档/画像维度枚举集_l1_骨架.json.merge_bak` | 已由 commit 0bd3aaf（2026-08-27）加入 gitignore（`*.merge_bak` 属"合并脚本临时备份"）；本次在**主库工作区**物理移入备份目录保留，未跟踪不入库 |

> ⚠️ 因 `*.merge_bak` 命中 .gitignore 规则，该文件不会出现在版本库/分支中——它是本地保留的参考备份。若需长期留档，应去掉 .gitignore 中该规则并 `git add -f`，由集成 Agent 评估后再定。

## 2. B1.md 修订提交状态（D8 第 2 项）

- 文件：`忆述光华_交付文档/忆述光华_深度开发设计/01_用户画像系统_B1.md`
- **结论：+40 行修订已在 develop 提交**——commit `c649fdb`（2026-08-25，`docs(b1): 用户画像系统 B1 修订 v1.1`，40 insertions / 1 deletion），早于 Wave 1，工作区无未提交版本，无需重复提交。
- 已核对修订内容合理（v1.1 修订对照 §8：L0 内容重构 / 性格 Big5 迁 L1（方案 D）/ 敏感双层分治 / 置信双门 ≥0.7/≥0.8 / 证据锚点+新鲜度戳 / 选材池 ~1100 维 / 开放枚举）。
- 任务卡"提交入库"项视作已满足；如需复核见 `git show c649fdb -- <B1.md>`。

## 3. 恢复方法

- **跟踪文件**（git mv 历史保留）：`git mv backups/20260827_残留归档/<文件> <原路径>` 即可还原，或 `git log --follow -- <文件>` 查历史。
- **merge_bak**（未跟踪）：`git checkout` 类命令不会影响它；手动 `Move-Item` 回 `docs/` 即可。
- **后续使用注意**：这些是一次性管线产物（L0/L1 细化合并已回写主枚举集 `docs/画像维度枚举集_l0.json` / `l1_骨架.json`）。若需复跑生成管线，须先将脚本与片段从 backups 移回原路径。

## 4. 归档后门禁

- 归档后快速门禁 `python scripts/review_agent.py` 通过（无 import 断裂：被归档脚本无外部引用方，主枚举集 JSON 不受影响）。
- `docs/画像维度枚举集_l0.json` / `l1_骨架.json`（正式枚举集）未改动。
