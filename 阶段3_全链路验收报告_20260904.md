# 阶段 3/4 全链路验收报告：205 张照片（2026-09-04）

> 分支：codex/photo-merge @ 7b192c3（已提交，未推送）
> 运行：真实服务链路（minio 存储 + 真实百度 OCR + 本机 BGE-M3 + PostgreSQL/Qdrant docker）
> 照片：D:\projects\photo_pipeline\test_album（205 张，7 类子目录）

## 1. 验收指标（实测）

| 指标 | 数值 | 说明 |
|---|---|---|
| 照片总数 | 205 | 7 类：连续拍摄/间隔/相似/重复/含文字/纯图像/边界敏感 |
| 成功入库 | **202** | 全部 status=done |
| pHash 去重跳过 | **3** | 重复副本自动识别（04_重复副本），不重复入库 |
| 失败 | **0** | errors=[] |
| OCR 识别出文字 | **177/202（87.6%）** | 其余 25 张为纯图像/无文字图（合理） |
| files 文件台账 | 202 | 每张照片一条（SHA256 + 大小 + 对象键） |
| 唯一 pHash | 202 | 无重复入库 |
| 事件聚合（full 模式） | L1 日卡片 1 个 / 关联 71 条 | 见说明 2 |
| 单张耗时 | avg 5.5s / p95 18.7s / max 24.5s | 含真实 OCR（1-3s）+ BGE-M3 向量；含模型/稀疏层首次加载 |

## 2. 事件聚合说明（诚实登记）

- 205 张测试图**无 EXIF 拍摄时间**（截图类居多）→ taken_at 为空，全部落在导入窗口（同一天 19 分钟内）。
  端侧聚合把 <5s 间隔当作连拍折叠（导入速度快所致），因此 L1 关联 71 条——这是测试数据特征，
  非代码缺陷；真实相册照片带拍摄时间后行为正常（上游 ST-DBSCAN 已验证 497 张基准）。
- 上游云侧默认只跑 L2/L3（L1 由手机端上报）；服务端全量导入需 mode="full"，本验收已按此执行。

## 3. 与目标的对应

| 本轮缺口 | 状态 |
|---|---|
| 37 表对齐（files/content_chunks/contents 列） | ✅ 已迁移+验证（阶段 1 报告） |
| OCR 服务 | ✅ 真实验证（本报告 177/202） |
| pHash 服务端去重 | ✅ 真实验证（3 张重复自动跳过） |
| Docker compose（本机） | ✅ 4 服务健康 |
| MinIO 双桶 | ◑ compose 已建 originals/thumbnails；**代码路由仍单桶过渡**（下一批做） |

## 4. 遗留（下一批）
1. MinIO 双桶代码路由（storage.py 按 cos_key/thumbnail_key 分桶）
2. DASHSCOPE key 就绪后补 caption（Qwen3-VL）与以图搜图真实验证
3. 全量门禁 pytest（需 SetFit/reranker 模型与完整测试库），提交前按 AGENTS.md 补跑
4. 本批 7b192c3 已过快速门禁（syntax/lint/secrets/todos）