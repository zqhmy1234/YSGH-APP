# 阶段 2 验收报告：OCR + pHash（2026-09-03）

> 分支：codex/photo-merge｜验证环境：backend/.venv（Python 3.12.13）
> 真实密钥：BAIDU_API_KEY/BAIDU_SECRET_KEY（backend/.env，已 gitignore）

## 1. 新增/修改

| 文件 | 内容 |
|---|---|
| backend/app/services/external/ocr.py（新增） | 百度 accurate_basic OCR + 2000px 压缩（防 216205）+ token 缓存 + 重试 |
| backend/app/services/perceptual_hash.py（新增） | imagehash pHash（40MP 解压炸弹防护，失败可回退） |
| backend/app/services/text_utils.py（新增） | NFKC 归一化 + SHA256 文本指纹 |
| backend/app/services/photo_content.py（改） | 注册时服务端算 pHash（优先客户端哈希）→ 去重 409；落 files 表（SHA256 文件哈希 + file_id） |
| backend/app/services/pipeline.py（改） | 照片处理加 OCR 步骤：raw_text=OCR，text=OCR+caption，normalized_text 留痕 |
| backend/requirements.txt（改） | + imagehash>=4.3 |

## 2. 真实验证（2026-09-03 实测）

### OCR（百度高精度，真实调用）
- 测试图：test_album/05_含文字/1.png（历史知识点截图）
- 结果：识别出 **1466 字**，内容为该截图中的中文正文（OCR 文本已成功回传）

### pHash（imagehash）
- 重复对：1785408458709_533.png 与 1785408458711_730.png（04_重复副本）
  - A = 94906d2fd23e4973；B = 94906d2fd23e4973 → **完全相同（True）**
- 不同图：idlefish-...jpg → 87687a78786cda16 → 与 A 不同（True）

### 语法/接线
- 5 个改动文件 py_compile 全过
- 代码走查：pHash 失败自动回退客户端 perceptual_hash（旧契约不破坏）；重复照片先查后写（并发 IntegrityError 兜底仍在）

## 3. 待办（诚实登记）
- pipeline OCR 接线已编译通过；**完整 E2E 导入验收**（照片→OCR→caption→向量→事件）需完整后端环境
  （fastapi/rq/worker + BGE-M3 模型），安排在阶段 3/4 全量验收时执行。
- files 表当前在 multipart/分片 original 新建路径落行；thumbnail_meta 占位补传（update 模式）暂不建 files 行（file_id 可空，后续补）。