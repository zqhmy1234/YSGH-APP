# 照片管线 MVP —— 下一阶段优化实施方案

> 状态：**已实施并验证**（2026-08-20）
> 对齐依据：《忆述光华·数据处理技术方案报告》第二部分"照片处理"及 5.2/5.6 评估要求
> 前置条件（已满足）：百度高精度 OCR 密钥已填且验证通过；Chinese-CLIP 本地模型完整；全链路 /upload → /search 已跑通；Git 已存档

---

## 0. 本轮优化目标与边界

目标：在现有"上传 → OCR → CLIP 向量化 → 入库 → 文字搜图"闭环基础上，补齐照片域的三块缺口：

1. **隐私合规**（P0 红线）：敏感信息（证件/卡类）不进 OCR 管线
2. **成本与数据干净**：重复图片不再重复 OCR、重复入库
3. **功能完整**：实现以图搜图 / 相似照片聚合，并在不存原图的前提下保留可检索图像
4. **健壮性**：批量上传、失败重试、失败原因可统计

明确不做（本轮范围外）：

- 评估测试集与基线（用户已拍板不建）
- 三端相册读取客户端（AssetRepository / PHPhotoLibrary / MediaStore）
- 上云（MySQL / MinIO / Milvus 替换）、用户鉴权、部署上线
- OCR 文本精确匹配 + CLIP 的混合检索（留作阶段三之后的扩展项）

---

## 阶段一：敏感信息拦截（P0 隐私红线）

### 做什么

在 `/upload` 流程中，OCR 调用之前增加一道本地检查：

- 新增 `sensitive_guard.py` 模块，内置正则规则：
  - 证件类型关键词：身份证 / 护照 / 驾驶证 / 军官证 / 港澳通行证 等
  - 证件号格式：身份证 18 位（含校验位逻辑）、护照号（1 字母 + 7–9 位数字）等
- 判定策略：**证件关键词 + 证件号格式双重命中**才拦截（降低误报），命中即返回 `400` + 明确错误码 `SENSITIVE_BLOCKED`，**默认不调用百度 OCR、不存文件、不调用 CLIP、不入库**
- 授权机制（向用户请示）：拦截响应附 `require_consent: true`；只有用户明确授权（客户端带 `user_consent=true` 重试）后才允许继续处理；**未经授权一律不放行**
- 提供开关 `.env`：`SENSITIVE_BLOCK=true`（默认开启；测试时可临时关闭）
- 拦截行为写入日志（不落业务库，避免敏感数据留痕）

### 具体效果

| 指标 | 现状 | 优化后 |
|---|---|---|
| 隐私误上传率（敏感图进云 OCR） | 100% 会传 | 0% |
| 证件类图 OCR 费用 | 正常计费 | 0 元（未经授权不调用） |
| 敏感内容在云侧留痕 | 会 | 不会 |

### 验收标准

1. 上传一张身份证照片（未授权）→ 返回 400 + `SENSITIVE_BLOCKED` + `require_consent:true`，`uploads/` 无文件、SQLite 无记录、百度无调用；授权重试后按授权策略处理
2. 上传普通图书封面 → 流程与现在完全一致
3. 名片上的手机号/银行卡号（非证件类）→ 不拦截，正常识别

---

## 阶段二：图片去重（SHA-256 content_hash）

### 做什么

- `db_mysql.py` 迁移：`photo_assets` 表新增 `content_hash TEXT` 列并建唯一索引；`init_db()` 用幂等 `ALTER TABLE`（老库自动升级，不丢数据）
- `main.py` 上传流程插入去重：读完文件字节 → 计算 SHA-256 → 查库：
  - 命中 → 直接返回已有记录的 `asset_id` + `"duplicate": true`，**不调 OCR、不调 CLIP、不重复入库**
  - 未命中 → 走正常流程，并把 `content_hash` 一并入库
- 响应体新增 `content_hash` 字段（便于端侧与后台排查）

### 具体效果

| 指标 | 现状 | 优化后 |
|---|---|---|
| 重复图 OCR 调用 | 每次重复计费 | 0 次 |
| 重复图入库 | 重复记录 | 仅 1 条 |
| 重复上传响应耗时 | 全流程（秒级） | 毫秒级（只查一次库） |

### 验收标准

1. 同一张图连续传两次 → 第二次返回相同 `asset_id` + `duplicate:true`，库中只有一条记录
2. 老库（现有 `photo_pipeline.db`）启动后自动升级成功，历史记录可正常查询
3. 不同图片不误判为重复（SHA-256 碰撞概率可忽略）

---

## 阶段三：以图搜图 + 缩略图

### 做什么

- `storage.py` 新增 `save_thumbnail(data, max_size=(512, 512))`：把原图压成 512px JPEG（质量 85），存到 `thumbnails/年/月/日/uuid.jpg`；尺寸与质量支持 `.env` 配置（`THUMB_MAX_SIZE=512`、`THUMB_QUALITY=85`），改配置即可调整，无需改代码
- `/upload` 流程调整（仅当 `KEEP_ORIGINAL=false` 时）：OCR 后删除原图，同时保留缩略图；`photo_assets` 新增 `thumb_path` 列；响应体返回 `thumb_path`
- 新增 `/search_by_image` 接口：接收图片 → 本地生成缩略图 → CLIP 图片向量 → 向量检索 → 返回相似资产列表（含 `asset_id`、距离、`thumb_path`、`ocr_text`）
- `KEEP_ORIGINAL=true` 时行为不变（仍存原图，`thumb_path` 为空）
- 兼容处理：历史记录无缩略图时，搜索返回 `thumb_path: ""` 不报错

### 具体效果

| 指标 | 现状 | 优化后 |
|---|---|---|
| 以图搜图 | 不支持 | 支持（图 → 相似图） |
| 相似照片聚合 | 不支持 | 支持（同一场景/角度聚合） |
| 单图存储体积 | 原图（几百 KB ~ 数 MB） | 缩略图（约 30–80 KB） |
| 跨模态检索 | 仅文字 → 图 | 文字 → 图 + 图 → 图 |

### 验收标准

1. 上传图 A，用 A 本身搜图 → 首位命中 A
2. 上传与 A 相似的图 B，用 B 搜图 → 结果里同时出现 A、B
3. 原图确实被删除，缩略图存在且可访问；文字搜图仍正常
4. `KEEP_ORIGINAL=true` 时上传仍存原图，功能不受影响

---

## 阶段四：上传健壮性（批量 / 重试 / 可统计）

### 做什么

- 新增 `/upload/batch`：一次接收多张图，逐张走完整流程，**同步返回**结果数组 `[{asset_id, status, error?}]`（不做异步任务/进度回调，MVP 决策），单张失败不影响其他张
- 失败分类与重试：
  - OCR 失败 / 向量化失败：自动重试 1 次（间隔 1 秒）
  - 入库失败：记录失败原因，不重试（避免脏数据）
  - 格式/大小超限：直接返回错误，不重试
- 失败原因统计：新增 `task_log` 表（或仅日志文件），记录 `asset_hash / 阶段 / 错误类型 / 时间`，用于统计 Pipeline 成功率
- 幂等：阶段二的 `content_hash` 天然提供批量重试的幂等保障（重复提交不产生重复数据）

### 具体效果

| 指标 | 现状 | 优化后 |
|---|---|---|
| 批量导入 | 需逐张手动传 | 一次多张，逐张独立结果 |
| 单张失败影响 | 整批中断 | 互不影响 |
| 失败原因 | 只有 HTTP 错误码 | 分类明确（OCR/向量/入库/超限） |
| Pipeline 成功率 | 不可统计 | 可计算、可定位失败点 |

### 验收标准

1. 一次传 5 张（含 1 张超限、1 张正常）→ 返回 4 成功 + 1 条明确错误
2. 模拟 OCR 网络失败 → 自动重试后成功
3. 重复提交同一批 → 不产生重复记录
4. 失败记录能从 `task_log` 按类型统计

---

## 实施顺序与工作量

| 阶段 | 内容 | 预估工作量 | 依赖 |
|---|---|---|---|
| 一 | 敏感信息拦截 | 0.5 天 | 无 |
| 二 | 图片去重 | 0.5–1 天 | 无 |
| 三 | 以图搜图 + 缩略图 | 1 天 | 二（复用 content_hash 做图级幂等） |
| 四 | 批量/重试/统计 | 1–2 天 | 二（幂等） |

建议合并节奏：

- **第一次提交**：阶段一 + 阶段二（一起做，都改 `main.py` 上传主链路）
- **第二次提交**：阶段三（新接口 + 缩略图）
- **第三次提交**：阶段四（批量与可观测性）

每阶段完成后跑对应验收标准，并提交 Git（保持可回退）。

---

## 总体验收清单

| # | 操作 | 预期效果 | 通过标准 |
|---|---|---|---|
| 1 | 上传身份证照片（未授权） | 拦截 | 400 + SENSITIVE_BLOCKED + require_consent:true，无任何落库/云调用；授权后按授权策略处理 |
| 2 | 同一张图传两次 | 第二次秒回 | 相同 asset_id，duplicate=true，库仅 1 条 |
| 3 | 上传图 A 后用 A 搜图 | 命中 A | 首位返回 A |
| 4 | 上传相似图 B 后搜 B | 命中 A+B | 结果同时含 A、B |
| 5 | 传 5 张（含 1 张超限） | 4 成功 1 报错 | 逐张独立结果 |
| 6 | 重复提交同一批 | 不重复 | task_log / 业务库无重复记录 |
| 7 | 回归：图书封面上传+文字搜图 | 与现状一致 | OCR 正常、512 维、原图不存 |

---

## 风险与预案

| 风险 | 影响 | 预案 |
|---|---|---|
| 证件识别误报 | 正常图片被拦 | 证件关键词+证件号格式双重判定；误拦时用户可通过授权流程放行 |
| 数据库迁移破坏老库 | 历史数据丢失 | `ALTER TABLE` 幂等 + 迁移前自动备份 `photo_pipeline.db` |
| 缩略图生成失败（损坏图） | 上传失败 | 缩略图失败则回退为不存图，仅文字+向量入库，不阻断主流程 |
| 批量并发写 SQLite 锁冲突 | 入库失败 | 批量接口内串行处理 + 单事务提交 |
| 拦截开关误关 | 隐私风险 | `.env` 默认 `SENSITIVE_BLOCK=true`，代码内硬校验 |

---

## 已确认决策

| 问题 | 决策 |
|---|---|
| 敏感拦截范围 | 先只做**证件类**（身份证/护照/驾驶证等），不含银行卡/手机号 |
| 被拦截请求 | **向用户请示**：返回 `require_consent:true`，用户授权（`user_consent=true`）后才允许继续 |
| 缩略图参数 | 默认 512px / JPEG 85，支持 `.env` 可调（`THUMB_MAX_SIZE` / `THUMB_QUALITY`） |
| 批量接口 | **同步返回**结果数组，不做异步/进度回调 |

## 补充决策（已确认）

1. **授权后证件图处理路径**：采纳推荐方案——即使授权也**不上云 OCR**；本地向量化 + 缩略图 + 敏感标记入库，`ocr_text` 留空。
2. **缩略图默认值**：采纳 512px / JPEG 85，参数由 `.env` 控制（`THUMB_MAX_SIZE` / `THUMB_QUALITY`）。

---

## 实施验证记录（2026-08-20）

| 用例 | 操作 | 结果 |
|---|---|---|
| 普通图上传承载 | 上传图书封面 sample.jpg | ✅ asset_id=3，OCR 全文正确，vector_dim=512，thumb_path 已生成（37KB），is_sensitive=false |
| 图片去重 | 同一张图再传一次 | ✅ 返回相同 asset_id + duplicate:true，耗时毫秒级，未重复 OCR |
| 敏感拦截（未授权） | 上传合成身份证图 | ✅ HTTP 400 + SENSITIVE_BLOCKED + require_consent:true，matched=中国居民身份证照片（0.46），未落库 |
| 敏感拦截（授权后） | 同上 + user_consent=true | ✅ asset_id=4，is_sensitive=true，ocr_text=""（未调云 OCR），缩略图+向量已入库 |
| 以图搜图 | 用 sample.jpg 搜图 | ✅ 首位命中 asset_id=3（距离 1.0），第二位 0.40 |
| 文字搜图回归 | query=政治哲学 | ✅ 命中 asset_id=3（0.49） |
| 批量上传 | 3 个文件：重复图/敏感图/非法格式 | ✅ success=2（均为 duplicate），requirements.txt 单独报 400，互不影响 |
| 数据落地 | 检查 SQLite / 目录 | ✅ 4 条资产记录、task_log 建好、thumbnails 2 个文件、uploads 无原图残留 |

说明：

- 敏感判定采用本地 Chinese-CLIP 零样本（不上传任何数据），当前阈值 0.24：合成证件图 0.46 拦截、普通图书封面 0.34 放行。**阈值需用真实证件样例进一步校准**（`.env` 的 `SENSITIVE_THRESHOLD`）。
- OCR 失败自动重试 1 次（间隔 1 秒）；批量/去重/缩略图/拦截均已上线。
