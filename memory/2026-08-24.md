# 2026-08-24（补充：真机 E2E 验收）

## 真机 nova 11 E2E 全链路验收（下午，提交 56c605b，门禁全绿）

- 链路全通：ContentObserver 相册监听 → 游标去重 → 4s 静默窗口攒批 → multipart 上传 → 后端 EXIF → 云侧聚合 → F8 时间轴渲染。50 张测试照片 → L1 日卡片 08-22(40)/08-23(10) 与真值一致，截屏验证（空状态/上传进度/时间轴卡片全对，视觉规范落地）。
- 关键事故与修复：
  1. 首扫把设备 9319 张真实照片全量上传（隐私红线）→ 游标初始化到 max(id) 修复（只监听新增）
  2. MediaProvider scan_file 污染 DATE_TAKEN → 后端 PIL EXIF 权威解析（客户端时间不可信）
  3. 双 ensureLogin 竞态撞 devices 唯一约束 500 → _issue_tokens IntegrityError 兜底 + 客户端单飞
  4. 后台 RQ worker 干扰 test_queue 隔离（pytest 前必须清 worker 进程）
- 用户硬性要求落实：冒烟测试替换为真实用例测试（api_smoke_cases.py 6 用例，test_agent 接入）
- 插件架构调整（需峰宝知悉）：Hybrid Kotlin → 纯 UTS（标准基座可跑，免自定义基座/云打包）；SecurePrefs(EncryptedSharedPreferences) 随自定义基座波次恢复，token 暂存 uni storage
- HBuilderX 环境：插件安装走 npmmirror 滞后（ETARGET）→ 从 update.liuyingyong.cn 手动下载 zip 解包（launcher/uniapp-runextension 219MB）；cli.exe launch app-android --compile true 为编译验证利器（~30s/轮）
- UTS 编译硬教训：interface.uts 多函数声明触发 overload panic；UTSJSONObject 是全局名（UTSJSON 不是）；UTSAndroid/UTSJSON 内置全局不可 import；Promise.catch 重载限制→resolve-only 模式；const 箭头不可自引用→模块级 function/class；先定义后使用
- 遗留：L2 语义归并待真实数据（P2-07）；设备时钟错乱导致日标签偏移（非代码缺陷）；30s 门禁服务端侧已验证（4.2s+5.6s）
- 后端 dev 服务仍在 8000 运行（真机可继续联调）；设备测试目录与 DB 测试数据已清理
