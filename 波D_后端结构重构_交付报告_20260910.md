# 波 D 交付报告：后端结构重构（归属校验收敛 / AST 归属门禁 / 巨文件拆分）

- **执行日期**：2026-09-10
- **工作树**：`D:\GuangH-App\.wt\missing-pages`（分支 `feature/missing-pages-impl`）
- **基线 commit**：`424203b`（波 C 收官 + 安全深扫七项已入库）
- **终态 commit**：`2f14af0`
- **交付形态**：仅本地 feature 分支 commit，**未 push、未 merge**（按提示词四节要求，回主干由波 E / 峰宝统一收口）
- **验收铁律**：测试计数不减、行为全等。**零功能增量**。

---

## 零、终局验收结果（一句话先给结论）

| 验收项 | 标准 | 实测 | 结论 |
|---|---|---|---|
| 全量 pytest | ≥820 passed / 4 skipped / 零失败 | **827 passed / 4 skipped / 20 deselected / 0 failed**（309.81s，exit 0） | ✅ |
| ruff | 全绿 | `app/services/`、`event_aggregation/`、本次 11 个文件 **All checks passed** | ✅ |
| git fsck --no-dangling | 零 error | **零输出、exit 0**（warning 已清零，见 §5.4） | ✅ |
| 安全批钉桩 | 逐条仍在且绿 | **190 passed / 0 failed**（安全面 147 + 安全深扫七项 43） | ✅ |
| 行为全等 | 状态码/错误码/文案/响应结构不变 | 唯一行为变更 1 处，已显式声明（§5.1） | ⚠️已声明 |

> 827 = 基线 820 + 波 D ② 双侧钉桩 1 + 波 D ③ 门禁自测 6。**拆分前后逐字相同**，即巨文件拆分零回归。

---

## 一、目标 ② 现状表：`app/api/` 归属校验收敛

### 1.1 收敛结果（做了什么）

| 实体 | 收敛产物 | 位置 | 改造前 | 改造后 |
|---|---|---|---|---|
| Content | `load_alive_content(db, user_id, content_id)` | `app/api/deps.py:84`（**新建公共 loader**） | `contents.py` 模块私有 `_load_alive_content`，被本域 7 端点复用 | 提升为 `deps` 公共件；`contents.py` 7 处调用改引用；`capsules.seal_capsule` 内联同款查询一并收敛 |
| Message | `load_owned_message(db, user_id, msg_id)` | `app/api/messages.py:37`（**新建**） | `mark_read` 手写 UPDATE + 存在性补查，归属语义隐式 | 先 `load_owned_message` 显式校验；**保留原子 UPDATE**（where 仍含 `status='unread'`），刻意不改成「读-改-写」以避免 TOCTOU 竞态 |
| Capsule | `_load_owned_capsule`（**既有，未动**） | `app/api/capsules.py:45` | — | 侦察确认为正例，无需收敛 |
| Event | 无新增 loader（**论证不建**，见 1.3） | 现状由 `services/events/edit.py::_get_event` 承担 | — | 门禁 `OWNERSHIP_LOADERS` 中占位登记 |
| UploadTask | 无新增 loader（**论证不建**，见 1.3） | 现状由 `services/upload/protocol.py` 承担 | — | 同上 |

**收敛判据（生命线）**：HTTP 状态码、`ApiError` code、message 文案、软删/归属四态行为，**改造前后逐个一致**。已用既有测试钉住：`test_contents.py:369 test_detail_idor_and_soft_deleted_404`、`test_ab_scenarios.py test_cross_user_four_routes_404`（favorite/PATCH/waveform/软删四路）、`test_bb3_waveform.py`、`test_notify.py`。

### 1.2 侦察实录：`app/api/` 归属校验的**五种写法**（21 个域文件全扫）

| # | 写法 | 代表位置 | 处置 |
|---|---|---|---|
| ① | 正例 loader（域内私有/公共） | `contents._load_alive_content`、`capsules._load_owned_capsule`、`services/events/edit._get_event` | Content 提升为公共；Capule 保持域内 |
| ② | 手写 `where(user_id==...)` + 手判 None | `event_items.py:57`、`media.py:81` | 已带归属过滤，**保持不动**（改造成本 > 收益） |
| ③ | 手写 UPDATE + 存在性补查 | `messages.mark_read` | **收敛**（本次） |
| ④ | 下沉 service 层校验 | `events.py` merge/split/confirm/cover、`upload.py` 全部、`wechat.wechat_delete`、`thumbnails.get_thumbnail` | 保持不动（service 层已校验，门禁以 EXEMPT 登记） |
| ⑤ | RQ job meta 校验 | `classify.py` 用 `job.meta.user_id`，越权返 **403** 非 404 | 保持不动（**错误码语义不同，红线禁止改动**） |

**各 api 文件 `user.id` 引用密度**（支撑上表，本次实测）：

```
contents.py 33 | events.py 28 | classify.py 15 | capsules.py 10 | messages.py 8
upload.py 7    | export.py 6  | stats.py 4     | event_items.py 4 | echo.py 4
sync.py 3      | wechat.py 2  | search.py 2    | interview.py 2   | deps.py 2
corrections.py 2 | users.py 1 | thumbnails.py 1| media.py 1
auth.py 0 | asr.py 0 | __init__.py 0   ← 无归属语义（登录/ASR 回调，公开端点）
```

### 1.3 为何**不**为 Event / UploadTask 新建 loader（论证）

- **Event**：`app/api/events.py` 的 5 个「按 id 操作」端点（merge/split/confirm/cover/lifecycle）全部把归属校验**下沉**到 `services/events/edit.py::_get_event`。在该处再包一层 `load_owned_event` 只是**同义转发**，会新增一条「同一语义两个入口」的漂移风险，且改造要动 service 层（越界）。
- **UploadTask**：`upload.py` 的 chunk 端点直接委托 `_upload_chunk_impl` → `services/upload/protocol.upload_chunk(user_id=user.id)`，归属在 service 层以 `user_id` 下传校验。
- **结论**：这两个实体**现状已合规**，新建 loader 属"为对称而改造"，不产生安全收益、只产生回归面。按提示词「切不开的允许论证不拆」的同一原则处理。

**发现并修复的真实缺口 1 处**（见 §5.1）：`corrections.py` 漏 `deleted_at` 过滤。

---

## 二、目标 ③ 门禁误报排除清单：`tests/test_authz_gate.py`

### 2.1 门禁规格与实现

- **候选** = `@<router>.(get|patch|put|delete|post)` 且（路径含 `{*_id}` 或形参含 `*_id`）
- **判定**：函数体满足其一 → ① 调用 `OWNERSHIP_LOADERS` 中的 loader ② 出现归属语义标识（`user.id` 属性访问 / `user_id` 标识符或关键字实参）③ 登记在 `EXEMPT`
- **技术选型**：用 **AST 精确解析**而非正则。依据：波 D 侦察时正则版得 4 命中**全误报**（创建型端点本无归属概念；`content.user_id != user.id` 这种写法没被模式认出来）。
- **规格外扩 1 项（已在文件头注释声明）**：规格原文只列 `get|patch|put|delete`，实测**操作型 POST 大量存在**（`POST /messages/{id}/read`、`/capsules/{id}/open`、`/contents/{id}/favorite`、`/trash/{id}/restore`、`/wechat/delete`…）。只覆盖四方法等于漏掉半个面，故纳入 POST 并以白名单排除「真·创建型」。

### 2.2 白名单 `CREATION_WHITELIST`（3 条，逐条人工核对源码后登记，**均带行内理由**）

| 文件 | 函数 | 排除理由（原文） |
|---|---|---|
| `contents.py` | `list_contents` | 列表型：`content_id` 是**可选 query 过滤条件**（列表筛选），不是「取单个既有实体」；查询自带 `user_id + deleted_at` 过滤（L403-409） |
| `upload.py` | `init_upload` | 创建型：`client_upload_id` 是幂等键，本端点**新建**上传任务，无既有归属可读 |
| `sync.py` | `sync_pull` | `device_id` 是**设备标识**而非既有实体 id；数据按 `user_id + since` 游标过滤 |

### 2.3 残余例外 `EXEMPT`（2 条，文件+函数+理由三列，**禁止通配兜底**）

| 文件 | 函数 | 排除理由（原文） |
|---|---|---|
| `upload.py` | `upload_chunk` | 薄壳：函数体直接委托 `_upload_chunk_impl(upload_id, ..., db, user)`，后者以 `user_id=user.id` 下传 service（`services/upload/protocol.upload_chunk`）做归属校验 |
| `upload.py` | `upload_chunk_post` | 同上：POST 别名（`uni.uploadFile` 不支持 PUT），与 PUT `/chunk` 共用同一 `_upload_chunk_impl` |

### 2.4 门禁自测（6 passed，证明「抓得住违规」而非摆设）

| 测试 | 作用 |
|---|---|
| `test_candidates_found` | **哨兵**：候选 ≥20，防扫描器失效后零候选全绿 |
| `test_every_id_endpoint_has_ownership_check` | 核心门禁（未来任何新端点写漏归属 → CI 红） |
| `test_whitelist_and_exempt_entries_still_exist` | **僵尸条目检查**：白名单/例外的函数被删或改名即失败 |
| `test_scanner_detects_missing_ownership` | **负例**：喂无归属函数必须判失败 |
| `test_scanner_accepts_both_valid_patterns` | **正例**：两种合规写法都必须判通过（防误报） |
| `test_scanner_covers_operational_post` | 覆盖性：操作型 POST 在候选面内 |

---

## 三、目标 ① 各文件拆前拆后行数图

拆分方式统一为「**只搬代码不改逻辑** + 全量兼容 re-export + 消费方零改动」（仿 `app/services/upload/` 子包化先例）。

### 3.1 文件 1/3：`app/services/events/aggregate.py`（提交 `1ec8a15`）

```
拆前  events/aggregate.py        ██████████████████████████████████████████████  558
拆后  events/aggregate.py        ███████████████████                           225
      events/aggregate_write.py  ███████████████████████████████               373  (新)
```
- 切法：按「读 / 写职责」切出**写库层**（`_write_l1_days` / `_write_upper_candidates` / `_write_upper_events` / `_previous_aggregate_result` / `_l3_confidence`）
- 保留未搬：`_to_raw_photo` / `aggregate_user` / `_l2l3_candidates_from_photos` / `_refresh_upper_candidates` / `run_user_aggregation`
  - ⚠️ `run_user_aggregation` **刻意未搬**：`tests/test_events.py:97` 断言 `_impl.__module__ == "app.services.events.aggregate"`

### 3.2 文件 2/3：`app/services/event_aggregation/pipeline.py`（提交 `b37dac7`）

```
拆前  event_aggregation/pipeline.py  ██████████████████████████████████████████████  615
拆后  event_aggregation/pipeline.py  ████████████████████                            252  入口 + re-export
      agg_types.py                   ████████                                        100  常量 + AGG_CONFIG + 2 dataclass
      agg_preprocess.py              █████████                                         116  preprocess + GPS 网格工具
      agg_candidates.py              █████████████████████                           261  L2/L3 候选 + 生命周期 + 叶子工具
```
- 依赖单向：`pipeline → {agg_types, agg_preprocess, agg_candidates}`，卫星模块只依赖 `agg_types`
- 单文件最大 **261 行** ✅ ≤350

### 3.3 文件 3/3：`app/services/pipeline.py`（提交 `2f14af0`）

```
拆前  services/pipeline.py        ██████████████████████████████████████████████████████████  784
拆后  services/pipeline.py        ███████████████████████████████████████                    488  ⚠️ 冷冻核心（见下）
      pipeline_common.py          ████                                                        59   logger / patch_extra / extra_get / 聚合入队
      pipeline_audio.py           ███████████████                                            193  音频落地 + 情绪增强 + enrich RQ 任务
      pipeline_photo.py           █████████                                                 118   _process_photo
      pipeline_tagging.py         ████                                                        50   tag_content RQ 任务
```

#### ⚠️ 本文件未达 ≤350 目标 —— 论证（非为指标硬切）

`tests/test_pipeline.py` 用**字符串 monkeypatch** 打在 `app.services.pipeline.<name>` 上：
`_get_classifier` / `_index_content` / `_classify_content` / `_process_text` / `_process_photo`。
**CPython 的函数全局查找走「函数定义模块的 `__dict__`」**，因此「被 patch 的名字」与其「调用方」必须**同模块共存**，否则 patch 静默失效（症状：测试真去加载 SetFit 大模型）。由此锁定四条同留约束：

1. `_classify_content`（patch 目标）+ `_process_text` / `_process_voice`（voice 测试打 `_classify_content` 期望生效的调用方）+ `_get_classifier` → 同留
2. `_index_content`（patch 目标）+ `_index_after_commit`（调用方）→ 同留
3. `_resolve_handler` 用 `globals().get(name)` 派发处理器（源码注释明写「测试 monkeypatch `_process_text` 后分发能取到替身」），其 globals 必须就是本模块 → `CONTENT_HANDLERS` / `register_content_handler` / 三个注册调用随其留驻
4. `process_content` 调 `_resolve_handler` + `_index_after_commit`，且自身是 **RQ pickle 路径**（`app/workers/worker.py` 预导入 `app.services.pipeline.process_content`）→ 留驻

**物理下限 ≈ 440 行**，其中 `process_content` 单函数 222 行（六阶段编排，拆它即改逻辑，违反「只搬代码不改逻辑」）。
现状 488 = 下限 440 + 本次新增的 50 行拆分论证文档（模块 docstring 16 → 50）。

> 诚实结论：本文件**已把可安全外移的部分全部外移**（784 → 488，-37.8%），剩余部分是**约束锁死区**，不是未完成的工作。

### 3.4 附带收益：`app/api/contents.py` 910 → 902 行（目标 ② 顺手收敛）

---

## 四、提交链（7 commit，全部本地，未 push）

| # | hash | 目标 | 内容 |
|---|---|---|---|
| 1 | `f8af3c7` | ② | `refactor(authz): Content 归属校验收敛——load_alive_content 提升为公共 loader` |
| 2 | `f12b9f4` | ② | `refactor(authz): Message 归属校验收敛——load_owned_message + mark_read 显式化` |
| 3 | `0efd838` | ② 补漏 | `fix(sec): corrections 补 content_id 软删过滤 + 双侧钉桩` |
| 4 | `e5bd216` | ③ | `test(authz): 归属校验 AST 静态门禁` |
| 5 | `1ec8a15` | ① 1/3 | `refactor(events): aggregate.py 拆分——写库层独立为 aggregate_write.py` |
| 6 | `b37dac7` | ① 2/3 | `refactor(agg): event_aggregation/pipeline.py 拆分——契约/预处理/候选三层独立` |
| 7 | `2f14af0` | ① 3/3 | `refactor(pipeline): pipeline.py 拆分——冷冻核心 + 4 卫星模块` |

**搬运正确性证据（AST 逐函数比对，强于行匹配）**：

| 拆分 | 顶层函数 | 逐字一致 | 丢失 | 新增 | 模块级符号 |
|---|---|---|---|---|---|
| `aggregate.py` | — | ✅ | 0 | 0 | — |
| `event_aggregation/pipeline.py` | 25 | ✅ 25/25 | 0 | 0 | 19 一致 |
| `services/pipeline.py` | 20 | ✅ 20/20 | 0 | 0 | 1（`logger`）一致 |

**回归证据链**：受影响子集（16 / 73） → 全量（827 passed / 4 skipped / 0 failed） → ruff 全绿 → `grep -c "def "` 计数核对（26=26、20=20）。

---

## 五、未尽事项与理由（逐条显式声明）

### 5.1 ⚠️ 唯一行为变更：`corrections.py` 补软删过滤

- **现象**：`app/api/corrections.py:42` 原为裸查 `select(Content).where(Content.id == req.content_id)`，**漏 `deleted_at` 过滤** → 本人已软删内容仍可写入纠错，并经 D-22 通路回写 `content_class`。
- **变更**：补 `Content.deleted_at.is_(None)`。
- **红线遵守**：**错误码保持 `ERR_EVENT_005` 不变**、文案 `"内容不存在或不属于当前用户"` 不变（该端点的错误码族与 contents 域不同，不得为了"统一"改码）。
- **钉桩**：`tests/test_correction.py::test_correction_soft_deleted_content_rejected` —— **双侧**：① 软删 → 404 `EVENT_005`；② 清 `deleted_at` 恢复 → 200（证明 404 归因软删，而非归属/参数等其他原因）。
- **依据**：全项目一致契约「`deleted_at` 内容对外不可达」（回收站走独立 `/api/v1/trash`）。

### 5.2 提示词规格的**两处实证偏差**（已按实测更正）

| 项 | 提示词原文 | 实测 | 处置 |
|---|---|---|---|
| 巨文件行数 | `pipeline.py` 708 / `aggregate.py` 516 | **784 / 558** | 按实测执行，偏差已在两份 commit message 中显式声明 |
| pytest 分层 | 「20 deselected = integration 分层默认排除」 | `pytest.ini` 的 `addopts = -m "not rag"` → **默认排除的是 `rag` 标记**；integration 默认**会跑** | 按实测执行；本报告所有计数含 integration |

### 5.3 未做且**刻意不做**的事项

| 事项 | 理由 |
|---|---|
| 不做 `pg_fallback` 全文索引 | 峰宝已撤销（= 伪任务），红线明列 |
| 不为 Event / UploadTask 新建 loader | 现状已合规（§1.3），属"为对称而改造"，无安全收益、只增回归面 |
| 不动 `classify.py` 的 403 越权语义 | 红线：不改任何错误码；且 403 是该端点既有契约 |
| 不修 `app/api/contents.py` / `events.py` 逾 350 行 | 超出波 D 三个指定文件范围（红线：不越界） |
| 不改 `backend/scripts/reinject_missing_photos.py` 的 ruff B905 | **非本期改动文件**（他窗提交 `b2d1bfa`「R9-B3/B4批次入库」，develop 基线既有）。按「不越界」登记不修 —— 需峰宝派单给该文件归属窗口 |

### 5.4 环境问题与根治（重要，需沉淀为 skill）

**git ref 静默失败**（本段 2 次 commit 2 次滞后，全波 D 共 6 次）：

- **症状**：`git commit` 报告成功（如 `2f14af0`），但三通道复核一致显示**旧 HEAD**；本次更出现**回退**（ref 停在上一次手写的 `b37dac7`）。
- **诊断证据**：`git reflog -1 --format=%H` → 真身正确；`git cat-file -t <sha>` → `commit`；`git log -1 <sha>` subject 正确 ⇒ **commit 真的成功，只有 ref 指针没动**。`git fsck` 报 `trailingRefContent`。
- **根因判断**：git 更新 ref 走「临时文件 + `rename`」原子模式，**本机沙箱拦截 `rename`**；`commit` 的退出码仍为 0（静默）。直接 `open()/write()` 有效。
- **修复 SOP（本波已固化，100% 有效）**：
  1. `git reflog -1 --format=%H` 挖真身 → `re.fullmatch(r"[0-9a-f]{40}", sha)` 断言
  2. `git log -1 --format=%H|%s` 校验对象为 commit 且 subject 含预期关键字
  3. `os.makedirs(dirname, exist_ok=True)` 后**二进制模式**写 loose ref（`wb` + `b"\n"`；**文本模式会写入 `\r\n` 触发 fsck `trailingRefContent` warning**）
  4. 二进制读写 `packed-refs`，锚点 `count != 1` 即拒绝，先备份
  5. 三通道复核：`git rev-parse HEAD` + `git log --oneline -1` + `git for-each-ref refs/heads/<分支>`
  - ⚠️ **备份文件不得落在 `.git/refs/` 目录内**（会被当 ref 扫描，本波踩过：`*.bak-crlf` 触发 fsck warning）
- **现状**：`git fsck --no-dangling` **零输出、exit 0**；三通道一致指向 `2f14af0`。

**pytest 临时目录清理被沙箱拦截**：全量回测报 `EXIT=1` 但测试结果全绿——根因是沙箱拦截 pytest 会话末的 `tmpdir` 批量清理（`garbage-*` 累积 134 个 > 阈值 70），**非测试失败**。解法：`--basetemp=$(mktemp -d)` 传独立空目录，摘要行恢复、exit 0。（**判读警告：`EXIT=1` 不可直接判为测试失败，须看进度标记 F/E。**）

### 5.5 建议（交峰宝拍板，本波不执行）

1. **波 D 未 push** —— 需决定回主干时机（提示词要求本波只 commit 在 feature 分支，回主干由波 E 统一收口）。
2. `docs/lessons.md` 登记本波两条教训（git ref 静默失败 / 沙箱 tmpdir 拦截误判），见同批提交。
3. 「命名空间冻结」这一约束（字符串 monkeypatch 迫使 patch 目标与调用方同模块）建议写入 `docs/决策台账.md` 术语区，使后续任何拆分任务开工前即可查到该约束，避免重复侦察。
