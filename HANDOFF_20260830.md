# 工作交接文档 — 忆述光华 UI 还原 + 前后端联调

**交接时间**：2026-08-30
**分支**：`wrap1-agentA2-ui-restore`
**Worktree 路径**：`D:\GuangH-App\.wt\wrap1-agentA2-ui-restore`
**远程状态**：已推送（`a586e80..1d62365`），未合并到 develop

---

## 一、已完成工作

### 1. 八页面 UI 像素级还原（各 Agent 提交合入本分支）

| 页面 | 文件 | 还原要点 |
|---|---|---|
| 时间轴主页 | `pages/index/index.uvue` | 照片出血氛围层（湖面图+顶部暗化+底部溶解渐变）、玻璃搜索胶囊、白色圆角卡片、玻璃 TabBar |
| 搜索页 | `pages/search/search.uvue` | 白色圆角搜索框(rx=48)、4个筛选标签(激活态锈红12%)、语音卡(琥珀区+波形)、文字卡(引用竖线) |
| 消息中心 | `pages/messages/messages.uvue` | 分段控件(系统通知/AI互动)、AI精选卡、消息列表卡(彩色头像+未读红点) |
| 我的页 | `pages/profile/profile.uvue` | 用户区(头像+名字+标语)、类型卡三列、菜单卡两行、Beta徽标 |
| 记录面板 | `pages/record/record.uvue` | 时间轴背景+模态遮罩+底部弹层(四入口:拍照/选照片/说一段/写几句) |
| 冷启动访谈 | `pages/interview/interview.uvue` | 径向氛围光斑、3进度点、金色四角星、三行问题卡、同心环录音按钮 |
| 设置页 | `pages/settings/settings.uvue` | 顶部导航、账号与安全卡、通知偏好卡(自定义开关)、深色模式/清理缓存卡 |
| 记忆详情 | `pages/detail/detail.uvue` | 照片出血区+三枚玻璃圆钮、serif主标题、AI观察卡、相关记忆横向双卡、底部玻璃悬浮操作栏 |

### 2. 统一 TabBar 组件
- 文件：`components/TabBar/TabBar.uvue`
- 5 标签：时间轴 / AI / 中央锈红大圆＋(凸起) / 搜索 / 我的
- 玻璃背景 `rgba(246,241,231,0.75)`，rx=62rpx，FAB 凸起 -24rpx
- 图标用内联 SVG 字符串（hens-svg 渲染，见遗留问题）

### 3. Sarasa Gothic SC 字体子集化
- 提交：`eb13443`
- 65.7MB → 26.4MB（TTF 子集化，fonttools 提取常用字符集）
- 字体文件放 `client/static/fonts/`

### 4. 八页面前后端 API 联调对接（提交 `1d62365`）

| 页面 | 对接内容 |
|---|---|
| search | `POST /api/v1/search`（真实 hits/类型过滤/降级），支持 onLoad type 入参 |
| messages | `GET /api/v1/messages` + 单条/全部已读，分段按 msg_type，onShow 刷新 |
| interview | `GET /api/v1/interview/questions` + `POST /api/v1/interview/answers`，按 key 收集三问 |
| detail | onLoad contentId，`GET /api/v1/contents?content_id=` 取详情 + `/contents/{id}/events` 相关记忆 |
| record | 照片入口接入 `uploadBatch` 真实上传（复用 index 范式，临时构造 PhotoItem） |
| profile | 恢复设置页真实跳转（goSettings） |
| 公共 | `event_ops.ts` 新增 ContentDetail/fetchContentDetail；`search_api.ts` 增加 contentTypes 参数 |
| 后端 | `GET /api/v1/contents` 增加 `content_id` 可选精确查询（向后兼容） |

### 5. 编译验证
- HBuilderX 5.24，Vapor 蒸汽模式，全量编译**零错误零警告**
- 10 个页面 + TabBar 组件 + hens-svg 插件全部编译通过

### 6. 测试数据
- 消息中心：已造 4 条测试消息（daily_review/voice_done/care_followup/echo，3 未读 1 已读）
- 后端 content_id 参数：HTTP 端到端验证通过（精确返回 1 条，id 匹配）

---

## 二、遗留问题（按优先级）

### P0-1：hens-svg 插件在标准调试基座不渲染（图标全空白）

**现象**：所有页面的图标（TabBar 5个、搜索放大镜、消息头像图标、详情星芒/返回/收藏、record 四入口、interview 麦克风/四角星）全部空白。运行时报 `Cannot read properties of undefined (reading 'HensSvgView')`，编译时提示 `uts插件[hens-svg]不存在，请重新打包自定义基座`。

**根因**：hens-svg 是含原生 Kotlin 代码的 UTS 组件（`utssdk/app-android/NativeSvgRenderer.kt`），标准调试基座是 DCloud 预编译的通用基座，不含项目自定义 UTS 插件的原生代码。

**解决方案（二选一）**：
- **方案 A（推荐）**：制作自定义调试基座
  - HBuilderX GUI：运行 → 运行到手机或模拟器 → 制作自定义调试基座
  - 或 CLI：`cli pack --project <path> --platform android --iscustom true --android.packagename com.yishu.guanghua --android.androidpacktype 1`
  - 打好后修改 `client/.hbuilderx/launch.json`：`"playground": "custom"`
  - 云打包已发起一次，输出"打包终止"，状态不明，需重试
- **方案 B**：替换 hens-svg 为 CSS/view 绘制或 image+static png（工作量大，失去矢量着色能力）

**注意**：yishu-photo-watch、yishu-recorder 等其他 UTS 原生插件同样依赖自定义基座（见 P0-3）。

### P0-2：index 页 fetchTimeline 的 uni.request 不回调（时间轴数据不加载）

**现象**：页面停在"正在翻找相纸..."，`loaded` 永远 false。

**诊断过程（已确认）**：
- 通过在 index.uvue 各环节加 toast 诊断，确认：
  - script setup 执行 ✅
  - import 全部成功 ✅
  - onLoad 触发 ✅
  - ensureLogin 成功（wechatLogin 的 uni.request 正常返回）✅
  - reload 调用 ✅
  - flushOpQueue 完成 ✅
  - **fetchAll 调用 ✅**
  - **fetchTimeline(null) → get() → uni.request 的 Promise 既不 resolve 也不 reject** ❌
- 无 success/fail 回调，无超时触发（REQUEST_TIMEOUT_MS=15000），无网络错误 toast

**可能原因**：
1. adb reverse 在 APP 启动后失效（虽然 `adb reverse --list` 显示规则存在）
2. Vapor 模式下 uni.request 对 127.0.0.1 的特定处理问题
3. 并发请求竞态（ensureLogin 之后紧接着 fetchTimeline，某种连接池问题）
4. 后端对 timeline 请求的响应异常（但电脑侧直接请求正常）

**下一步诊断建议**：
- 在设备侧直接验证 HTTP 连通性（Android 无 curl/wget，可用 `adb shell am start -a android.intent.action.VIEW -d http://127.0.0.1:8000/healthz` 打开浏览器测试）
- 在 `doRequest` 的 uni.request 前后加更细粒度诊断（打印 url、header、是否进入 request 调用）
- 尝试将 base URL 从 `127.0.0.1` 改为电脑局域网 IP（`config.ts` 的 `REAL_DEVICE_HOST`），绕过 adb reverse
- 检查后端日志是否收到 timeline 请求（`uvicorn_run.out`）

### P0-3：UTSAndroid is not defined（yishu-photo-watch 相册监听不可用）

**现象**：`PhotoWatchImpl.isReadPermissionGranted` 报 `ReferenceError: UTSAndroid is not defined`。

**根因**：同 P0-1，标准调试基座不含 yishu-photo-watch 的原生 Kotlin 代码。

**影响**：index 页相册监听（自动检测新照片）不可用。但手动选照片/拍照（uni.chooseImage）不受影响。

**解决**：同 P0-1，制作自定义基座。

### P1-1：loadFontFace 报错（自定义字体未加载）

**现象**：`Cannot read properties of undefined (reading 'vm')` at loadCustomFonts，页面用系统默认字体而非 Sarasa Gothic SC。

**可能原因**：Vapor 模式下 `uni.loadFontFace` 的 API 变化，或字体文件路径/格式问题。

**下一步**：查 uni-app x 5.24 Vapor 模式下 loadFontFace 的正确用法，或改用 CSS `@font-face`（如果 Vapor 支持）。

### P1-2：record 页照片上传后端保存链路未端到端验证

- record 页已接入 uploadBatch，但真机上传未验证（受 P0-2 网络问题影响）
- 上传后事件聚合/同步链路（aggregateToEvents + syncClientEvents）未在 record 页验证

### P1-3：页面间跳转链路未系统测试

- TabBar 5 标签跳转基本可用（reLaunch）
- 但 detail 页（从时间轴卡片点击进入）、settings 页（从我的页进入）、record 页（从 TabBar FAB 进入）等跳转链路未系统验证
- 部分页面 pages.json 注册需确认（detail/settings 之前未注册，已在联调中修复）

---

## 三、环境状态

| 项目 | 状态 |
|---|---|
| 后端 | 运行中（PID 15800，端口 8000，healthz ok），路径 `worktree/backend` |
| 设备 | nova 11（FOA-AL00，序列号 `DKS9K23526028855`）已连接 |
| adb reverse | `tcp:8000 → tcp:8000` 已建立（但 APP 侧请求可能不通，见 P0-2） |
| HBuilderX | 5.24.2026081301，已拉起 |
| 前端编译 | 全量编译零错误零警告 |
| 云打包自定义基座 | 已发起一次，输出"打包终止"，状态不明 |

**后端启动命令**（在 worktree/backend 目录下）：
```bash
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**真机部署命令**：
```bash
D:\HBuilderX\cli.exe launch app-android --project D:\GuangH-App\.wt\wrap1-agentA2-ui-restore\client
```

---

## 四、临时文件说明

Worktree 根目录有大量未提交的临时文件，**均不影响代码**，清理时可直接删除：

- `*.py` — 诊断/解析/重写脚本（rewrite_*.py、fix_*.py、check_*.py、probe_*.py 等约 50 个）
- `*_out.txt` / `*_err.txt` — 编译/部署日志
- `client/*.png` — 真机截图（home*.png、dbg*.png、t*.png、screenshot_*.png）
- `design_data_for_agents/`、`design_data_v2/`、`design_data_v3/` — SVG 设计稿解析数据（任务中间产物）
- `backend/uvicorn_run.out` / `uvicorn_run.err` — 后端运行日志
- `client/static/icons/` — 早期提取的 SVG 图标（未使用，TabBar 用内联 SVG）

**已删除但未提交的垃圾文件**（D 状态）：`client/deploy_log*.txt`、`client/screenshot_*.png`（之前误提交进仓库的构建/截图产物，删除是合理清理）。

---

## 五、下一步建议（按优先级）

1. **解决 P0-1 hens-svg 自定义基座**（否则所有页面图标空白，无法完整验证 UI 还原质量）
2. **解决 P0-2 index 页 uni.request 不回调**（否则时间轴数据不显示，核心页面不可用）
3. 自定义基座打好后，逐页真机验证 8 个页面的 UI 还原 + API 联调结果
4. 系统测试页面间跳转链路（TabBar → 各页 → 详情 → 返回）
5. 验证 record 页照片上传端到端链路
6. 修复 P1-1 字体加载问题
7. 清理临时文件，提交干净状态

---

## 六、关键提交记录

```
1d62365 feat(api): 8页面前后端联调对接（9文件，+462/-104）
eb13443 perf(fonts): Sarasa Gothic SC子集化 65.7MB→26.4MB
a586e80 docs: 任务单追加主Agent统一收口记录（8页面提交+3统一修复+待办）
```

各 Agent 页面还原提交（已合入本分支）：
- Agent 1 index：见分支历史
- Agent 2 search：`4a58c10`
- Agent 3 messages：`2713f1f`
- Agent 4 profile：`85e1a59`
- Agent 5 record：`4e3fae8`
- Agent 6 interview：`3dd33bb`
- Agent 7 settings：`3ccba78`
- Agent 8 detail：`7cd7e81`
