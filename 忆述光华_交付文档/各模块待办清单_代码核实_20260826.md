# 忆述光华 · 各模块待办清单（代码核实汇总）

> 核实日期：2026-08-26 ｜ 方式：6 个 subagent 并行代码探查（对照《忆述光华_深度开发设计》10 份文档逐项核实）
> 原始报告：`.cowork-temp/audit_B1_profile.md`、`audit_B2_rag.md`、`audit_B3_events.md`、`audit_B4_sync.md`、`audit_B5a_B5d_voice.md`、`audit_B5b_B5c_B5e.md`
> 图例：❌ 未实现 ｜ 🟡 部分实现 ｜ ✅ 已实现（仅列待办，已实现项从略）

---

## 总览

| 模块 | 核心状态 | 待办数 |
|---|---|---|
| B1 用户画像系统 | 仅冷启动三问落地，画像主体（标注/治理/披露/消费）零实现 | 16 |
| B2 RAG 多路检索 | 核心检索链路已实现；第二层精排/Zvec/真实评测集缺失，新发现 content_type 不一致 bug | 17 |
| B3 照片事件聚合 | L0/L1 端侧+云侧全通；L2/L3 为启发式占位，封面图/时间戳校验缺失 | 17 |
| B4 数据同步与离线优先 | 后端 LWW/游标/软删/分片全实现；客户端几乎未接线，缩略图/流量约束/状态 UI 缺失 | 11 |
| B5a 语音双通道 | 双通道名存实亡（SenseVoice 账号不可用，情绪恒"平静"），客户端语音链路断裂 | 16 |
| B5d 后台任务与录音 | Android 前台服务/WorkManager/attribution 全无，录音中断状态机未集成 | 含于上 |
| B5b 安全护栏 | 规则+qwen-flash chat 模拟护栏；托管护栏/画像敏感/事件级标记未落地 | 9 |
| B5c 分类纠错 | 主体已实现（三层裁决+reflow 流水线真实）；剩调度固化/噪音闸门 2 道/测量方案 | 5 |
| B5e Windows 桌面端 | F9 零代码（not-started 属实） | 6 |

**合计 ≈ 97 项待办**（含文档明确待办与代码缺口；部分项跨模块重叠）。

---

## B1 用户画像系统（16 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | LLM 枚举标注管线（qwen-flash 枚举映射，"标注是映射不是生成"） | ❌ | interview.py:6-9 自述待配 key |
| 2 | 冷启动 L1 兴趣稀疏 5-10 维激活（当前仅 3 个 L0 维度） | ❌ | interview.py:64-76 |
| 3 | 置信度双门槛（普通≥0.7 / 超细性格≥0.8）+ 低置信度事件池（周级复核） | ❌ | 全仓无 confidence 字段/池表 |
| 4 | 开放枚举运行时：同义归一/别名映射/直接新增 value/查重防碎片/新增审计日志/跨用户晋升 | ❌ | enum_policy 仅文档 |
| 5 | 三层披露注入：L1 常驻索引 ~300 token / L2 场景扩展 / L3 全量（RAG/复盘/追问/回响/摘要消费侧零接入） | ❌ | rag.py/notify.py/echo.py 零画像引用 |
| 6 | 消费侧四类 L2 场景扩展：复盘文案/追问生成/回响选择/今日摘要 | ❌ | notify.py:10-13 care_followup 待产品部 |
| 7 | B1-6 对话式增删改查 + 设置页导出按钮 + 画像操作审计日志 | 🟡 仅复述确认 | audit_log 空表 |
| 8 | 敏感标记落地：ProfileSensitive ORM + 5 级处置结构 + echo 双查接线 | 🟡 仅红线护栏 | echo.py:42 P1-06 |
| 9 | 证据锚点+新鲜度戳：profile_l2_evidence ORM 落地 + 消费按新旧打折 | ❌ | schema.sql:232 空表 |
| 10 | DEEPER 行为预测→纠错闭环（下事件验证/回滚/周级批量裁决） | ❌ | — |
| 11 | 时间字段显式剥离（兴趣开始/最近活跃独立字段） | ❌ | dimensions JSONB 无时间戳 |
| 12 | 更新规则：同值强度累加/异值替换+旧值进历史/同日同维度只写一次 | ❌ | interview.py:148 为并集写入 |
| 13 | L2 用户特有维度机制（创建阈值/30·90 天生命周期/3 条证据/晋升） | ❌ | 无 L2 服务 |
| 14 | 枚举集收尾：L0 13 维补 values_detail（confidence_trace/chat_history_recent 连 phrase 都缺）；sensitive_topic "??" 占位符修正；L1 补顶层 phrase+disclosure | 🟡 | docs/画像维度枚举集_l0.json |
| 15 | 枚举集 JSON 与代码接线（当前零加载，需 LLM 标注提示词/F 批对照） | ❌ | §7 剩余收尾 |
| 16 | 定期主动披露（每月 AI 复述新了解项） | ❌ | B1-6 文档决策 |

已实现：冷启动三问全链路、红线级护栏、枚举集文档资产（L1 193 维全 refined）。

---

## B2 RAG 多路检索（17 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | 第二层 LLM 精排（qwen-flash，Ilya 方案 top-50→top-10→top-5） | ❌ | feature_list 自认真实缺口 |
| 2 | 第一层 reranker 默认生效（已接线+模型已下载，但 rerank_enabled=False 默认关，CPU 40s 超门禁；GPU 部署后开） | 🟡 | config.py:101-104 |
| 3 | 以图搜图延迟 P95=7629ms 超 3s 门禁 | ❌ | image_search_report.json |
| 4 | 图片塔换 Qwen3-VL-Embedding（tongyi-embedding-vision-plus 视觉向量，当前为 BGE-M3(caption) 替代路径） | 🟡 | vector_store.py:164-168 |
| 5 | ⚠️ 新发现严重 bug：content_type 值不一致（检索过滤用 "image"，生产 photo 点 payload 为 "photo"）→ 生产文字搜图/以图搜图空结果（评测集用 "image" 索引故未暴露） | ❌ | rag.py:285/427 vs pipeline.py:184 |
| 6 | Windows 端 Zvec 离线检索（02b 双轨决策，零实现且 feature_list 未登记） | ❌ | 仅交付文档提及 |
| 7 | 上线评测集：50 条真实查询 + faithfulness≥0.9/relevancy≥0.85/context precision≥0.8（三指标零实现） | ❌ | B2设计落实度对照.md:57 |
| 8 | 评测体系 5 项落地：run_eval --truth-a、B/C/D 语料 ingestion、expect_empty 负样本钩子、Query 改写正确率评测、跨模态 route 评测 | ❌ | RAG评测体系与门禁标准.md §6 |
| 9 | TAP-RAG 升级版（LLM 任务感知路由 + 四类任务披露策略差异化；LLM route_query 接口存在但未调用，规则路由 route_acc=1.0 有意保留） | ❌ | rag.py:141-150 |
| 10 | 分层门禁矩阵按真实集校准（person/place/time filter 层未测，temporal 层"空结果即正确"占位） | ❌ | 待真实数据 |
| 11 | 生产 payload 补齐 place/tags 字段 → NER place/tag 过滤真正生效（当前仅空结果回退兜底） | ❌ | pipeline.py:87-93 |
| 12 | mixed 意图不可达（规则路由只产出 image/text，mixed 双路融合分支无入口） | 🟡 | rag.py:141-150 |
| 13 | LLM 改写真实链路验证（llm_rewrite_enabled 默认 True，待 DASHSCOPE key） | 🟡 | RAG测评报告 结论3 |
| 14 | corpus-A 剩余 61 张截图 caption 补齐 | 🟡 | 差距清单 P0 |
| 15 | 溯源 event_id/event_title 恒 None（事件级归因待 B3 接线） | 🟡 | rag.py:266-267 |
| 16 | Qdrant 降级后 PG 全文检索兜底（注释声称有，实际降级=空结果） | 🟡 | rag.py:339-344 |
| 17 | 时间词表扩充（去年夏天/上上周/三年前未覆盖，仅 4 个规则） | 🟡 | rag.py:18-23 |

已实现：Qdrant named vectors+payload filter、BGE-M3 dense+sparse+加权 RRF 0.7/0.3 k=60、caption 双写、溯源 trace、时间解析+N ER+空结果回退、四路降级、文字检索门禁 PASS（hit_rate@3=0.9091、P95<3s）。

---

## B3 照片事件聚合（17 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | L2 LLM 语义归并（qwen-flash 裁决+标题生成，只看元数据） | ❌ | 启发式候选占位 pipeline.py:237 |
| 2 | L2 地点域连续（5km/12hr）触发条件 | ❌ | 仅标签分组 |
| 3 | L3 生命周期状态机（活跃 30 天→静默→归档）+ 7 天窗口 | ❌ | 仅全量标签计数 |
| 4 | 封面图选择（人脸优先+质量分；L2 时间居中 / L3 独立封面；cover_content_id 永为空） | ❌ | models.py:235 |
| 5 | 时间戳可信度校验（EXIF vs 修改 vs 导入时间，标记"时间存疑"降级） | ❌ | 全库无 |
| 6 | GPS 漂移：单点众数纠正、系统性降级"不猜"、步行 6km/h 阈值启用（现仅速度校验+坐标置空） | 🟡 | pipeline.py:93-112 |
| 7 | L2 <0.7 独立"待确认"区（状态枚举/筛选/UI，现仅 confidence=0.6+draft） | 🟡 | events.py:222 |
| 8 | 算法侧 confirmed 保护（L1 追加成员/L3 同名重建需跳过用户背书事件） | 🟡 | events.py:141-155 |
| 9 | 增量"先匹配现有事件、超限才分裂"（现旧簇保留+新簇独立） | 🟡 | pipeline.py:153-157 |
| 10 | 30min 保守模式正式开关（端云可切换，现仅注释+参数覆盖） | 🟡 | agg_config.uts:7 |
| 11 | 聚合预处理去重步骤（客户端感知哈希，现仅 DB 唯一约束） | 🟡 | upload.py:178 |
| 12 | 反向"照片→事件"查询入口（照片详情页"属于"列表，无照片详情页/API） | ❌ | client/pages 无 |
| 13 | L2/L3 真值数据（50-100 张真实照片 + 3-5 天事件聚合真值） | ⏳ 等团队 | progress.md:276 |
| 14 | 30s 验收端到端计时实测 + 埋点（链路通但从未计时） | 🟡 | progress.md:186 |
| 15 | 【文档待办】产品部验收标准更新（30min 降级 L0 层；新增 L1 日聚合/L2 主题流分离正确率验收） | 📄 | B3 §8 |
| 16 | 【文档待办】端侧 ST-DBSCAN 开发量列入规划 | ✅ 已闭环（真机 10/10） | — |
| 17 | L2/L3 候选中 OCR 内容维接入（ocr_text 字段已备未用） | 🟡 | pipeline.py:40-54 |

已实现：L0 ST-DBSCAN（60min/500m/min_pts3）+ L1 日聚合（深夜归前一天）、端云双实现参数一致+真机 10/10、photo↔event 多对多（event_items）、连拍折叠、geohash 逆编码缓存、稀疏并入日卡片、确认/合并/拆分/改名存规则（EventEditLog）。

---

## B4 数据同步与离线优先（11 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | 缩略图管线缺失（thumbnail_key 无写入方）→ Windows 跨端"默认拉缩略图"无法实现 | ❌ | models.py:101 |
| 2 | 软删除 30 天物理清理缺失（deleted_logs.cleanup_status 无消费方，墓碑只增不清理） | ❌ | sync.py:120 |
| 3 | 客户端 B4 同步协议未接线（/sync/push、/sync/pull、/sync/reconcile 客户端零调用——字段级 LWW 主链路只有后端；客户端离线 op_log 非 SQLite、缺 status/retry_count/created_at） | ❌ | event_ops.ts:176 |
| 4 | 流量约束未实现（无 WiFi/蜂窝判断、无蜂窝缩略图+元数据路径、无"立即上传原图"入口） | ❌ | uploader.ts |
| 5 | 2 小时定时兜底缺失（无 WorkManager/setInterval 定时同步，仅前台+ContentObserver 两路） | ❌ | 全仓无 |
| 6 | 同步状态 UI 未实现（待上传图标/逐条失败标红可重试/暂停横幅/"继续上传"，仅进度浮层） | ❌ | index.uvue:104 |
| 7 | 批量失败暂停+恢复未实现（无连续失败计数与暂停语义；照片上传为立即重试，退避仅事件上云有） | ❌ | uploader.ts:37 |
| 8 | 微信图片云端原件未实现（image/voice 仅记 media_id，媒体下载→COS 后置） | ❌ | service.py:57-67 |
| 9 | code2session 未接入（恒 mock，auth.py TODO(T1)；待 WECHAT_APPID/SECRET + 企微三件套） | ❌ | auth.py:153 |
| 10 | COS 开通（账号/密钥/STS 角色未就绪，生产仍 fake/fs；代码已就绪） | ❌ | config.py:66 |
| 11 | 客户端真分片（>8MB 单块协议，留 Windows 波次，已知后置非缺陷） | 📄 | uploader.ts:26 |

已实现：后端分片上传状态机+断点续传、原件上云、存储抽象（fake/fs/minio/cos）、字段级 LWW+云端胜+提示、软删墓碑、op_id 幂等、端云对账、微信回调验签解密/msg_id 幂等/只收找/软删本条/文本敏感排除。

---

## B5a 语音双通道 + B5d 后台任务与录音（16 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | 验证阿里云 FunASR API 是否带情绪标签输出（文档明确待办，未做） | ❌ | B5a §5 |
| 2 | SenseVoice（sensevoice-v1）账号开通/替换可用情绪模型——当前生产声学情绪恒"平静"，双通道名存实亡 | ❌ | asr.py:28,227 |
| 3 | 音频事件 12 类取 3（笑声/静音/键盘环境音）读取与消费 | ❌ | asr.py:233-261 |
| 4 | 人声检测输入分布兜底（无人声→不产生情绪标签+标"疑似非口述"） | ❌ | asr.py:175-202 |
| 5 | 噪音降权（噪音大时 audio 权重降为持平） | ❌ | 全仓无 |
| 6 | 段级情绪合并重写为"时长最长段主导+峰值保留标记"（现为"非平静优先"） | ❌ | asr.py:370-375 |
| 7 | 客户端长录音链路（录音上限 3.5min+上传 8MB → >5min VAD 分段路径无入口；需音频持久化上传端点） | ❌ | voice.ts:22 |
| 8 | 情绪落库链路修复（客户端语音只传 text 无音频 → pipeline 直接 return → 情绪/分类/索引全不落库） | ❌ | voice.ts:151-175 |
| 9 | 双通道冲突合并（综合情绪取强、来源 audio/text/both、声学权重更高） | ❌ | 全仓无 |
| 10 | 情绪关怀分层触发逻辑（SAD 分原因/ANGRY 陪伴/深夜轻量/频次递减、<0.7 不触发）+ 文案库 + voice_done 通知接线 + 22:00 复盘调度 | ❌ | notify.py:10-11 |
| 11 | events.emotion 写入（事件层情绪消费） | ❌ | models.py:239 零写入 |
| 12 | 录音中断状态机（RECORDING→INTERRUPTED→恢复/30min 自动结束）+ MediaRecorder.resume()/降级拼接，接入 UTS（POC 服务按 wav 16k 契约重做；POC-02 声称 PASS 与代码事实不符） | ❌ | RecordingService.kt |
| 13 | Android 前台服务（microphone/dataSync 互斥、短命化）+ WorkManager 队列（P0-P4、WiFi 约束、指数退避）+ attribution tag 体系定稿与落地（全仓零命中） | ❌ | B5d §3/§5 |
| 14 | ASR 适配器抽象化（配置化 max_duration、Hy ASR/讯飞通道） | 🟡 | asr.py:277-310 |
| 15 | Windows App 内 30min 定时 + 计划任务（随 F9 第四波，未开始） | ❌ | — |
| 16 | 关怀文案库（待产品部提供） | ❌ | notify.py:10-11 |

已实现：ASR 接口双通道+降级+mock（11 测试）、FunASR 真实可用（paraformer-realtime-v2）、VAD 分段（>5min，服务端）、≤5min 整段、无停顿均分硬切、后端 RQ 两级队列（P0/P1 high、P2-P4 low）、相册监听插件（ContentObserver，页面级）。

---

## B5b 安全护栏（9 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | 百炼托管护栏接入（qwen_response_check / X-DashScope-DataInspection，替换 qwen-flash chat 模拟）+ 验证计费单价 | ❌ | dashscope.py:25 注释自认 |
| 2 | 追问/回响模板骨架池 30-50 建设（产品运营） | ❌ | P2-ECHO gate |
| 3 | 事件级敏感标签分类器（规则词表+LLM 补漏，5-8 类）→ 入库管线写 sensitive_tags（当前零写入） | ❌ | models.py:96 |
| 4 | 画像级敏感落地：重建 profile_sensitive 表（⚠️ baseline 迁移已 DROP）+ ORM + 回响 L1 校验接线 | ❌ | 431bcaa8bd54:28-32 |
| 5 | 敏感有效期（主动提及计数 ≥3 次降级 + 画像永久） | ❌ | 全仓无 |
| 6 | 违规词回流（检测违规→加入本地规则表；sensitive_words 表需重建，baseline 已 DROP） | ❌ | — |
| 7 | 可插拔检测器接口抽象（规则/托管/自部署三实现） | ❌ | 仅 moderate() 单入口 |
| 8 | 用户内容上架前接阿里云内容安全（当前腾讯 CI image_audit + moderate 顶替） | 🟡 | tencent_ci.py:59 |
| 9 | 摘要/搜索规则级敏感过滤显式化（可选） | 🟡 | rag.py 无 guard |

已实现：规则预检（sensitive_words 5 词表 23 万字节+号码打码+归一化）、qwen-flash chat 模拟护栏（fail-safe 拒发）、回响敏感双查（sensitive_status+moderate）、图片腾讯 CI 审核（Porn/Illegal/Teenager）。

---

## B5c 分类纠错（5 项）

| # | 待办 | 状态 | 关键落点 |
|---|---|---|---|
| 1 | reflow 调度固化（RQ scheduler vs 系统 cron；当前仅手动脚本 reflow_global.py） | ❌ | refactor-plan P3 |
| 2 | 批量操作保护（>10 条确认弹窗 + 24h 撤销，客户端） | ❌ | 全仓无 |
| 3 | 权重衰减（时间权重、连续同方向 ≥3 次→强规则） | ❌ | 仅"最后为准" |
| 4 | "来回改 2 次以上→轻提示"交互 | ❌ | — |
| 5 | 纠错 7 天准确率提升 ≥10% 测量脚本交付 + 产品部口径确认 | 🟡 | feature_list F4 |

已实现（主体 ✅）：correction_log 表（含 500 条上限）、三层裁决（个人规则余弦>0.8 同类型 → 全局 SetFit → 共性纠错回流）、主动纠错立即生效、被动确认 ≥3 次一致、一致性校验（3 天回改窗口）、reflow 流水线真实（≥50 条触发/训练前备份/门禁 ≥75%/staging 防污染）。

---

## B5e Windows 桌面端（6 项，F9 全部未启动）

| # | 待办 | 状态 |
|---|---|---|
| 1 | Electron + Python sidecar 骨架（M4） | ❌ |
| 2 | PP-OCRv5 mobile 集成 + 置信度门控（>0.85 直接用 / 0.6-0.85 标注 / <0.6 或手写复杂→云端） | ❌ |
| 3 | AI 引擎适配器接口（云端 qwen-flash MVP，Ollama P1） | ❌ |
| 4 | 批量导入（预扫描/去重预筛/分批 200/进度落盘续传） | ❌ |
| 5 | 三档离线降级 + 离线数据入队联网后补 | ❌ |
| 6 | 删除告知文案（"此操作只删除云端副本，本地文件请自行删除"）列入产品验收 | ❌ 仅文档 |

---

## 横切：阻塞性前置项（拿 Key / 账号类）

1. DASHSCOPE key（B1 标注管线、B2 LLM 精排/改写、B3 L2 归并、B5a 情绪模型、B5b 托管护栏 全部依赖）
2. SenseVoice 账号开通（B5a 双通道名存实亡根因）
3. 腾讯云 COS 密钥 + STS 角色（B4 生产存储仍 fake；OPS-SECRETS 待办④）
4. WECHAT_APPID/SECRET + WECOM_CORP_ID/TOKEN/AES_KEY（F6 code2session 恒 mock）
5. 阿里云内容安全（上架前必接，B5b-8）
6. 关怀文案库 / 追问模板骨架池 / 纠错测量口径（3 项产品部配合项）

## 横切：新发现/高危缺口（本次核实新增）

1. **B2 content_type 值不一致（"image" vs "photo"）** → 生产文字搜图/以图搜图将空结果（评测未暴露，需立即修）
2. **B1/B5b profile_sensitive 表已被 baseline 迁移 DROP** → 画像级敏感与回响双查的"更前置校验"无载体（P1-06）
3. **B5a 客户端语音链路断裂** → 真机录的语音情绪/分类/索引全不落库（pipeline 对无音频 voice 直接 return）
4. **B5d POC-02 声称"中断状态机 PASS"与代码事实不符**（POC 仅 start/stop，无中断监听；且 m4a 格式与后端 wav 契约不符）
5. **B3 端云参数"单一来源"实为双份硬编码**（靠 AGG-016 双跑测试兜底）
