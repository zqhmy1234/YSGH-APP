# 01 · 深度开发设计完成度评估（vs 完整 MVP）

> 2026-08-27｜未动代码｜B5e Windows 桌面端已从 MVP 中去除

## 1. 设计层：B1–B5d 全部收敛

- B1 画像系统、B2 RAG 检索、B3 照片事件聚合、B4 数据同步、B5a 语音双通道、B5b 安全护栏、B5c 分类纠错、B5d 后台任务 → 8 份设计文档全部标记「✅ 已收敛」。
- B5e Windows 桌面端已移出 MVP（F9 仍标 not-started），相关桌面端用户故事与验收条款（如"Windows 删除告知"）同步失效/降级为 Android 端处理。

## 2. 实现 vs 设计对照表

| 模块 | 核心能力 | 后端实现状态 | 客户端实现状态 | 证据 |
|---|---|---|---|---|
| B1 画像 | 枚举化标注（LLM 映射+证据锚点+双门槛 0.7/0.8）、冷启动三问、L1 索引披露、对话式管理 | ✅ 真实 qwen 通道+确定性 mock 同构，25 测试 | 🟡 冷启动访谈页真机通；画像对话式查看/修改/导出 UI 未建 | services/profile_annotator.py、profile_schema.py、api/interview.py |
| B2 RAG | BGE-M3 dense+sparse、Qdrant RRF、NER/意图/改写、双层 rerank、PG 降级、溯源、以图搜图 | ✅ 全实现（含第二层 LLM 精排 llm_rerank）；负样本误召回 0.5714 待真实语料重校 | ✅ 搜索页+溯源+以图搜图真机通过 | services/rag/*、vector_store.py、api/search.py |
| B3 事件聚合 | 四层模型（L0/L1 端侧+ L2/L3 云侧候选）、GPS 漂移、封面、confirmed 保护、手动操作、事件级敏感 | ✅ L2/L3 为候选 draft 落库；LLM 归并真实 qwen 通道验证 | ✅ 时间轴/操作菜单/待确认区真机通过 | events/aggregate.py、event_aggregation/*、api/events.py、api/event_items.py |
| B4 数据同步 | 字段级 LWW、op 队列幂等、增量拉取、reconcile、软删 30 天、缩略图、流量约束、微信媒体上云 | ✅ sync 46 路径 + 30 天清理 job；COS key 已配（dev 用 fs 后端） | ✅ sync_client/UploadStatusBanner；蜂窝暂缓真机补验待做 | services/sync.py、thumbnails.py、api/sync.py、api/upload.py |
| B5a 语音 | FunASR 多格式转写、SenseVoice 声学情绪、VAD 长录音分段、音频事件 3 类、情绪消费/关怀 | ✅ 真实 FunASR 云端+本地 SenseVoice 已验证；J 消费域集成完成 | ✅ 录音/长录音分片/中断状态机；来电实测待做 | external/asr/*、pipeline_ext/emotion.py、api/asr.py |
| B5b 护栏 | 分级护栏、托管护栏（qwen_response_check）、规则预检、三层敏感词表、事件级敏感、违规回流 | ✅ 托管优先 chat 兜底；可插拔检测器 | ✅ 已接线（搜索/画像敏感过滤） | llm_ops/guard_managed.py、moderate.py、external/content_safety.py |
| B5c 纠错 | SetFit 5 类、三层裁决、共性纠错 ≥50 微调、三道噪音闸门、异步化 | ✅ classify/arbitrate 异步入队+轮询；correction_log 向量匹配 | ✅ 点标签纠错真机通过 | services/classifier.py、correction.py、api/classify.py、api/corrections.py |
| B5d 后台 | RQ worker 队列（优先/幂等/重试）、process_content 分阶段提交 | ✅ 后端侧实现 | ✅ 客户端 WorkManager+前台服务短命化真机通过 | workers/*、services/pipeline.py、uni_modules/yishu-background-tasks |
| F6 微信 | 回调验签/解密/幂等入库、code2session、内容安全适配器、微信找 | 🟡 沙箱已通+代码就绪；生产待企微三件套+微信 AppID/Secret（未配 501） | —（微信为独立入口） | services/wechat/*、api/wechat.py |

## 3. 后端待 key/部署才生效的缺口（代码已就绪）

1. 微信生产：企微回调三件套 + code2session AppID/Secret（生产 501 门控）。
2. 短信验证码真实通道（TODO T1）：mock 生产 501 冻结，公开上线前需接阿里云短信。
3. uni-push 真实厂商通道（notify TODO T1）：推送走 mock。
4. COS 生产切换：本地 dev 用 fs 存储后端；上生产切 cos（key 已配）。
5. REFRESH_TOKEN_HMAC_KEY：生产部署需独立强随机密钥（G1 遗留）。
6. cleanup 孤儿对象扫描（P0-6）：仅软删墓碑清理，孤儿扫描待补。

## 4. 后端尚未完成的关键缺口清单

1. F6 微信"找"端到端实网（企微 key 到位前无法验收 10s/3s 门禁）。
2. 真实短信通道接入（上线门槛）。
3. 性能压测（100 并发 / P95 曲线）未做。
4. 孤儿对象扫描清理 job（P0-6 遗留）。
5. 上游通知推送（uni-push 厂商通道）待接。

## 5. 量化结论

- 功能代码口径：**85-90%** 完成（后端全部模块真实实现、客户端核心页面端到端打通、测试 502+ passed）。
- 可对外内测口径：**60-70%**——被外部凭证、真实数据、合规三申请 + 少量 UI/部署收尾阻塞。
- 工程基线佐证：CI 双层门禁双绿（#21）、真机 nova 11 多链路 E2E、覆盖率 78.46%。
