# 04 · 团队需提供的数据支持与规格文档清单

> 2026-08-27｜规格权威：docs/真值数据规格标准_v1.md + research/truth-data/templates/（8 份示例）+ scripts/validate_truth_data.py 校验器
> ⚠️ B5e 桌面端已移出 MVP → H 批（OCR 真值）随之下架/暂停，避免白收。

## 1. 真值数据（产品部人工采集，每周五交付）

| 批 | 数据内容 | 目标量 | 当前状态 | 消费方 / 门禁 |
|---|---|---|---|---|
| A | 真实搜索查询（命中类 + 干扰类负样本，真实负样本≥70%） | ≥50 条（命中类≥35 + 干扰类≥10） | 🟡 仅 a_v1 一版（未达标/分层不全） | RAG 搜索上线评测（M1 硬依赖；负样本误召回 0.5714 待重校） |
| B | 真实文字碎片 + 5 类标注 | 100–200 条（每类≥20） | ⏳ 未交付 | SetFit 分类校准（M1≥75% / M2≥80%） |
| C | 自述录音 + 人工转写 + 情绪听感（clean/noisy/bgm 分层） | ≥60 段 | ⏳ 未交付 | 语音 WER/CER 校准 + 情绪识别门禁（happy/sad≥85%） |
| D | 照片事件真值（D1 短期 3–5 天 + D2 长期 30 天，人脸打码） | D1 50–100 张 + D2 30 天 | ⏳ 未交付 | L0–L3 事件聚合验收（成员 F1） |
| E | 纠错样本（复用 B 批，模型判错清单） | 10–20 条 | ⏳ 未交付 | 三层裁决纠错门禁（7 天提升≥10%） |
| F | 画像标注真值（对照 docs/画像维度枚举集清单_v1.md） | 50–80 条（含 10–15% 反例） | ⏳ 未交付（枚举集 v1 已定稿） | 画像标注验收（映射完整性/不臆造） |
| G | 护栏基准：用户个人敏感 30–50 + 边界难例 30–50（preset 层借用 SafetyBench） | 自收 60–100 条 | ⏳ 未交付 | 护栏漏杀率趋 0 / 误杀率≤5% |
| H | OCR 手写真值（截图/老照片/复杂版面借用公开集） | 自收 15–30 张 | ⏳ 未交付 | **Windows 桌面端 OCR —— B5e 已移出 MVP，本批暂停** |

规格文档：✅ 已存在且完整 —— `docs/真值数据规格标准_v1.md`（A–H 八批字段/分布/验收口径）+ `research/truth-data/templates/` + 校验器 `scripts/validate_truth_data.py` + 目录 `research/truth-data/{a..h}/` + manifest 机制。

## 2. 产品/运营文案类数据（非标注，但阻塞功能验收）

| 数据项 | 用途 | 当前状态 |
|---|---|---|
| 关怀文案库（B5a 情绪关怀分层） | SAD 追问/ANGRY 陪伴/深夜轻量/频次递减文案 | ⏳ 缺（触发逻辑已实现） |
| 追问/回响模板骨架池（B5b） | 30–50 个骨架（照片/文字/语音各 10+）+ 30 天不重复 | ⏳ 缺（MVP 仅回响、池未建） |
| 隐私政策定稿（初稿见 忆述光华_隐私政策初稿.md） | OCR/图片向量/护栏均上传云厂商需知情定稿 | ⏳ 待定稿 |
| 纠错测量口径确认（B5c） | "纠错后 7 天同类准确率提升≥10%"的测量方案 | ⏳ 待确认（埋点已就绪） |
| 合规三申请 | 企微认证 / ICP 备案 / 软著（M0 硬依赖，5–6 周串行） | ⏳ 未提交 |

## 3. 规格文档缺口

1. A/C/D/F/G/H 批**评测程序**多为"待补"状态（`run_eval --truth-a`、`run_wer_bench --truth`、画像/护栏/OCR 评测器未写完）——**不影响产品部采集**（规格文档已完整，先收数据入 manifest，评测器后续补齐）。
2. H 批规格文档存在但**因 B5e 移出 MVP 应标记暂停**，避免团队白收 OCR 手写数据。

## 4. 采集流程提醒（产品部）

1. 先收 B/C/D 语料（或同步收），A 批 expected 引用其 id。
2. 按 templates/ 模板导出 JSON，命名 `{批次}_v{n}.json`，放入对应子目录。
3. 跑 `python scripts/validate_truth_data.py` 自检全绿。
4. 隐私铁律：user_id_hashed 脱敏、C 批 self_talk_only=true、D/H 批人脸/个人信息打码、涉第三方信息置空。
