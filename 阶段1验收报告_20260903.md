# 阶段 1 验收报告：数据库 37 表对齐（2026-09-03）

> 分支：codex/photo-merge（基于 develop@cea5025）｜数据库：本机 docker-compose（yishu-postgres）
> 说明：物理表 40 张 = 37 张图纸业务表 + profile_annotation_pool（上游自加）+ upload_tasks/upload_chunks（上传基础设施，保留）

## 1. 迁移版本
- alembic_version = c0d1e2f3a4b5（v4 · 37 表对齐）

## 2. 结构体检（运行中的 yishu 库，实测）

| 检查项 | 结果 |
|---|---|
| 物理业务表数 | 40（+ alembic_version 台账表） |
| contents 新列 | file_id / raw_text / normalized_text / normalized_text_hash / summary / summary_strategy / image_phash / text_vector_id / image_vector_id 全部存在 |
| contents 旧列 | perceptual_hash / qdrant_text_id / qdrant_image_id 已不存在（0 个） |
| 去重唯一索引 | uq_contents_user_image_phash、uq_contents_user_text_hash、uq_contents_user_client_generated_id（部分唯一） |
| files 表 | 16 列齐全（SHA256 file_hash + 部分唯一索引 uq_files_user_hash） |
| content_chunks 表 | 10 列齐全（content_id+chunk_index 唯一） |
| correction_log | content_embedding vector(1024) + HNSW 索引（qdrant_point_id 保留过渡） |

## 3. 带数据升级演示（实测通过）

升级前（旧结构 3 条样本）：
- photo ph-aaa-111 / t-1 / i-1；photo ph-bbb-222 / t-2 / i-2；text（无哈希）

升级后（v4 迁移自动完成）：
- photo → image_phash=ph-aaa-111、text_vector_id=t-1、image_vector_id=i-1
- photo → image_phash=ph-bbb-222、text_vector_id=t-2、image_vector_id=i-2
- text 行不受影响
- 旧列数量 = 0（已彻底移除）

## 4. 程序代码同步
- ORM：File / ContentChunk 新增；Content 列对齐；CorrectionLog 加向量列（py_compile + 元数据加载通过）
- 服务层：perceptual_hash → image_phash 属性切换（接口字段名不变，手机端零改动）
- 依赖：requirements.txt 加回 pgvector（correction_log 向量列）

## 5. 关键说明
- 对外 API 仍收 perceptual_hash 字段（内部落 image_phash），Android 客户端无需改动
- correction_log 的 qdrant_point_id 保留过渡（纠错存储切换到 pgvector 属后续单独任务）
- 本批改动尚未提交；按 AGENTS.md 提交前需跑 review_agent 质量门禁